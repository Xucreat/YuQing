# 微博源修复验证报告（2026-08-06）

## 一、验证背景

修复前，微博数据源（`weibo_mediacrawler`，id=40）连续 3 次采集全部失败，对应三处互不相干的故障：

| 时间 | 状态 | 错误 |
|------|------|------|
| 12:06 | failed | `MediaCrawlerTimeoutError: command timed out after 900 seconds`（qrcode 登录卡死） |
| 14:46 | failed | `MediaCrawlerProfileUnavailableError: ...profiles/manual unavailable`（目录缺失） |
| 15:07 | failed | `MediaCrawlerRuntimeError: real-run gate is disabled`（真实采集门禁未开） |

**已实施的修复**：
1. `.env`：`MEDIA_CRAWLER_REAL_RUN_GATE=false` → `true`（开启真实采集门禁）
2. `.env`：新增 `MEDIA_CRAWLER_LOGIN_TYPE=cookie`（改用 cookie 登录，规避 qrcode 900s 卡死）
3. 创建空目录 `D:/code files/mediaCrawler/MediaCrawler/profiles/manual`（根治 ProfileUnavailable）

修复后已重启后端（PID 484 监听 8000，完整加载 `.env`）。

## 二、验证方法

通过内部脚本直接调用 `CollectorService`（与 `POST /api/collector/run` **完全相同的代码路径**）触发单源 `weibo_mediacrawler` 真实采集，同步等待完成，并核对 `collector_runs` 与 `opinions` 库。
- 触发入口：`CollectorService(include_data_source_keys={'weibo_mediacrawler'})`
- batch_id：`verify_20260806154258`
- 实时抓取产物：`D:/code files/mediaCrawler/MediaCrawler/runs/verify_20260806154258/`

## 三、验证结果

### 3.1 MediaCrawler 实时日志（crawler.log）
- `real_command_started` → `real_command_finished exit_code=0`：**真实门禁已开，命令跑通**（不再是 "gate disabled"）
- `[WeiboLogin.login_by_qrcode] Begin login weibo by cookie` + `Cookie updated successfully`：**cookie 登录生效，无 900s 卡死**
- 全程**无** `ProfileUnavailableError`：`profiles/manual` 目录可用
- 搜索关键词「廊坊」（round-robin 取 1 词，因 `config_json.keywords=[]`），含多条真实 `get_note_full_text` 网络日志（**非 fixture 回放**）
- `raw_count=23 output_count=20`，`read_count=20 success_count=20 failed_count=0 duplicate_count=0`

### 3.2 CollectorService 返回
| 指标 | 值 |
|------|-----|
| fetched_raw / upstream_returned | 20 / 20 |
| created | **6** |
| analyzed | 6 |
| admission_filtered | 14 |
| duplicate / failed | 0 / 0 |
| 自动聚合 | 执行（incremental） |

> 14 条被准入服务拦截属**预期行为**：纯地域词「廊坊」未命中公共信号，被准入层（封顶 35 分）拒绝。这正是此前"只抓到 8 条"现象的成因之一（准入 + 重复 + 旧源抢占三层叠加），并非本次故障。

### 3.3 数据库核对
- `opinions`（source='weibo'）计数：**110 → 116**（+6，与 `created=6` 一致）
- 最新 `collector_run`：`status=success`，`fetched_raw=20`，`created=6`，`admission_filtered=14`，`failed=0`，`error_msg` 为空
  - 对照上一条（15:07）仍为 `failed (gate disabled)`
- 新增 6 条 id 2447–2452，`created_at=15:43:45`（采集时刻），`publish_time` 均为真实有值（本次关键词"廊坊"抓到近期帖，无 NULL）

## 四、结论

**三处故障全部修复，端到端采集闭环恢复正常运行**：
```
真实抓取(20) → 归一化 → 准入(过6/拦14) → 入库(6) → 风险分析(6) → 自动聚合
```
不再出现 gate / profile / qrcode 三类报错，真实微博数据已成功入库。

## 五、已知遗留（非本次故障，建议后续优化）
1. **准入过滤偏高**：纯地域词下 70% 被拦截。若要提升入库量，需调整关键词策略（参考此前《微博源改进实施方案》三方向：时间窗 / 多关键词覆盖 / 正确配关键词）。
2. **region_id 归属偏差**：本次新增记录 `region_id` 落 12/18/19 而非 131000（廊坊），建议将 `config_json.scope_region_codes` 明确绑 131028/131000，避免下游全国视图污染（详见工作记忆「微博 MediaCrawler 接入」已知缺陷③）。
3. **风险模型低估**：对微博口语文本风险评分偏低（此前诊断已记录），不影响入库但影响预警灵敏度。
