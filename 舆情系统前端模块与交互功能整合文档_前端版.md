# 舆情监测系统 · 前端页面模块与交互功能整合文档

> 适用范围：本仓库 `frontend/` 下的 Vue 3 + Element Plus 前端，对应后端以 `/api` 为前缀。
> 文档目标：把分散在各页面/组件中的「页面模块」与「可实现的交互功能」做一次系统整合，便于交接、评审与二次开发。
> 权限说明：前端 `v-if`/`canAccess` 仅为体验层门禁；真正的强制校验在后端 RBAC，下文标注的权限为前端可见性依据，不代表安全边界。

---

## 0. 系统总览

系统采用「单页应用（SPA）+ 侧边栏导航 + 聚合子页」结构：

- **国内 / 外网双域**：舆情、事件、预警、关键词、数据源、采集日志均分国内（domestic）与外网（foreign）两套数据，UI 上通过「国内/外网」切换或独立工作区隔离。
- **聚合页模式**：`/data`（数据管理）、`/system`（系统管理）把多个子页用顶部 Tab 收敛到一个路由下，减少侧边栏条目。
- **全局壳层 `AppLayout`**：承载侧边菜单、顶栏标题、采集入口（`CollectMenu`）、消息铃铛、退出登录；`AlertToastHost` 全局监听新预警并右下角弹窗。
- **三类采集动作**：顶栏「采集」下拉提供「国内全量 / 外网按源 / 外网全量」三档（详见 §1.3）。
- **权限驱动**：统一经 `usePermission`（hasPermission / hasModulePermission / hasAnyModulePermission / isSuperuser）。后端 401 触发登出跳转，403 统一提示「权限不足，请联系管理员」（仅 RBAC 类 403，业务类 403 保留后端语义）。

---

## 1. 导航架构与全局能力

### 1.1 路由地图

| 路径 | 页面组件 | 进入权限（前端） | 说明 |
|---|---|---|---|
| `/` | 重定向 | — | → `/dashboard` |
| `/login` | `Login.vue` | 无 | 登录页 |
| `/dashboard` | `Dashboard.vue` | 登录 | 国内驾驶舱 |
| `/opinions` | `Opinions.vue` | 登录 | 国内舆情列表 + AI 人工复核 |
| `/opinion/:id` | `OpinionDetail.vue` | 登录 | 单条舆情详情 |
| `/foreign` | `ForeignWorkspace.vue` | 登录 | 外网工作区（看板/舆情/事件） |
| `/events` | `Events.vue` | `events:read` | 国内事件中心 |
| `/event/:id` | `EventDetail.vue` | `events:read` | 国内事件详情 |
| `/foreign/event/:id` | `ForeignEventDetail.vue` | `foreign:events:read` | 外网事件详情 |
| `/alerts` | `Alerts.vue` | `alerts:read` | 预警中心（规则 + 记录，国内/外网） |
| `/data` | `DataManage.vue` | keywords/sources/collectors/foreign 任一 | 数据管理聚合（关键词/数据源/采集日志/AI线索审核） |
| `/system` | `SystemAdmin.vue` | users/roles/login_logs/audit_logs 任一 | 系统管理聚合（用户/角色/日志） |
| `/propagation` | `Propagation.vue` | `propagation:read` | 传播溯源 |
| `/ai-search`(含 /web /ai /anspire) | `AiSearch.vue` | `ai:search` | AI 检索外壳 |
| `/command-screen` | `CommandScreen.vue` | 登录 | 指挥大屏（全屏布局） |

> 旧路由 `/keywords`、`/sources`、`/users`、`/roles`、`/login-logs`、`/operation-logs`、`/foreign/alerts`、`/foreign/alert-rules` 均重定向到聚合页对应子页，保留书签兼容。

### 1.2 通用框架与组件

- **侧边栏菜单**：驾驶舱 / 舆情列表 / AI检索 / 事件中心 / 预警中心 / 传播溯源 / 指挥大屏 / 数据管理 / 系统管理，按权限 `visible` 隐藏；支持收起/展开（localStorage 记忆）、移动端抽屉导航。
- **顶栏**：承接 `DataManage`/`SystemAdmin` 的 Tab（`Teleport` 到 `#page-nav-target`）；`/dashboard` 显示国内/外网 `el-radio-group`（cockpitScope）；渲染 `CollectMenu`；用户区含「预警记录 / AI线索审核」消息菜单（红点计数）、退出登录（确认后 `authStore.logout()`）。
- **全局预警 Toast `AlertToastHost`**：新预警右下角弹出（10s 倒计时进度条），可按「查看 / 稍后」；风险等级配色（critical/high 红、medium 橙、low 蓝），数据来自 `useAlertNotifier`。
- **通用组件**：`Pager`（分页，中文 locale）、`SegmentedControl`（分段切换）、`ReportExportDrawer`/`ModuleSelector`（报告导出，见 §6.3）、`EventDispositionDialog`（事件处置，见 §4.3）、`OpinionDetailModal`/`ForeignOpinionDetailModal`（舆情详情弹窗）、`BatchAIModal`（批量研判）、`BochaDetailModal`（检索线索详情）。

### 1.3 三类采集能力（`CollectMenu` + `useCollectionActions`）

| 按钮 | 作用域 | 后端调用 | 权限 |
|---|---|---|---|
| 采集数据 | 国内全量 | `POST /collector/run` → 完成后 `POST /alerts/evaluate` | 仅超管 |
| 采集外网 RSS | 按所选外网源 | `POST /foreign/collect` `{source_ids}` | `foreign:sources:collect` |
| 采集全部已启用外网数据源 | 外网全量 | `POST /foreign/collect` `{all_sources:true}`（二次确认） | `foreign:sources:collect_all` |

> 启动后写入 `collectStore`，触发 `ForeignCollectionLogView`/`CollectionLog` 的实时进度卡，并派发 `foreign-data-refresh` 刷新列表。

---

## 2. 驾驶舱与态势感知

### 2.1 国内驾驶舱 `/dashboard`（`Dashboard.vue`）
- **KPI 卡**：总舆情数、今日新增、高风险、事件数、采集状态（运行中/等待触发）。
- **态势区**：态势等级（平稳/需警惕/紧张）+ 风险率/负面率；权限内显示「导出舆情报告」按钮（`openReportDrawer`，见 §6.3）。
- **舆情趋势图**：分段控件切换 7/14/30 天。
- **预警滚动列表 / 实时快讯**：自动滚动，点击（带 `opinion_id`）跳转舆情详情弹窗。
- **分布图**：来源分布（柱状）、情感分布（`SentimentDonut`）、地域分布 TOP。
- **热点词云**：双模式「风险关键词 / 热点主题」；热点主题模式点击词 → 打开「热点主题相关事件」抽屉（懒加载，可进事件详情）。
- 接口：`GET /dashboard/stats`、`/dashboard/recent`、`/dashboard/alerts`、`/dashboard/hot-keywords`、`/collector/status`、`GET /events`（按热点主题）。

### 2.2 指挥大屏 `/command-screen`（`CommandScreen.vue` + `command-screen/*`）
- 独立全屏布局（`FullscreenLayout`，无侧栏），深色主题，独立轮询刷新。
- **顶部 `ScreenHeader`**：链路状态徽标（LIVE/STALE/DOWN）、实时时钟、全屏/退出全屏、返回系统。
- **`KpiBar`**：5 个 KPI（舆情总量/今日新增/高危/事件总数/监测信源）+ count-up 动画 + Sparkline（30s 刷新）。
- **三列布局**：左（情感分布环图 + 来源 TOP8 条形）、中（`ChinaMap` 省级 choropleth + 市级下钻 + 自动轮播 + 趋势折线）、右（`HotKeywordList` 热词榜 + `FeedList` 实时快讯/预警滚动 + `RegionDetailList` 市县级 TOP）。
- **底部 `ScreenTicker`**：无缝滚动实时摘要。
- 轮询：`stats`(30s) / `recent`(15s) / `alerts`(15s) / `sourceCount`；总状态取三路最差。`exitScreen()` → `/dashboard`。

---

## 3. 舆情管理

### 3.1 国内舆情 `/opinions`（`Opinions.vue`）+ `/opinion/:id`
**列表页交互：**
- 国内/外网切换；Tab「国内舆情 / AI 人工复核」（复核见 §6.1）。
- 筛选栏：来源、类型、相关性（高/低）、情感、级别（高危/中危/低危）、展示口径切换（当前风险/系统规则/AI研判，localStorage 持久化）、日期区间、关键词搜索（回车/清空）、搜索/刷新。
- 超管可勾选「显示低价值内容」。
- 批量 AI 研判（`BatchAIModal`）、运行记录（历史批次弹窗）。
- 行内/批量操作：修改情感（胶囊）、删除（超管）、批量删除；AI 研判进度条（processed/total、成功/失败/跳过、取消、重试失败）。
- 表格列含风险评分（AI 徽标）、分析状态、发布时间、操作（删除）；点击行开详情弹窗。

**详情页 `/opinion/:id`（`OpinionDetail.vue`）：**
- 原文 + 准入分析（相关性/命中项）+ AI 分析卡（评分/情感/摘要/关键词/建议/时间）。
- 「触发 AI 分析」按钮（`ai:analyze` 且非 processing）。
- 接口：`GET /opinions`、`GET /opinions/:id`、`PATCH /opinions/:id`、`DELETE /opinions/:id`、`POST /analyze/:id` 等。

### 3.2 外网舆情（`ForeignWorkspace` → `ForeignOpinionListView` + `ForeignOpinionDetailModal`）
- **列表 `ForeignOpinionListView`**：搜索（标题/摘要/正文）、来源、类型(10类)、命中关键词、语言、风险口径(current/rule/ai)、风险等级、分析状态、发布时间区间筛选；批量 AI 研判、运行记录；实时进度条；行操作「分析/重新分析」。
- **详情弹窗 `ForeignOpinionDetailModal`**：跳转原文（新窗）、翻译/显示原文切换、风险口径三卡（当前/系统规则/AI研判历史）、查看分析历史、触发/重新触发 AI 分析（`foreign:ai:analyze`）。
- 接口：`GET /foreign/opinions`、`POST /foreign/risk/{id}/analyze`、`GET /foreign/opinions/{id}/detail`、`POST /translate`、`/foreign/ai-analysis/batch/*`。

---

## 4. 事件处置

### 4.1 国内事件 `/events` + `/event/:id`
- **事件中心 `Events.vue`**：标题搜索（防抖）、风险等级/影子风险（自定义浮层）、主题、处置状态、趋势筛选；「更多操作」展开地区/热度/时间区间；「手动聚合」（`events:write`，后台任务轮询）；运营状态 chips（前端过滤）；行操作：查看、处置（`EventDispositionDialog`）、删除。
- **事件详情 `EventDetail.vue`**：概览卡 + 处置按钮；态势条；研判与统计区（`EventAnalysisStats`）；Tabs「关联舆情 / 关联预警」，关联舆情行开详情弹窗。
- 接口：`GET /events`、`POST /events/aggregate`、`DELETE /events/:id`、`GET /events/:id`、`GET /events/:id/situation`。

### 4.2 外网事件（`ForeignWorkspace` → `ForeignEventsView` + `/foreign/event/:id`）
- **候选/事件列表 `ForeignEventsView`**：自动聚合状态条（enabled/调度/阈值/窗口）；「候选 Dry-Run」重建候选；子 Tab「事件候选 / 外网事件」；候选行「确认/拒绝」（`foreign:events:confirm`）；事件行查看/处置/删除（`foreign:events:write`）。
- **详情 `ForeignEventDetail.vue`**：概览 + 处置；态势条（含语种/置信度/事件类型）；`EventAnalysisStats`；关联舆情/预警 Tabs。
- 接口：`GET /foreign/events/candidates`、`/foreign/events`、`POST /foreign/events/candidates/{id}/confirm|reject`、`DELETE /foreign/events/{id}`、`GET /foreign/events/{id}`。

### 4.3 统一事件处置弹窗 `EventDispositionDialog.vue`（国内+外网共用）
- props：`scope('domestic'|'foreign')` / `modelValue` / `eventId` / `alignCenter`。
- 交互：状态流转按钮（按 `nextStatus` 规则与 `canChangeStatus` 启用/禁用）；合并到其它事件（远程搜索目标 + 原因）；拆分舆情（多选迁出 + 原因）；添加备注（≤5000 字带计数）；右侧处置记录时间线。
- 接口（外网 base=`/foreign/events`）：`PATCH {base}/{id}/status`、`POST {base}/{id}/actions`、`POST {base}/{id}/merge`、`POST {base}/{id}/split`。
- 权限：`events:write`（国内）/ `foreign:events:write`（外网），后端进一步约束 status/merge/split。

---

## 5. 预警中心 `/alerts`（`Alerts.vue`）

- Tab「预警规则 / 预警记录」+ 国内/外网切换；外网分支通过 `?scope=foreign` 进入。
- **规则页**：刷新、新增规则、执行评估（「检查 X 条，生成 Y 条」）；国内规则启用开关/编辑/删除；外网规则启用/停用/删除。
- **记录页（国内）**：筛选（等级/处置状态/隐藏误报/日期）；操作列「处置」→ 处置弹窗（状态/备注）。
- **记录页（外网）**：筛选（隐藏误报/严重度/来源/日期）；关联舆情点开 `ForeignOpinionDetailModal`。
- **外网双列处置（Phase 15-C）**：`status`（生命周期：triggered/acknowledged/resolved/suppressed/failed）+ `disposition_status`（人工处置：pending/processing/resolved/ignored/false_positive）。`PUT /foreign/alerts/{id}/handle` 主参 `disposition_status`；列表支持 `disposition_filter`(all/hide_fp/only_fp，默认 hide_fp)。
- 接口：`GET|POST|PUT|DELETE /alerts/rules`、`GET /foreign/alert-rules`、`POST /foreign/alert-rules/:id/{enable,disable}`、`GET /alerts/records`、`POST /alerts/evaluate`、`PUT /alerts/records/:id/handle`、`GET /foreign/alerts`、`POST /foreign/alerts/evaluate`、`PUT /foreign/alerts/:id/handle`。

---

## 6. AI 研判与人工复核

### 6.1 国内 AI 人工复核（`Opinions.vue` review-view）
- 分段筛选：待复核/已确认/已驳回/全部。
- 批量操作下拉：采用 AI 展示 / 保留规则风险 / 确认事件影响 / 确认预警影响 / 驳回 / 全量确认事件 / 全量驳回。
- 行操作：确认事件影响 / 确认预警影响 / 更多▾（采用AI展示/保留规则/完成复核/驳回全部）。
- 决策前 `ElMessageBox.confirm` 不可撤销提示；完成复核可填说明。
- 接口：`GET /domestic/ai-analysis/reviews`、`POST .../reviews/:id/decision`、`POST .../reviews/batch`。

### 6.2 外网 AI 人工复核（`ForeignAIReviewView`）
- 挂载于外网工作区「国外舆情 → AI 人工复核」。
- 分段筛选（待复核默认）；批量操作▾（确认选中采用AI展示 / 保留规则 / 确认事件影响 / 确认预警影响 / 驳回选中 / 全量确认事件 / 全量驳回）。
- 行复选 + 标题链接详情弹窗 + 事件/预警影响候选计数 + 行操作「确认事件/预警影响」「更多▾」。
- 权限：`foreign:ai:review:read`、`foreign:events:review:read`、`foreign:alerts:review:read`（可见）；`foreign:events:review:confirm`、`foreign:alerts:review:confirm`、`foreign:ai:review:reject`、`foreign:ai:review:complete`、`foreign:ai:full-confirm`（操作）。
- 接口：`GET /foreign/ai-analysis/reviews`、`POST /foreign/ai-analysis/reviews/:id/decision`、`POST /foreign/ai-analysis/reviews/batch`。

### 6.3 报告导出（`ReportExportDrawer` + `ModuleSelector`）
- 由 Dashboard「导出舆情报告」唤起。
- 交互：报告模板下拉（公共模板）、删除模板、报告名称（≤40）、统计时间字段（采集/发布）、时间范围（7/15/30 天或自定义）、模块选择（增删/上下移/移除，`ModuleSelector`）、参数编辑、保存为模板（名称/描述/公开）、生成并下载 PDF（blob）。
- 权限：`reports:read`（可见）/ `reports:export`（导出）/ `reports:manage`（模板管理）。
- 接口：`@/api/report`：getReportModules、generateReport、getTemplates、createTemplate、deleteTemplate。

---

## 7. 传播溯源 `/propagation`（`Propagation.vue`）

- 左侧事件列表（标题实时搜索、点击选择、双击进详情）；右侧指标卡（节点数/最大传播深度/来源平台数/时间跨度/负面占比）+ ECharts 力导向图（拖拽/缩放，节点按深度着色）+ 来源分布（进度条）+ 传播时间线（前 15 条）。
- 「构建传播链」按钮（`events:write`，`POST /propagation/rebuild/:event_id`）。
- 接口：`GET /propagation/events`、`GET /propagation/graph/:event_id`、`POST /propagation/rebuild/:event_id`。

---

## 8. AI 检索 `/ai-search`（`AiSearch.vue` + `WebSearch`/`AiSearchPanel`/`AnspireSearch`）

- 外壳按路由切换三个子视图 Tab：Bocha 网页搜索 / AI 搜索（Bocha）/ Anspire 网页搜索；导航带 loading 防抖。
- `BochaDetailModal` 展示检索线索详情（原文链接/来源/发布时间/检索词/摘要）。
- 权限：`ai:search`（后端 `/bocha/*`、`/anspire/*` 已收敛为 `ai:search`）。

---

## 9. 数据管理 `/data`（`DataManage.vue` 聚合）

聚合页含顶部 Tab：`关键词管理 / 数据源管理 / 采集日志 / AI线索审核（仅超管）` + 国内/外网二级切换（外网需 `canUseForeign`）。初始 tab 来自 `route.query.tab`，按权限回退至首个可见 tab，`keep-alive` 保留状态。

### 9.1 关键词管理
- **国内 `Keywords.vue`**：搜索、类型（监测词/敏感词）、来源（系统内置/自定义）、状态（启用/停用）、分类筛选；批量启用/停用；系统内置敏感词🔒受保护（仅启停，不可编辑/删除）；新增/编辑弹窗（类型/关键词/权重 1-100/分类/启用）；删除确认。权限 `keywords:write`。
- **外网 `ForeignKeywordsView.vue`**：搜索、主题、类型、状态筛选；批量启用/停用；行启用/停用/编辑/删除；弹窗（关键词/分类/类型/权重/风险权重/启用）。写操作后端鉴权（前端无显式门控）。

### 9.2 数据源管理
- **国内 `Sources.vue`**：区域 cascader、启用状态、名称/key 搜索、刷新；超管可见「统一采集频率设置」「+ 新建采集源」及行内启停/优先级/配置/调度/历史；健康/采集质量/调度徽标；弹窗：采集历史、配置（区域/filter_mode/keyword_scope 联动校验/config_json 高级框）、新建（测试连接）、采集调度（周期 5–1440 分）、统一频率设置。仅超管可写。
- **外网 `ForeignSourcesView.vue`**：刷新、搜索、语言/来源筛选；`+ 新增外网源`（`canManageSource`）；编辑器弹窗（名称/Key/语言/RSS 多行/代理/超时重试/间隔/上限/robots 检查 + 连通性测试逐 Feed 结果）；行编辑/测试/历史 + 启用/定时采集 switch；采集历史弹窗。权限 `foreign:data:manage` 或 `foreign:sources:write`/`test`。

### 9.3 采集日志
- **国内 `CollectionLog.vue`**：顶部「当前采集任务」实时卡（触发方式/批次/已运行/统计/处理源/当前步骤）；筛选（触发方式/状态/刷新）；行展开明细；实时轮询 `tasks/{id}`(1.5s) 与 `collection-logs?status=running`(2s)。
- **外网 `ForeignCollectionLogView.vue`**：同结构，scope=foreign 时激活实时卡；字段含触发方式/批次/统计/正在处理源/`live-step`。

### 9.4 AI 线索审核 `BochaLeadReview.vue`（仅超管）
- 挂载于 `/data?tab=bocha-leads`；AppLayout 消息铃铛红点轮询 `GET /admin/bocha/leads`(status=new)。

---

## 10. 系统管理 `/system`（`SystemAdmin.vue` 聚合）

顶部 Tab `用户管理 / 角色权限 / 登录日志 / 操作日志`，按模块权限逐项可见；无权限显示空态。

### 10.1 用户管理 `/system/users`（`Users.vue`）
- 搜索、+ 新增用户；行编辑/启用停用（保护：当前用户、最后一个启用超管不可停）/删除（admin 禁删，确认）。
- 弹窗：新增/编辑（用户名/初始密码/角色：管理员/分析员/观察员；admin 角色固定不可改）、密码管理（自改 ≥6 字符 → `authStore.logout()` 跳登录；或管理员重置）。
- 接口：`GET /users`、`POST /users`、`PUT /users/{id}`、`POST /users/me/password`、`POST /users/{id}/reset-password`、`/activate`、`/deactivate`、`DELETE /users/{id}`。

### 10.2 角色权限 `/system/roles`（`Roles.vue`）
- 搜索、+ 新建角色；行「权限」抽屉 / 启用停用（系统角色不可停）/ 删除（系统角色不可删）。
- 权限抽屉：按业务模块分组（舆情/事件/关键词/用户/角色/预警/报告/AI/数据源/采集器/传播/驾驶舱/审计/外网风险/外网组合权限等）；「展开兼容权限」折叠外网旧细粒度；`foreign:*` 组合权限中文标签；admin 只读全选。
- 接口：`GET /roles`、`GET /permissions`、`PUT /roles/{id}`、`POST /roles`、`DELETE /roles/{id}`。

### 10.3 登录日志 `/system/login-logs`（`LoginLogs.vue`）
- 筛选：用户名、登录结果（成功/失败/退出）、查询/重置；表格（时间/IP/结果/失败原因/UA）。`GET /login-logs`。

### 10.4 操作日志 `/system/operation-logs`（`OperationLogs.vue`）
- 筛选：操作人、操作类型（CREATE/UPDATE/DELETE…）、结果、查询/重置；表格含「详情」`el-popover`（before/after 与权限增减摘要，90 字截断）。`GET /operation-logs`。

---

## 11. 跨模块权限矩阵（前端可见性）

| 模块 | 关键权限码 |
|---|---|
| 舆情 | `opinions:write`（情感校正）、`isSuperuser`（删除/运行记录）、`ai:analyze`/`domestic:ai:analyze`（AI 研判/触发） |
| 事件 | `events:read`、`events:write`（处置/删除/聚合/构建传播链）、`foreign:events:read`、`foreign:events:write`、`foreign:events:confirm` |
| 预警 | `alerts:read`、`alerts:write`、`foreign:alerts:evaluate`、`foreign:alerts:rules:write`、`foreign:alerts:enable`、`foreign:alerts:review:confirm`、`foreign:ai:review:reject` |
| AI 检索/复核 | `ai:search`、`ai:review:read`、`domestic:events:review:read`、`domestic:alerts:review:read`、`foreign:ai:review:read`、`foreign:ai:review:complete`、`foreign:ai:full-confirm` |
| 数据管理 | `keywords:read/write`、`sources:read`、`foreign:keywords:read`、`foreign:sources:read/write/test`、`foreign:data:manage`、`collectors:*`（reserved） |
| 系统管理 | `users`/`roles`/`login_logs`/`audit_logs` 模块权限、`users:activate`、`roles:write/delete` |
| 采集 | 超管（国内）、`foreign:sources:collect`、`foreign:sources:collect_all` |
| 报告 | `reports:read`/`export`/`manage` |
| 传播 | `propagation:read`、`events:write`（构建） |

---

## 12. 交互功能速查总表（按操作类型归纳）

| 功能类别 | 可实现的交互 |
|---|---|
| 采集 | 国内全量采集、外网按源采集、外网全量采集、连通性测试、实时进度卡与轮询 |
| 检索/筛选 | 全局关键词搜索（回车/清空）、相关性/情感/级别/类型/风险等级/日期区间多维度筛选、展示口径（规则/AI）切换 |
| 研判 | 单条触发 AI 分析、批量 AI 研判（范围选择 + 成本预览 + 提交 + 取消/重试）、AI 人工复核（采用AI/保留规则/确认事件/预警影响/驳回/全量） |
| 处置 | 事件状态流转、合并、拆分、备注、处置记录时间线；预警记录处置（状态/备注）；外网双列处置（status + disposition_status） |
| 配置管理 | 关键词增删改启停、数据源增删改/调度/过滤策略、预警规则增删改启用、角色权限配置、用户增删改启停改密 |
| 聚合/分析 | 事件手动聚合（Dry-Run/正式）、传播链构建、关联舆情/预警查看、事件研判统计 |
| 导出/报告 | 舆情 PDF 报告（模板/模块选择/参数/时间范围）、保存/删除模板 |
| 审计 | 登录日志查询、操作日志查询（变更摘要 popover） |
| 态势展示 | 驾驶舱 KPI/趋势/分布/词云、指挥大屏地图下钻/轮播/实时刷新、预警滚动与 Toast 推送 |
| 账号 | 登录、退出、自改密码、超管重置他人密码 |

---

> 文档生成依据：前端路由 `router/index.ts`、各 `views/*.vue` 与 `components/*`、API 客户端 `api/index.ts` 及 `usePermission`/`useCollectionActions` 等 composable 的实读结果。后端接口路径以代码中出现的前缀为准；权限码以前端 `usePermission` 引用为准（真实校验以后端 RBAC 为准）。
