# 廊坊切换前只读审计报告

> 任务：舆情数据范围切换（河北省全量 → 廊坊市）+ 历史业务数据重置
> 阶段：**阶段一（只读审计）** —— 未修改任何代码 / 配置 / 数据库
> 生成时间：2026-07-24
> 结论：审计完成，等待确认后再进入阶段二/三

---

## 零、执行摘要

- 当前生产库已确认为真实生产库（VERIFIED），舆情主表 `opinions` 共 **1088** 条，覆盖河北省全域（省级 651 条 + 14 个地市/县）。
- 系统采集范围**不做严格地理裁剪**，而是「**关键词过滤 + 采集器 scope 绑定 region_id**」双机制协同。切换到廊坊需要同时调整：数据源启停、监测关键词、以及一处硬编码回退区域码。
- 数据清理需按外键子表→主表顺序执行，**无 ON DELETE CASCADE**，必须显式分步删除。
- 存在 **1 处代码级硬编码河北省(130000)** 回退（`service.py:188`），需要评估是否改为廊坊(131000)。

---

## 一、数据链路与业务表

### 1.1 采集数据生命周期

```
采集器 fetch() ──(关键词过滤)──▶ CollectorService
   │  绑定 region_id (按采集器 scope_region_codes, 空则回退 130000)
   ▼
opinions (舆情主表, +AI分析/风险评分字段)
   │
   ├─▶ event_opinions ◀── events        (事件聚合: 定时/触发时按 region+时间窗聚类)
   │
   ├─▶ alert_records                     (预警评估: 命中规则生成告警记录)
   │
   └─▶ propagation_nodes                 (传播溯源: 挂在 event/opinion 下)

每次采集另写:  collector_runs (采集运行审计日志, 每源一行, 同批共享 batch_id)
```

### 1.2 表清单（表名 / 作用 / 是否清理 / 清理顺序 / 当前行数）

**A. 业务数据表（采集及衍生数据，需清理）**

| 顺序 | 表名 | 作用 | 外键依赖 | 是否清理 | 当前行数 |
|---|---|---|---|---|---|
| 1 | `propagation_nodes` | 传播溯源节点 | → events / opinions / self(parent_id) | ✅ 是 | 662 |
| 2 | `alert_records` | 预警记录 | → opinions(可空) / events(可空) / users(handled_by) | ✅ 是 | 80 |
| 3 | `event_opinions` | 事件↔舆情关联 | → events / opinions（唯一约束） | ✅ 是 | 441 |
| 4 | `events` | 聚合事件 | 无（被上表引用） | ✅ 是 | 212 |
| 5 | `opinions` | 舆情主表（含 AI 分析/风险字段） | → regions | ✅ 是 | 1088 |
| 6 | `collector_runs` | 采集运行审计日志 | 无 FK | ⚠️ **待确认** | 4506 |

> **关于 `collector_runs`（决策点①）**：它是「采集运行日志」，不是舆情业务数据本身，与 opinions 等无外键关联。清空 opinions 后这些日志会指向已不存在的历史采集。建议**一并清空以获得干净起点**，但因其属"运行审计"性质，需您确认是否保留。

**B. 配置 / 系统表（严禁清理）**

| 表名 | 作用 | 行数 | 说明 |
|---|---|---|---|
| `users` | 用户 | 3 | RBAC，禁删（alert_records.handled_by 引用） |
| `roles` / `permissions` / `role_permissions` / `user_roles` | RBAC | 3/26/19/0 | 禁删 |
| `alert_rules` | **预警规则（配置）** | 2 | 禁删（仅删 alert_**records**，规则保留） |
| `data_sources` | **采集器配置** | 30 | 禁删（阶段三只做启停/scope 调整） |
| `keywords` | 关键词库（监测+敏感） | 46 | 禁删（阶段三调整监测词内容，不清表） |
| `regions` | 区域字典 | 17 | 禁删（切换目标区域来源） |
| `user_login_logs` / `user_operation_logs` | 审计日志 | — | 禁删 |

> **`risk_analysis` / `analysis_records` 表不存在**：AI 分析结果是 `opinions` 表的列（`ai_summary`/`risk_factors`/`risk_category` 等），随 opinions 删除自动清除，无需单独处理。

### 1.3 清理顺序（依据实际外键，子表→主表）

```
propagation_nodes  →  alert_records  →  event_opinions  →  events  →  opinions
（collector_runs 独立，如确认清理可单独 TRUNCATE/DELETE）
```
所有外键均**无级联删除**，必须严格按上述顺序，否则触发 FK 约束报错。

---

## 二、当前生产库确认（数据库身份门禁）

已执行 `scripts/db_identity_check.py`，结果 **VERIFIED（退出码 0）**：

| 项 | 值 |
|---|---|
| DATABASE_URL | `postgresql+psycopg://opinion_user:opinion_pass@127.0.0.1:5432/opinion_db` |
| Host / Port / DB | 127.0.0.1 / 5432 / **opinion_db** |
| system_identifier | 7663057120701798896（与期望一致） |
| data_directory（期望） | `C:\Users\Administrator\Desktop\舆情监测系统\pgdata` |
| Alembic head | `p12_rbac_roleperms` |
| opinions 业务指纹 | 1088（≥100，判定真实生产库） |

> 结论：连接的是**真实生产库**，非早期空克隆。阶段二/三所有写操作前将再次跑该门禁（退出码 0 才执行）。

---

## 三、采集范围配置（河北 → 廊坊 需调整的位置）

### 3.1 区域绑定 / 过滤机制总览

采集器分两类，区域生效方式不同：

1. **地方源（有 scope_region_codes）**：`CollectorService._resolve_region_id` 取 scope 中最具体 code 绑定 region_id；抓取 URL 本身即该地区站点。
2. **全国源（scope 为空：百度新闻/新华网/人民网/中新网）**：靠 **monitoring 关键词过滤**决定采什么，region_id **回退绑定到 130000 河北省**。

### 3.2 当前 `data_sources`（30 行）区域分布

**已启用（enabled=T）：**
- 廊坊域源（保留）：`government`(131028 大厂)、`langfang_news`(131000)、`xianghe_news`(131024 香河)
- 全国源（靠关键词）：`baidu_news`、`xinhua`、`people`、`chinanews`
- **省级源（河北全省，建议停用）**：`hebei_news` / `hebei_daily` / `changcheng` / `hebei_gov`（scope=130000）
- **其它地市源（非廊坊，建议停用）**：`chengde_gov`(承德)、`shijiazhuang_gov`(石家庄)、`handan_gov`(邯郸)、`baoding_gov`(保定)、`zhangjiakou_gov`(张家口)、`hengshui_gov`(衡水)、`xiongan_gov`(雄安)、`tangshan_huanbohai`(唐山)、`qinhuangdao_news`(秦皇岛)、`xingtai_daily`(邢台)、`cangzhou_news`(沧州)

**已停用（enabled=F）：** `langfang_gov`(131000)、`sanhe_gov`(131082 三河)、`xianghe_gov`(131024)、`guan_gov`(131022 固安) 等——**若廊坊范围含下辖县，可按需启用这些廊坊源**；其余（tangshan_gov/qinhuangdao_gov 等）保持停用。

### 3.3 monitoring 关键词（29 个，决定全国源采集内容）

```
地域锚点: 河北, 石家庄, 唐山, 保定, 邯郸, 秦皇岛, 邢台, 沧州, 衡水,
          张家口, 承德, 廊坊, 雄安, 大厂, 大厂回族自治县
主题词:   消防, 安全生产, 安全事故, 民生, 投诉, 环保, 征地, 拆迁,
          食品安全, 教育, 医疗, 交通, 城管, 舆情
```
> 切换后应**停用/删除非廊坊地域锚点**（河北、石家庄…雄安），保留：廊坊、大厂、大厂回族自治县（+ 若含下辖县则加 三河/香河/固安）与全部主题词。

### 3.4 需修改的文件 / 配置清单

| 层 | 位置 | 当前 | 阶段三建议动作 |
|---|---|---|---|
| 数据源 | `data_sources` 表（DB 数据） | 省级+多地市源启用 | 停用省级/非廊坊源，启用廊坊下辖源 |
| 关键词 | `keywords` 表（DB 数据） | 含河北全域锚点 | 停用非廊坊地域锚点 |
| 兜底关键词 | `.env` `COLLECTOR_KEYWORDS` | `河北,大厂,舆情,消防,安全生产,民生,投诉,廊坊,大厂回族` | 去「河北」，聚焦廊坊域 |
| 兜底关键词 | `backend/app/core/config.py:66` | 同上默认值 | 同步（低优先，表非空时不生效） |
| **硬编码回退** | `backend/app/collectors/service.py:188` | 全国源回退 `130000`（河北省） | 见「四」——决策改 131000 |

---

## 四、硬编码河北范围（文件 / 行号 / 逻辑 / 建议）

**★ 关键项（影响新数据区域归属）**

- **文件**：`backend/app/collectors/service.py`
- **行号**：179–188（`_resolve_region_id`）
- **当前逻辑**：
  ```python
  # scope 为空/None（国家级源，靠关键词过滤河北）→ 绑定河北省(130000)
  if region is None:
      region = db.query(Region).filter(Region.code == "130000").first()
  ```
  全国源（百度/新华/人民/中新网）无 scope，抓到的舆情一律绑定 **130000 河北省**。
- **修改建议**：改回退为 **131000（廊坊市）**，使切换后全国关键词源的新舆情归属廊坊，而非笼统河北省。（需改代码 + 重启 uvicorn；若决定停用全国源，则此项可不改。）

**其它涉河北位置（多为省级源，停用其 data_sources 行即可，不必改类）**

| 文件 | 行 | 逻辑 | 建议 |
|---|---|---|---|
| `.env` | `COLLECTOR_KEYWORDS` | 含「河北」省级词 | 去「河北」，改廊坊域 |
| `app/core/config.py` | 65-66 | 兜底关键词默认含河北 | 同步（可选，低优先） |
| `app/collectors/hebei_gov_collector.py` | 32-33 | 抓 hebei.gov.cn 全省要闻 | 停用 `hebei_gov` 数据源行 |
| `app/collectors/hebei_news_collector.py` | 34-37 | 抓 hebnews.cn 省级频道 | 停用 `hebei_news` 数据源行 |
| `app/collectors/hebei_daily_collector.py` | — | 河北日报（全省） | 停用 `hebei_daily` 数据源行 |
| `app/collectors/changcheng_collector.py` | 3 | 长城网（河北全省） | 停用 `changcheng` 数据源行 |
| `app/collectors/registry.py` | 42-97 `DEFAULT_SOURCES` | 表空回退种子含省级源 | 仅表空时生效，可选同步 |
| `scripts/init_db.py` | 76-131 `EXISTING_SOURCES` | 种子含省级源 | 幂等种子，可选同步 |
| `app/services/dashboard_service.py` | 61-63 | 大屏地图按省级上卷（廊坊→河北省显示） | 展示层，非采集范围；如需大屏聚焦廊坊另议（可选） |

**顺带发现（数据一致性，非本任务范围）**：`regions` 中 `大厂回族自治县(131028)` 的 `parent_code=None`（应为 131000 廊坊市），是种子历史遗留。仅记录，不在本次处理。

---

## 五、待确认决策点（进入阶段二/三前）

1. **`collector_runs`（4506 行采集日志）**是否一并清空？（建议：清空，获得干净起点）
2. **廊坊范围定义**：仅廊坊市本级(131000)，还是廊坊市全域（含下辖 大厂131028/三河131082/香河131024/固安131022）？（建议：**全域**，符合"廊坊市"行政范围）
3. **全国源（百度/新华/人民/中新网）**是否保留？
   - 保留 → 靠关键词过滤廊坊，需改 `service.py:188` 回退为 131000；
   - 停用 → 只留廊坊本地源，`service.py` 可不改。
4. 是否允许**改动 1 处代码**（`service.py` 回退区域码）并重启 uvicorn？（其余均为 DB 数据/配置调整）

---

## 六、阶段二/三预案（确认后执行，此处仅预告不执行）

- **阶段二**：先跑身份门禁 → 快照各表数量 → 按 `propagation_nodes→alert_records→event_opinions→events→opinions`（+可选 collector_runs）顺序 DELETE → 校验目标表归零 → 出《历史舆情数据清理报告》。
- **阶段三**：停用省级/非廊坊数据源、精简监测关键词、调 `.env`（+可选 `service.py`）→ 重启 → 跑一次测试采集 → 校验新数据 `region` 落在廊坊域、不再采河北其它城市 → 出《廊坊采集范围切换报告》。

**⚠️ 阶段一到此结束，未做任何修改，等待您确认以上决策点后再继续。**
