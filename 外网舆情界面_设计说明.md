# 外网舆情界面 · 布局改版设计说明

> UI Designer（像素君）· 2026-08-10
> 范围：仅调整布局与排版，**所有卡片/表格字段内容保持不变**。

## 一、设计目标
- **美观**：延续系统既有的 Apple-light 品牌语言（`#0071e3` 主色、`#1d1d1f` 墨色、`#f5f5f7` 浅灰底、12–20px 圆角、柔和阴影），统一为一套可复用的设计令牌。
- **易用**：解决三个真实痛点——① 14 列表格横向滚动时标题/表头丢失；② 看板告警滚动条占满整行却只在顶部；③ 图表被挤进三等分窄列。

## 二、设计令牌（Design Tokens）
| 类别 | 取值 |
|---|---|
| 背景 | 页面 `#f5f5f7` / 卡片 `#ffffff` / 次表面 `#fbfbfd` |
| 文本 | 主 `#1d1d1f` / 次 `#6e6e73` / 辅助 `#86868b` |
| 边框 | `#e8e8ed` / 强 `#d2d2d7` |
| 主色 | `#0071e3`（仅强调，克制使用） |
| 语义 | 成功 `#34c759` / 警示 `#ff9f0a` / 危险 `#ff3b30` |
| 圆角 | 12 / 16 / 20px |
| 间距 | 基准 4px，栅格 16px |
| 阴影 | 柔光 `0 1px 2px / 0 8px 24px` |

令牌定义在 `.foreign-page` 作用域内，未改动全局 `theme.css`，零污染。

## 三、看板（Dashboard）布局重构
**旧**：KPI（5）→ 三等分栅格（告警滚动整行 + 3 个分布卡 + 3 个整行图表）。
**新**：KPI（5）→ **主区 + 常驻侧栏** 两栏栅格（`1.9fr / minmax(320px,1fr)`）。

- **主区（左，约 2/3 宽）**：数据源分布、每日趋势、外网热词三张图表卡，宽度充足、阅读顺畅。
- **侧栏（右，约 1/3 宽，sticky 常驻）**：外网告警滚动播报 + 风险分布 / 事件状态 / 采集状态。滚动主区图表时，告警与关键分布始终可见——这是驾驶舱式"处置优先"信息架构。
- 告警滚动逻辑（`ResizeObserver` 自动重测高度）无需改动，迁入侧栏后自动适配。

## 四、表格可读性升级（内容零改动）
对所有 `.table-wrap` 表格统一增强：
1. **冻结首列**（标题/名称）：横向滚动时第一列 `position: sticky; left:0` 始终可见，配右侧分隔线。
2. **表头吸附**：`thead th` 吸附于顶部，长表纵向滚动不丢列名。
3. **斑马纹 + 悬停高亮**：偶数行 `#fbfbfd`，悬停 `#f2f2f5`，首列同步着色避免透字。
4. 原 14 列"国外舆情"表（min-width 1720px）因此从"必须来回横滑找标题"变为"标题常驻、一眼定位"。

## 五、排版与动效细节
- KPI 卡、内容卡增加轻微 hover 抬升/阴影过渡，给出可点击反馈（图表卡仅阴影、不位移，避免图表抖动）。
- 栅格响应式：≤1100px 主区与侧栏堆叠（侧栏取消 sticky）；≤820px KPI 转 2 列；≤700px 顶栏纵向排列、输入框满宽。
- 保留 `prefers-reduced-motion`：告警滚动在用户偏好减少动效时自动停止。
- 全站保持系统字体栈、14px 基准、4.5:1+ 对比度（WCAG AA）。

## 六、改动清单（可追溯）
- `frontend/src/views/ForeignWorkspace.vue`
  - 模板：看板 widgets 重组为 `.fw-dash-body > .fw-main + .fw-side`（告警卡与 3 分布卡迁入侧栏，3 图表卡留在主区），**字段文案未变**。
  - 样式：新增令牌块；新增 `.fw-dash-body/.fw-main/.fw-side`；表格冻结首列/吸附表头/斑马纹；圆角、间距、阴影统一为令牌；图表高度微调（280/320px）。
- 已 `vite build` 通过并部署至 `backend/app/static`（46 文件），生产静态已含新布局。

## 七、可继续打磨（可选，非本次范围）
- 顶栏"已批准数据源"可收为状态药丸；Tabs 可改为分段控件（Segmented）以更贴近 Apple 习惯。
- 超宽表可加列宽拖拽 / 列显隐，进一步降低信息密度。
