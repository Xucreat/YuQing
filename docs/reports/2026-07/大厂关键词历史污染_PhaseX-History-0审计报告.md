# 大厂关键词历史污染治理 · Phase X-History-0 只读审计报告

> 阶段定位：Phase X-2 已上线新增数据入口过滤；本阶段治理**历史存量污染**。
> 审计原则（严格执行，本阶段零变更）：**不修改代码 / 不修改数据库 / 不删除数据**。
> 审计范围：Phase X-0 识别的 122 条含「大厂」历史 Opinion（49 真实 / 73 噪声）。
> 数据来源：`_audit_dachang_backtest.json`（决策 oracle）+ 生产库只读查询（DB 门禁 VERIFIED，未执行任何写操作）。
> 生成时间：2026-07-31 13:26（后端重启验收后）。

---

## 0. 结论速览

| 维度 | 73 条噪声（应治理） | 49 条真实（保持不变） |
|---|---|---|
| 当前 region_id | 大厂(1)×22、廊坊市(12)×51 | 大厂(1)×20、廊坊市(12)×28、香河(16)×1 |
| Event 关联 | 30 条→**12 个幻影事件**(全噪声)；43 条独立无事件 | 17 条→**9 个真实事件**(纯净无噪声) |
| Alert 关联 | **0** | **0** |
| propagation_nodes | 30 个（全部落在 12 幻影事件内） | 无异常 |
| Dashboard 影响 | 大厂区被高估 50%、廊坊市高估 ~6.9% | 无 |

**核心发现**：级联影响被**完美隔离**——30 条噪声意见全部集中在 12 个「100% 噪声、零外部成员」的幻影事件中，真实事件/真实意见/预警（alert）**完全未被污染**。治理可闭环，风险可控。

---

## A. 73 条噪声数据逐项核查

### A.1 region_id 归属（错标分布）
| region_id（surrogate FK→regions.id） | 条数 | 说明 |
|---|---|---|
| 1 = 大厂回族自治县 | 22 | 被错误贴上「大厂县」地域标签，直接污染分县统计 |
| 12 = 廊坊市 | 51 | 被回退归属到廊坊市级，污染市级统计 |
| **合计** | **73** | 全部存在地域错标，需解除归属 |

### A.2 Event 关联（关键级联点）
- **30 条** 落入 **12 个幻影事件**（event# 397/400/405/409/449/466/468/547/550/551/562/563）。
  - 每个幻影事件成员**100% 为本次噪声意见**，且**零条 122 集合外成员**（即事件完全由互联网/科技大厂噪声聚成，无任何真实地域舆情）。
  - 示例：event#397「赛马结束,大厂各寻Agent主心骨」、event#400「腾讯留矩阵,阿里并入口:大厂开始收拢AI Agent」、event#551「数码大厂抢滩"电子吧唧"」——均为典型的「互联网大厂」语义，与河北省廊坊市大厂回族自治县无关。
- **43 条** 为独立噪声，**未关联任何事件**，仅存在 region_id 错标。

### A.3 Alert 关联
- 73 条噪声意见关联 `alert_records`：**0 条**。
- 12 个幻影事件关联 `alert_records`（按 event_id）：**0 条**。
- ✅ 治理噪声**不会触发任何预警级联变更**。

### A.4 propagation_nodes 关联
- 30 条「事件内噪声意见」在 `propagation_nodes` 中各有 1 个节点，**30 个节点全部通过 event_id 指向上述 12 个幻影事件**，distinct opinion 亦为 30。
- ✅ 随幻影事件废弃可一并闭环，不会残留孤儿节点指向真实数据。

### A.5 进入 Dashboard 统计的影响量化
| 区域 | 当前总舆情 | 本次噪声贡献 | 去噪后 | 高估比例 |
|---|---|---|---|---|
| 大厂回族自治县 (id=1) | 44 | 22 | 22 | **50.0%** |
| 廊坊市 (id=12) | 740 | 51 | 689 | 6.9% |

> 注：大厂区 44 条中 22 条即来自本批噪声，去噪后该区分县舆情量将减半；廊坊市因基数大，绝对量影响较小但统计口径失真。

---

## B. 49 条真实地域数据（确认保持不变）

- **region_id**：大厂(1)×20、廊坊市(12)×28、香河(16)×1。
- **Event 关联**：17 条分布在 **9 个真实事件**（event#376/377/408/422/505/507/508/509/560），经核验这 9 个事件**内部零噪声**（122 集合内 noise=0），为纯净的大厂回族自治县真实舆情（如「闻汛而动守安澜——大厂全力筑牢潮白河防汛安全屏障」「协同创新'链'上发力——大厂以科技联动驱动产业焕新升级」）。
- **Alert 关联**：0 条。
- **边缘标注**：id=1509「香河：跨域联动优服务 精准送岗惠民生」因正文含「大厂」被关键词命中且判为真实，但 region_id=16(香河)。属真实跨域协作稿，**按 B 要求保持不变**，仅在此提示为轻微跨区归属，不影响治理结论。

✅ 治理动作对 49 条真实数据**零改动、零级联**。

---

## C. 治理方案（草案，待确认后于后续 Phase 实施）

### C.1 哪些记录需要解除地域归属
- **73 条噪声全部需要解除地域归属**：22 条错标大厂区 + 51 条错标廊坊市。
- 解除方式二选一（见 C.3）：置 `region_id=NULL`，或加标记字段保留原值以供审计追溯。

### C.2 哪些记录需要保留
- **49 条真实全部保留**，region_id / Event / Alert 均不动。
- 12 个幻影事件**不属于保留范围**，应废弃（见 C.4）。

### C.3 是否需要新增标记字段（建议：**是**）
当前 `opinions` 表已有 `resolution_flag`(bool)、`admission_reason`(jsonb)，但**无专门「语义噪声/地域过滤」标记**，且 `region_id` 直接驱动 Dashboard 统计。两种方案：

| 方案 | 做法 | 优点 | 风险 |
|---|---|---|---|
| 方案① 置空 region_id | `region_id=NULL` | 简单，Dashboard 自动不再计入任何区县 | 丢失原始错标痕迹，难回溯「曾错归大厂」 |
| **方案② 新增 `geo_filtered` 标记** ✅推荐 | 新增列 `geo_filtered BOOLEAN DEFAULT FALSE`，**保留原 region_id**，Dashboard 统计时 `WHERE geo_filtered IS NOT TRUE` | 满足「不删除数据」、可回滚、可审计；与 Phase X-2 过滤语义一致 | 需同步改 Dashboard 统计口径（只读查询 +1 条件） |

**推荐方案②**：新增 `opinions.geo_filtered`（可空布尔，默认 false），并保留原始 `region_id` 原值。这样既解除了 Dashboard 统计失真，又完整保留了历史归属证据，符合「不删除数据」红线，且未来可与 Phase X-2 运行期过滤共用一套语义。

### C.4 是否存在级联影响（**存在，但已隔离、可闭环**）
| 对象 | 影响 | 处理方式 |
|---|---|---|
| 12 个幻影事件 (events) | 全部由噪声构成 | 软废弃：`status` 置 `deprecated` / 或 `DELETE`（本阶段不执行） |
| 30 行 event_opinions | 全部指向幻影事件 | 随事件废弃一并移除 |
| 30 个 propagation_nodes | event_id 全指向幻影事件 | 随事件废弃一并移除 |
| 9 个真实事件 / 17 真实意见 | **无** | 不动 |
| alert_records | **0 关联** | 无影响 |
| 43 条独立噪声 | 仅 region_id 错标 | 置 `geo_filtered=true`（方案②） |

✅ **级联边界清晰**：所有噪声的下游（event / propagation / event_opinions）均收敛在 12 个自洽的幻影事件内，与真实数据零交叉。治理后可彻底消除大厂区/廊坊市区统计失真，且不触及任何真实舆情与预警。

### C.5 实施前置待确认（不在此阶段执行）
1. **标记字段载体**：新增 `opinions.geo_filtered`（推荐）vs 复用 `resolution_flag`？
2. **幻影事件处置力度**：软废弃(status=deprecated) vs 硬删除 event+event_opinions+propagation_nodes？（均需「不删除数据」红线约束，建议软废弃 + 标记）
3. **Dashboard 统计口径**：是否同步增加 `geo_filtered` 排除条件（需改只读查询，属代码改动，留待实施 Phase）。
4. **是否接受「大厂区去噪后由 44→22、廊坊市 740→689」的统计结果**作为验收基线。

---

## 附：审计操作留痕（零写操作证明）
- 全部查询均为 `SELECT`；未调用任何 `UPDATE/DELETE/INSERT`/`alembic`/文件写入（除本报告文件）。
- 运行实例仍为 Phase X-2 重启后的 PID 9940，未改动。
- oracle 文件 `_audit_dachang_backtest.json` 仅读取，未修改。
