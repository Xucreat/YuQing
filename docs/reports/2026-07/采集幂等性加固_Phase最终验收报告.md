# 采集幂等性加固 Phase · 最终验收报告

- **验收时间**：2026-07-24 10:42 ~ 10:50
- **验收方式**：只读检查（全程 SELECT / 日志查看 / 进程查看，零修改）
- **目标库**：opinion_db @ 127.0.0.1:5432（生产）
- **验收结论**：✅ **通过**（附观察期说明与风险清单）

---

## 一、24小时采集数据质量

| 检查项 | 结果 | 判定 |
|---|---|---|
| opinions.url 精确重复（全库） | **0 组** | ✅ |
| opinions.url 精确重复（近24h新增 297 条内） | **0 组** | ✅ |
| event_opinions (event_id, opinion_id) 重复 | **0 行** | ✅ |
| opinions 总数 | 966（清理前 1000 − 34，账目吻合） | ✅ |
| 近24h非 success 采集 run | 1 条（7-23 11:00 邯郸市政府网，"采集进程重启异常中断已超时回收"，属加固前旧事件、已被超时回收机制自愈） | ✅ 可接受 |

**采集批次时间线（关键证据）**：

| 时段 | 每个整半点批次数 | 说明 |
|---|---|---|
| 7-23 15:00 ~ 7-24 10:00 | **2 批**（间隔 <1ms） | 旧双实例双调度状态 |
| **7-24 10:30（advisory lock 生效后首个触发点）** | **1 批**（22 源全 success，fetched 199 / created 5 / failed 0） | ✅ 双触发消除 |

注：双触发期间产生的重复已在 10:31 清理事务中全部移除；此后即使残留双调度，url 唯一索引也会物理拦截重复。

## 二、scheduler 运行状态

| 检查项 | 结果 | 判定 |
|---|---|---|
| pg_locks advisory lock 持有者 | **恰好 1 个**（pid 69104，granted=t，10:14:27 建立，归属 :8000 实例） | ✅ |
| :8000 实例日志（uv_8000.err.log） | 正常启动，无重复调度记录 | ✅ |
| :8011 实例日志（uv_8011.err.log） | **明确打印「本进程未获得 scheduler 单例锁（其他实例已在运行调度器），跳过启动采集/预警调度器」** | ✅ 决定性证据 |
| 是否存在多个 scheduler 日志 | 否——仅 :8000 运行调度器，:8011 仅提供 API | ✅ |

三重证据闭环：**锁表 1 持有者 + :8011 弃权日志 + 10:30 单批次**。

## 三、数据一致性（7 项全 0）

| 检查项 | 结果 |
|---|---|
| propagation_nodes 悬空 parent_id | **0** ✅ |
| propagation_nodes 孤儿 opinion 引用 | 0 ✅ |
| event_opinions 孤儿 opinion 引用 | 0 ✅ |
| event_opinions 孤儿 event 引用 | 0 ✅ |
| alert_records 孤儿 opinion 引用 | 0 ✅ |
| 无任何舆情关联的空事件 | 0 ✅ |
| 传播节点跨事件父引用 | 0 ✅ |

结构复核：`ix_opinions_url_unique`、`uq_event_opinions_event_opinion` 在位；alembic version = `p7evtuniq01`（head）。

---

## 四、系统可靠性评级

### 评级：**A-（生产可靠，附观察期）**

| 维度 | 评分 | 依据 |
|---|---|---|
| 数据唯一性保障 | A | 数据库层唯一索引/约束物理兜底，不依赖应用层自觉 |
| 调度单例保障 | A | PG advisory lock，随连接断开自动释放，无死锁残留风险 |
| 数据一致性 | A | 7 项完整性检查全 0 |
| 故障自愈 | B+ | 采集中断有超时回收机制；实例崩溃后锁自动释放、另一实例重启可接管 |
| 验证充分性 | B | **加固后观察窗口仅约 30 分钟（1 个调度点）**，未满完整 24h 加固后运行期 |

> 评级说明：不给满级 A 的唯一原因是加固代码 10:14 才上线，"连续 24 小时"窗口中约 23.5 小时为加固前数据（其价值在于反证：旧状态确实每点双批，新状态单批）。待加固后连续运行 24h（约 48 个调度点均为单批、url 零重复）即可升级为 A。

## 五、后续风险清单

| # | 风险 | 等级 | 建议 |
|---|---|---|---|
| 1 | **:8011 冗余实例仍在运行**（PID 9868）。调度已单例化，但双实例多耗资源；若 :8000 意外退出，:8011 会在下次重启时才可能接管调度（advisory lock 需重新走 start_scheduler） | 中 | 择机 taskkill 关停 :8011，保持单实例部署 |
| 2 | 采集层遇 url 冲突时将抛 IntegrityError（此前无约束不会触发）。`common.py` 先查后插使概率极低，但并发窗口仍存在 | 低 | 观察采集日志；如出现冲突报错可在采集层捕获 IntegrityError 按"已存在"跳过 |
| 3 | url 唯一为**精确匹配**，带跟踪参数/大小写变体的同文重复无法拦截（本项目源以政府网站为主，实测 0 例） | 低 | 如未来接入社媒源，再评估 URL 归一化（此前已明确暂缓） |
| 4 | advisory lock 绑定 DB 会话：若 :8000 与 PG 之间连接闪断，锁释放但 :8000 的 APScheduler 仍在跑，理论上 :8011 重启后可能出现双调度 | 低 | 单实例部署（风险1落实）后此项自然消除 |
| 5 | 备份 `backup/opinion_db_pre_cleanup_20260724_103142.dump` 为唯一清理前快照 | 低 | 保留 ≥7 天；建议纳入定期备份机制 |
| 6 | 7-23 11:00 有 1 条 failed run（进程重启中断），属历史事件 | 已闭环 | 无需处理 |

## 六、验收签署建议

- ✅ 数据质量、调度单例、数据一致性三大项全部达标，**建议验收通过**。
- 📋 附带条件：完成风险 1（关停 :8011）；加固后满 24h 复查一次（只需重跑本报告第一、二节 SQL，预期全 0 且每点 1 批）。
