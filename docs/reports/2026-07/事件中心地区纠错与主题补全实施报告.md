# 事件中心地区纠错与主题补全实施报告

执行日期：2026-07-29

## 一、审计结果

### 1. scope_region_codes 如何转换为 Opinion.region_id

审计对象：`backend/app/collectors/service.py`、`backend/app/collectors/registry.py`、`backend/app/models/data_source.py`、`backend/app/models/region.py`。

原链路是采集器级别绑定地区：`data_sources.scope_region_codes` 由 registry 注入到 collector；CollectorService 通过 `_resolve_region_id()` 按 code 查询 `regions.code`，取最具体 code 后写入 `Opinion.region_id`。该方式把“来源覆盖范围”当成“内容事实地区”，导致全国源、区域源和单条内容混在一起判断。

本次已调整为 item 级判断：CollectorService 对每条标准化 item 先调用 `OpinionRegionService.decide()`，再调用 `OpinionAdmissionService.evaluate()`，最终用 `region_decision.region_id` 写入 `Opinion.region_id`。

### 2. 全国来源为什么回退到 131000

审计对象：`data_sources.scope_region_codes`、历史库数据、CollectorService 地区绑定逻辑。

新华网、人民网、中国新闻网、百度新闻等全国源的 `scope_region_codes` 为空、NULL 或 ALL。历史逻辑/配置在廊坊切换后把空范围来源兜底到廊坊市 `131000`，导致全国新闻只要被采集入库，就被统一绑定为廊坊市。

本次已移除“全国来源为空则回退 131000”的准入路径：全国源必须在 title/content 中明确命中廊坊市或下辖区县词，否则拒绝创建本地 Opinion，并计入 `CollectorRun.admission_filtered`。

### 3. Event.region_id 如何继承 Opinion.region_id

审计对象：`backend/app/services/event/aggregator.py`、`backend/app/models/event.py`。

Event 创建时取最高风险代表 Opinion 的 `region_id` 写入 `Event.region_id`；Event 更新重算时仍按事件代表 Opinion 重置 `event.region_id`。因此只要 Opinion 地区被污染，Event 地区会继续继承污染。

本次未改变聚类算法，仅确保进入聚合的 Opinion 已经有事实地区判断；Event 创建和更新自然继承纠正后的 Opinion 地区。

### 4. Event.topic_category 当前如何计算

审计对象：`backend/app/models/event.py`、`backend/app/services/event/aggregator.py`。

数据库字段已有枚举约束，但历史事件大多为 NULL/other。审计时工作区已有局部主题服务草稿，本次收口为正式纯规则 `EventTopicService`：Event 创建和更新均根据关联 Opinion 重算 `topic_category`。

### 5. 当前数据库 Event 主题 NULL/other 分布

首次 dry-run：

- Event 总数：141
- NULL：138
- other：2
- gov_service：1

执行回填时生产库新增了 1 条事件，因此 apply 时基数为 142。

## 二、修改文件

- `backend/app/services/opinion_region_service.py`
- `backend/app/services/opinion_admission_service.py`
- `backend/app/collectors/service.py`
- `backend/app/services/event/topic_service.py`
- `backend/app/services/event/aggregator.py`
- `backend/scripts/event_region_topic_maintenance.py`
- `backend/tests/test_event_region_topic_p0.py`
- `backend/tests/test_weibo_octopus_collector.py`

前端 `Events.vue` / `EventDetail.vue` 已审计：继续使用现有 `region_name`、`region_id`、`topic_category` 字段，无 API contract 变化。

## 三、地区判断规则

- `scope_region_codes` 只表示来源覆盖范围，不再直接代表内容事实地区。
- 本地政府/本地新闻源：无明确地区词时允许使用可信 scope 默认地区。
- 全国源：scope 为空、NULL、ALL 时视为全国源，必须明确命中廊坊或下辖区县词，否则拒绝创建本地 Opinion。
- 明确命中单一区县：绑定该区县。
- 同时命中多个同级区县：主地区回退廊坊市，所有命中写入 `admission_reason.region_decision.region_hits`。
- 命中“没有廊坊依据”“与廊坊无关”等小范围否定语境时，不作为地区命中。
- 微博评论仍跳过，不创建 Opinion。
- 微博正文继续走 `OpinionAdmissionService` 规则准入。

## 四、主题分类规则

纯规则、无 AI、无外部 NLP、无新字段。

信号优先级：

1. `Opinion.risk_category` 显式映射。
2. Opinion 标题和正文主题词计分。
3. `content_type` 作弱辅助。
4. 无法确定返回 `other`。

支持枚举：

`livelihood`、`traffic`、`education`、`healthcare`、`environment`、`safety`、`market`、`gov_service`、`social_security`、`public_emergency`、`other`。

重要校正：

- `news` 只作为内容形态，不直接映射为事件主题。
- `complaint` 不固定为民生，而是结合文本命中交通、教育、医疗等具体主题。
- 治安、诈骗、打架等归入 `safety`，不误归入 `social_security`。
- 多 Opinion 事件按主题分累计；平分时用最高风险代表 Opinion 打破平局。

## 五、数据流变化

旧流程：

Collector fetch -> 采集器 scope 统一算 region_id -> Opinion -> Event 继承 Opinion.region_id -> topic 多为 NULL/other。

新流程：

Collector fetch -> 每条 item 地区判断 -> 每条 item 准入判断 -> Opinion 写入事实 region_id 和可解释 admission_reason -> Event 聚合时继承纠正后地区 -> Event 创建/更新重算 topic_category。

未改动：

- Collector 内部解析逻辑。
- RiskEngine。
- AlertService。
- EventAggregator 的聚类、时间窗口、相似度、merge_condition、cluster_opinions。
- 微博 post/comment 分流和评论禁止创建 Opinion 的规则。
- API 字段契约。
- 数据库字段和 migration。

## 六、测试与 build 结果

为避免污染生产库，创建了独立测试库 `opinion_p0_test` 并用 `scripts/init_db.py` 初始化。

通过：

- `python -m pytest tests/test_event_region_topic_p0.py tests/test_opinion_admission_service.py tests/test_weibo_octopus_collector.py tests/test_phase2c_event_center.py -q`
- 结果：43 passed，4 warnings。
- `frontend/npm run build`
- 结果：通过。

额外尝试过老 `test_events.py` / `test_events_aggregator_v2.py`，失败原因是这些测试仍断言旧契约：Event 无 `status` 字段、`POST /events/aggregate` 同步返回 created/updated/linked、旧关键词交集聚合行为等，和当前系统已存在实现不一致；未作为本 P0 验收依据。

## 七、历史地区 dry-run 结果

命令：

`python scripts/event_region_topic_maintenance.py region-dry-run`

结果：

- changed：false
- 疑似污染 Opinion：353
- 关联 Event：61
- 样例来源：新华网、人民网、中国新闻网等
- 样例标题包括：
  - 新华社权威速览丨未来五年，法治宣传教育如何开展
  - 深圳自然博物馆正式开馆
  - 新华社权威快报丨未来五年，邮政业将这样发展

该工具只读扫描，不删除、不更新 Opinion/Event/Alert。

## 八、历史主题回填前后分布

首次 dry-run：

- before：NULL 138，other 2，gov_service 1
- after：safety 34，other 36，market 10，livelihood 12，gov_service 23，healthcare 8，traffic 4，education 9，public_emergency 3，social_security 2

正式 apply：

- Event 总数：142
- change_count：139
- before：NULL 138，gov_service 2，other 2
- after：safety 34，other 36，market 10，livelihood 12，gov_service 24，healthcare 8，traffic 4，education 9，public_emergency 3，social_security 2

apply 后复核 dry-run：

- change_count：0
- 当前分布：education 9，gov_service 24，healthcare 8，livelihood 12，market 10，other 36，public_emergency 3，safety 34，social_security 2，traffic 4

## 九、未实施内容

- 未自动清理历史污染 Opinion、Event、Alert。
- 未新增数据库字段、索引或 migration。
- 未引入 ES、Redis、MQ、LLM、外部 NLP 平台。
- 未扩展成全国地理识别平台。
- 未改事件处置系统。
- 未改微博评论分流和微博准入主规则。

## 十、风险和后续建议

- 地区词规则是轻量词典，适合廊坊监测闭环；若后续扩大城市范围，应扩展词典和测试，不建议直接做全国地名识别平台。
- 历史地区污染数量明确，但本次按约束只读扫描。建议后续单独走人工审核清理方案，先备份再按样例核验。
- 主题分类是可解释规则，后续可按误判样例补充词表和权重，但不应引入重型 NLP。
- 测试库原 `opinion_test` schema 陈旧且迁移链断在旧 alert migration，已用独立 `opinion_p0_test` 验证本次任务；建议后续修复统一测试库初始化流程。
