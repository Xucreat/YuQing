# 舆情监测系统交付前全面审计 — D1 报告

- **文档性质**：审计执行报告（只读，未修改任何代码/数据/配置/服务）
- **执行角色**：交付前质量审计负责人
- **执行时间**：2026-07-24 18:00 GMT+8
- **执行范围**：D1 = 数据库审计 + 系统架构审计 + 审计环境确认
- **纪律遵守**：全程仅 SELECT / GET / 文件读取；数据库连接前已通过身份门禁；无任何写操作

> **🔴 头条警报（不等待评分，立即上报）**
> 发现 **P0 认证完全绕过** 漏洞（ARCH-01）：JWT 签名密钥运行时实际生效值为源码中公开的默认弱值 `change-me-in-production`。已**实测**用伪造 admin token 成功访问生产接口（HTTP 200 返回真实数据）。
> **结论：当前状态不建议交付。**

---

## 一、审计环境确认结果

| 项 | 实测值 | 判定 |
|----|--------|------|
| 代码目录 | `C:\Users\Administrator\Desktop\YQ` | ✅ 符合基线 |
| 当前代码版本 | git `b184f86b`（2026-07-24 17:42:09）"feat: RBAC 三角色权限收口与采集器解析健壮性优化" | ✅ 最新提交 |
| 启动入口 | `backend/app/main.py`（FastAPI app，`title=大厂县公安互联网舆情监测研判平台`，version 0.1.0） | ✅ |
| 当前运行服务 | uvicorn `:8000`(PID 27020)、`:8011`(PID 75212)；PostgreSQL `:5432`(PID 61436) | ✅ 在运行 |
| Python 环境 | 生产 venv `backend/.venv`（3.13）；managed 3.13.14 | ✅ |
| Node 环境 | 22.22.2（managed） | ✅ |
| PostgreSQL 版本 | 16；`system_identifier=7663057120701798896` | ✅ 匹配预期 |
| **数据库连接目标** | `postgresql+psycopg://opinion_user:***@127.0.0.1:5432/opinion_db`；进程 `-D "C:/Users/Administrator/Desktop/舆情监测系统/pgdata"` | ✅ **真实生产库，非克隆/空库** |
| 数据库身份门禁 | `db_identity_check.py` → **[DATABASE IDENTITY: VERIFIED]**，opinions=1040（>100 阈值） | ✅ 通过 |
| Alembic 版本 | DB current = `p12_rbac_roleperms`；`alembic heads` 单头一致，无分叉；18 个迁移文件 | ✅ 版本对齐 |
| 前后端版本一致性 | 后端 0.1.0；前端版本核对属 D4 范围，本阶段未涉及 | ⏸ 待 D4 |

**基线锁定确认**：5432 实例 `-D` 参数经进程命令行核实指向 `舆情监测系统\pgdata`（用户指定的唯一生产库）。由于本环境 `system_identifier` 无法区分文件级克隆，已叠加"进程 -D 路径 + 业务指纹 opinions=1040"双重确认，**审计对象合法，未发现连错库/版本不匹配**。`alembic check` 检出模型漂移（见 DB-01），但 DB 版本号与迁移头一致，不构成"代码版本与 schema 不匹配"的停止条件，审计继续。

---

## 二、数据库审计结果

### 2.1 总体数据画像（精确 count）

| 表 | 行数 | 表 | 行数 |
|----|------|----|------|
| collector_runs | 4265 | alert_records | 79 |
| opinions | 1040 | user_login_logs | 50 |
| propagation_nodes | 635 | user_operation_logs | 21 |
| event_opinions | 414 | data_sources | 30（启用 22） |
| events | 206 | keywords | 46（sensitive 17 / monitoring 29） |
| permissions | 26 | roles | 3 |
| role_permissions | 19 | regions | 17 |
| users | 3 | alert_rules | 2 |
| **user_roles** | **0** | | |

> 注：`pg_stat_user_tables.n_live_tup` 对 keywords/roles/data_sources 等显示为 0，系**从未 ANALYZE**（见 DB-03），精确 count 才是真实值。

### 2.2 数据完整性 —— 良好

- **孤儿数据全部为 0**：event_opinions→opinions/events、alert_records→opinions/handled_by、propagation_nodes→opinions、events→event_opinions 均无孤儿；events 无"零关联"事件。
- **外键：14 个真实 FK**（非逻辑外键），覆盖全部核心关联（alert_records×4、event_opinions×2、opinions→regions、propagation×3、RBAC×4）。数据完整性由 DB 层强保证。
- **url 唯一约束生效**：`ix_opinions_url_unique`（部分唯一索引），opinions 重复 url 组数 = 0。
- **告警双写一致**：alert_records 的 `handled` 与 `status` 完全一致（false_positive/resolved→handled=true，pending→false）。

### 2.3 risk-v2.0 落库 —— 良好

- `risk_model_version`：**risk-v2.0 = 1039 / 1040（99.9%）**，仅 1 条历史 NULL。
- `risk_factors` 非空 1039/1040。
- `risk_category`：other 973 / safety_accident 31 / political 15 / social_security 7 / **NULL 14**。
- risk_score 段：high(≥70)=17 / med(40-69)=39 / low(<40)=984。
- 正面误报保护：`sentiment=positive AND risk_score≥70` 仅 **4 条**（待 D2 抽样确认是否均为危害词命中的合理保留）。

### 2.4 历史告警口径 —— 已被治理（修正方案假设）

- `risk_level`：medium 22 / critical 16 / low 41（合计 79）。
- `status`：pending 43 / false_positive 34 / resolved 2。
- **修正**：方案基线假设的"74 critical / 9 low"已不存在，当前 critical 仅 16，说明历史告警口径已经过治理/重算。

### 2.5 数据库问题清单

**编号：DB-01**
- **等级**：P2
- **问题**：`alembic check` 检出模型定义与数据库 schema 漂移（drift）。
- **证据**：`alembic check` 输出 `FAILED: New upgrade operations detected`，涉及 `data_sources.key`、`keywords.word`(缺单列 index)、`permissions/roles.description`(nullable 表述)、`role_permissions/user_roles`(模型声明 UniqueConstraint 而库中为 PRIMARY KEY)、`user_operation_logs`(索引命名)。经 SQL 复核：唯一性均由 PK 保证、`role_permissions/user_roles` 重复行=0、`description` 库中已是 nullable。
- **影响**：功能与数据完整性**无损**（差异集中在索引命名与约束类型表述）；但模型与迁移不同步会误导后续 autogenerate，产生噪声迁移。
- **是否影响交付**：否。
- **建议**：交付后补一次对齐迁移或修正模型声明，使 `alembic check` 干净。

**编号：DB-02**
- **等级**：P2（数据增长后升 P1）
- **问题**：核心大表关键查询列缺索引。
- **证据**：`opinions` 仅 3 索引（pkey/region_id/url_unique），被索引列仅 `id/region_id/url`；`publish_time`、`created_at`、`risk_score`、`sentiment`、`analysis_status`、`risk_level`、`risk_category` **无索引**。`events` 仅 pkey；`alert_records` 无 `status` 索引；`collector_runs`(4265 行) 无 `start_time`/`collector_name` 索引。
- **影响**：当前数据量（opinions 1040）全表扫描无感；但大屏聚合、列表按时间/风险/状态过滤在 10 倍以上数据量将显著变慢。
- **是否影响交付**：否（当前量级）。
- **建议**：按高频查询路径补索引（opinions: publish_time、risk_score、analysis_status；alert_records: status；collector_runs: start_time）。

**编号：DB-03**
- **等级**：P2
- **问题**：核心配置表从未执行 ANALYZE，统计信息缺失。
- **证据**：`pg_stat_user_tables` 中 keywords/roles/data_sources 的 `last_analyze` 与 `last_autoanalyze` 均为 `None`，`n_live_tup=0`（与精确 count 46/3/30 不符）。
- **影响**：查询计划器可能基于错误统计选错执行计划。
- **是否影响交付**：否。
- **建议**：执行 `ANALYZE`（或纳入运维例行 VACUUM ANALYZE）。

**编号：DB-04**
- **等级**：P2
- **问题**：opinions 存在标题级重复。
- **证据**：`(title,source)` 重复 41 组、多余行 49 条（占 1040 的 4.7%）；但 url 重复=0。
- **影响**：url 去重有效，标题级重复多为转载/多轮采集所致，可能轻微影响统计口径。
- **是否影响交付**：否。
- **建议**：评估是否需要标题+来源的二级去重策略（属产品口径决策，非缺陷）。

**编号：DB-05**
- **等级**：P3
- **问题**：14 条 `risk_category` 为 NULL、1 条 `risk_model_version` 为 NULL。
- **证据**：见 2.3 分布。
- **影响**：历史残留，对新数据无影响；大屏分类分布会有极小占比缺失。
- **是否影响交付**：否。
- **建议**：可选的历史回填（非必须）。

**编号：DB-06**
- **等级**：P3（运营层面，非技术缺陷）
- **问题**：43 条告警长期处于 pending 未处置（占 54%）。
- **证据**：alert_records status 分布 pending=43。
- **影响**：反映处置闭环在运营上未跟进，不影响系统功能。
- **是否影响交付**：否。
- **建议**：交付培训中明确告警处置 SOP。

---

## 三、架构审计结果

### 3.1 总体评价

- **模块划分清晰**：`backend/app/` 分层为 `api`(15 路由) / `services`(12+，含 ai/、event/ 子包) / `models`(13) / `collectors` / `core` / `db` / `schemas` / `utils`，职责边界基本合理。
- **路由前缀统一**（修正方案假设）：全部经 `main.py` 以 `/api` 统一挂载；`/admin/data-sources`、`/collector`、`/tasks` 均为 `/api` 下的二级路径，非并列前缀。
- **技术债可控**：TODO/FIXME 仅 14 处且多带 Phase 说明（ai_service Phase2、collectors Phase4、cache Redis）。
- **但存在多个安全与工程卫生问题**，其中一个为 P0（见下）。

### 3.2 架构问题清单

**编号：ARCH-01 🔴**
- **等级**：**P0（不建议交付）**
- **问题**：JWT 签名密钥为源码公开默认值，**认证可被完全绕过**。
- **证据**：
  - `app/core/config.py:40` `secret_key: str = "change-me-in-production"`。
  - 运行时实测：`settings.secret_key == "change-me-in-production"` 返回 `True`，长度 23（root `.env` 虽有 `SECRET_KEY` 行，但生效值仍为该默认弱值）。
  - **API 实测**：用该公开默认密钥 + HS256 伪造 `{"sub":"1"}`（admin）token，请求 `GET http://127.0.0.1:8000/api/opinions?page=1&page_size=1` → **HTTP 200，返回真实舆情数据**。
- **影响**：任何知晓该默认值（值就在开源/交付源码中）的人可伪造任意用户（含 admin）身份，访问全部数据与管理接口。认证机制形同虚设。
- **是否影响交付**：**是，直接阻断交付。**
- **建议**：生产必须设置高强度随机 `SECRET_KEY`（≥32 字节随机）并确保 `.env` 正确加载生效；建议加启动自检：secret_key 等于默认值时拒绝启动。

**编号：ARCH-02**
- **等级**：P1
- **问题**：CORS 全开。
- **证据**：`app/main.py:48-54` `allow_origins=["*"]`, `allow_methods=["*"]`, `allow_headers=["*"]`（`allow_credentials=False`）。
- **影响**：任意来源网站的前端 JS 可调用本 API；credentials=False 略降风险（不自动带 cookie），但 Bearer token 场景仍存在跨站滥用面。
- **是否影响交付**：建议交付前修复。
- **建议**：限制为受信任前端来源白名单。

**编号：ARCH-03**
- **等级**：P1
- **问题**：危险清库脚本残留项目根目录，且不受数据库身份门禁保护。
- **证据**：根目录 `clear_db.py`、`clear_db2.py`。`clear_db.py` 内容直接 `from app.db.session import SessionLocal` 并 `db.query(...).delete()` 删除 propagation_nodes/event_opinions/alert_records/events/opinions，`db.commit()`。该脚本经 SessionLocal 直连生产库，**不经过 `db_identity` 门禁**（门禁仅覆盖 alembic/迁移路径）。
- **影响**：误运行即导致生产核心业务数据被清空且无门禁拦截。
- **是否影响交付**：建议交付前移除/隔离。
- **建议**：从交付包剔除所有 `clear_db*.py`、`fix_*.py`、`add_debug.py` 等一次性脚本；如需保留运维脚本须纳入 `scripts/` 并接入身份门禁。

**编号：ARCH-04**
- **等级**：P2
- **问题**：无鉴权调试端点暴露。
- **证据**：`app/main.py:56-60` `@app.get("/_debug_static")` 无任何认证依赖，返回 `static_dir` 服务器绝对路径等信息。
- **影响**：泄露服务器目录结构，属调试残留。
- **是否影响交付**：建议修复。
- **建议**：交付前移除该端点或加鉴权。

**编号：ARCH-05**
- **等级**：P2
- **问题**：项目根目录工程卫生差，临时文件大量堆积。
- **证据**：根目录含 32 个 `.py`（add_debug/clean_debug/clear_db/clear_db2/fix_*/create_*/decompress_* 等一次性脚本）、12 个 `.cjs`（chk/chk2/fix2-9/fix_app 等）、17 个 `.log`、62 个 `.md` 报告。
- **影响**：交付包混杂大量开发废弃物，降低专业度并混淆真实代码边界。
- **是否影响交付**：否（但强烈建议清理）。
- **建议**：整理到 `scripts/`、`docs/`、`logs/` 或从交付包剔除（清理动作需用户确认，本阶段不执行）。

**编号：ARCH-06**
- **等级**：P2
- **问题**：分层不彻底，部分 router 直接操作 ORM。
- **证据**：`app/api/` 中 30 处直接出现 `.query(` / `db.execute` / `text(` 等数据库访问；15 个路由中仅 11 个 import `app.services`。
- **影响**：简单 CRUD 直接在 router 写查询，业务逻辑与接口层耦合，严格企业标准下职责边界模糊。
- **是否影响交付**：否。
- **建议**：后续将数据访问逐步下沉到 service/repository 层。

**编号：ARCH-07**
- **等级**：P2
- **问题**：源码文件注释编码损坏。
- **证据**：`app/api/__init__.py` 第 1-41 行中文注释为 GBK/UTF8 错乱乱码（如"API 璺敱鑱氬悎"），含 BOM 头；而第 42-52 行中文注释正常。代码逻辑部分（import/include_router）不受影响。
- **影响**：不影响运行，但损害可读性与可维护性（建议以 node 二次确认排除虚拟化读取假象）。
- **是否影响交付**：否。
- **建议**：以 UTF-8 重写该文件注释。

**编号：ARCH-08**
- **等级**：P2
- **问题**：RBAC 存在双权限源，新机制空转。
- **证据**：`app/core/permissions.py:27-44` `get_user_permissions` 同时读取"主角色 `users.role` 字符串列"与"附加角色 `user_roles` 关联表"；而 `user_roles` 表行数=0。当前权限完全由 `users.role` 字符串驱动（admin→superuser `["*"]`、analyst→13 权限、viewer→6 权限，均实测生效）。
- **影响**：新增的多对多授权机制（user_roles 表 + uq_user_role 约束）已建但无数据、未被使用；两套并存增加理解成本。此外 admin 角色（role_id=1）在 role_permissions 无映射，完全依赖 `is_superuser` 特判，若某用户 role='admin' 但 is_superuser=False 将得到空权限（当前无此数据）。
- **是否影响交付**：否（RBAC 功能实测生效）。
- **建议**：明确单一权限源，二选一收敛。

**编号：ARCH-09**
- **等级**：P3
- **问题**：死代码残留。
- **证据**：`app/services/event_service.py` 标注 `[DEPRECATED]`、`aggregate()` 返回 `[]`，全代码库 grep `EventService` 零引用；`app/services/ai_service.py` 为 ABC + 2 处未落地 TODO。
- **影响**：无功能影响，属清理项。
- **是否影响交付**：否。
- **建议**：删除 `event_service.py`。

**编号：ARCH-10**
- **等级**：P1（待 D3 确认）
- **问题**：admin 初始密码为弱默认值。
- **证据**：`app/core/config.py:46` `init_admin_password: str = "admin123"`，运行时 `settings.init_admin_password == "admin123"` 为 `True`。
- **影响**：若 admin 账号仍沿用初始密码即为弱口令（结合 ARCH-01 风险叠加）。实际库内密码哈希是否已修改属 D3（安全审计）范围。
- **是否影响交付**：待 D3 确认；若未改则建议交付前修复。
- **建议**：D3 阶段验证 admin 实际密码强度；生产初始化后强制改密。

### 3.3 架构正面结论

- 模块分层结构清晰、路由前缀统一（`/api`）、外键完整、启动生命周期含僵尸采集回收（`main.py:20-35`）、SPA fallback 合理、身份门禁机制设计到位（虽未覆盖 clear_db 直连场景）。

---

## 四、已发现问题清单汇总

| 编号 | 等级 | 问题 | 影响交付 |
|------|------|------|---------|
| **ARCH-01** | **P0** | JWT 密钥为公开默认值，认证完全可绕过（已实测） | **是（阻断）** |
| ARCH-02 | P1 | CORS 全开 `allow_origins=["*"]` | 建议交付前修复 |
| ARCH-03 | P1 | 危险清库脚本 clear_db*.py 残留根目录、无门禁保护 | 建议交付前修复 |
| ARCH-10 | P1 | admin 初始密码默认 admin123（待 D3 确认） | 待确认 |
| ARCH-04 | P2 | `/_debug_static` 无鉴权调试端点 | 建议修复 |
| ARCH-05 | P2 | 根目录临时文件堆积（32py+12cjs+17log+62md） | 否 |
| ARCH-06 | P2 | 分层不彻底，router 30 处直接 ORM | 否 |
| ARCH-07 | P2 | `api/__init__.py` 注释编码损坏 | 否 |
| ARCH-08 | P2 | RBAC 双权限源，user_roles 空表空转 | 否 |
| ARCH-09 | P3 | 死代码 event_service.py | 否 |
| DB-01 | P2 | alembic 模型↔schema drift（无数据损失） | 否 |
| DB-02 | P2→P1 | 核心大表关键查询列缺索引（增长后风险） | 否（当前量级） |
| DB-03 | P2 | 核心配置表从未 ANALYZE | 否 |
| DB-04 | P2 | opinions (title,source) 重复 49 行 | 否 |
| DB-05 | P3 | 14 条 risk_category NULL + 1 条无 risk 版本 | 否 |
| DB-06 | P3 | 43 条 pending 告警堆积（运营） | 否 |

**统计**：P0 × 1，P1 × 3（含 1 待确认），P2 × 8，P3 × 3。

---

## 五、疑似 P0 单独报告

### 🔴 P0-1：JWT 认证完全可绕过（ARCH-01）

- **性质**：认证绕过（用户方案中定义的立即 P0 触发条件之一）。
- **确证方式**：静态（源码默认值）+ 运行时（settings 生效值）+ **动态实测**（伪造 admin token 调生产接口返回 HTTP 200 真实数据）三重证据链闭合。
- **结论**：**不建议交付**。此问题不因其它维度得分高而缓解——只要 secret_key 为公开默认值，整个认证/授权体系（含 RBAC）在攻击者面前均可绕过。
- **修复方向**（供后续阶段执行，本阶段不修改）：生产环境注入高强度随机 `SECRET_KEY` 并验证 `.env` 加载生效；增加启动自检拒绝默认密钥；轮换现有已签发 token。

> 另有两项 P1 具备"数据不可逆风险"属性，一并提前提示：ARCH-03（clear_db*.py 无门禁清库脚本残留）虽非 P0，但误运行后果等同"数据严重丢失"，建议与 P0 同批处理。

---

## 六、审计边界与说明

- 本阶段全程只读：所有数据库访问为 SELECT，所有 API 调用为只读 GET（伪造 token 实测仅访问 GET 接口，未写入任何数据）。
- 未修改任何代码、数据、配置、测试；未重启任何服务；未删除任何文件。
- 方案基线中的 3 处假设经实证被修正：① `collector_runs` 已有 `batch_id` 列+索引；② 历史告警口径已治理（非 74 critical）；③ 路由为 `/api` 统一前缀（非三并列前缀）。
- `.env` 中 `SECRET_KEY` 未生效为强密钥的根因（值本身即默认串 vs 加载路径问题）需在修复阶段进一步定位，本阶段仅确认"运行时生效值为弱默认值"这一事实。

---

**D1 审计完成，在此暂停，等待下一步指令。**
如需立即进入 P0 修复，请注意本审计阶段的只读约束——修复应作为独立授权动作执行。
