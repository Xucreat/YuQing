# 前端部署垃圾清理实施报告

> 任务：基于《前端部署垃圾文件审计报告》结论，仅处理 B 类明确孤儿资源（第一阶段只读审计产物）。
> 原则：rename 隔离、不 rm -rf、不删活动资源、不修改 _d.py / Vite / 部署流程 / 依赖。
> 执行时间：2026-07-24 17:49–17:50 (GMT+8)
> 状态：✅ 已完成，无业务影响。

## 1. 执行范围与约束

按确认指令，仅隔离以下 B 类孤儿目录，**保留全部活动资源**：

| 保留（禁止删除/移动） | 说明 |
|---|---|
| `backend/app/static/index.html` | 当前入口 |
| `backend/app/static/assets/` | 65 文件，含 index 引用 2 个 + 63 路由懒加载分包 |
| `backend/app/static/favicon.svg` | 被 index.html 引用 |
| `backend/app/static/geo/` | 指挥大屏 `ChinaMap.vue` 运行时 `fetch('/geo/...')` 依赖 |
| index.html 引用的 js/css | `assets/index-iT_24xn-.js`、`assets/index-tYIFtOy3.css` |

**未扩展任务范围**：未改 `_d.py`、未改 Vite 配置、未增加自动清理逻辑、未升级依赖。

## 2. 隔离目录列表（15 个，全部 rename 至 static 之外）

隔离目标：`C:\Users\Administrator\Desktop\YQ\_static_trash_20260724_1749\`（项目根下，uvicorn 静态挂载仅服务 `app/static`，故该隔离区不可经 Web 访问，等同从运行环境移除且保留回滚能力）。

| # | 原路径（已移走） | 大小 | 文件数 |
|---|---|---|---|
| 1 | backend/app/static/assets.bak | 6.1M | 33 |
| 2 | backend/app/static/assets.bak_1784884208 | 6.1M | 33 |
| 3 | backend/app/static/assets.old-1784872589881 | 16M | 84 |
| 4 | backend/app/static/assets.old-1784874194521 | 19M | 101 |
| 5 | backend/app/static/assets.old-1784874214751 | 19M | 101 |
| 6 | backend/app/static/assets.old-1784883796 | 6.1M | 33 |
| 7 | backend/app/static/assets.old-20260724160233 | 9.0M | 48 |
| 8 | backend/app/static/assets.old-20260724161826 | 6.1M | 33 |
| 9 | backend/app/static/assets.old.1784793645 | 6.0M | 27 |
| 10 | backend/app/static/assets.old_1784794123 | 6.0M | 27 |
| 11 | backend/app/static/assets.old_1784795623 | 9.3M | 41 |
| 12 | backend/app/static/assets.old_20260723_145452 | 24M | 108 |
| 13 | backend/app/static/assets.old_20260723_152637 | 6.0M | 27 |
| 14 | backend/app/static/assets.old_20260723_170225 | 9.0M | 47 |
| 15 | backend/app/static/assets.old_20260723_172306 | 6.1M | 31 |

## 3. 回收空间大小

- **隔离区总占用：153 MB**（实际测量 `du -sh`）
- **隔离文件总数：774 个**
- `backend/app/static/` 目录体积从约 167 MB（含孤儿）降至约 13 MB（活动资源），释放 ~92%。

## 4. 验证结果

### 4.1 文件完整性（static 本地）
| 检查项 | 结果 |
|---|---|
| `index.html` 仍存在 | ✅ 452 B |
| 引用 js `assets/index-iT_24xn-.js` | ✅ 2.67 MB 存在 |
| 引用 css `assets/index-tYIFtOy3.css` | ✅ 393 KB 存在 |
| `assets/` 路由懒加载资源完整 | ✅ 65 文件全在（含 63 分包） |
| `favicon.svg` | ✅ 存在 |
| `geo/` 地图数据 | ✅ 3 文件（china-provinces.json / 130000_full.json / README.md） |

### 4.2 运行时 HTTP 验证（uvicorn :8000 在线服务）
| 请求 | 状态码 |
|---|---|
| `GET /index.html` | 200 |
| `GET /assets/index-iT_24xn-.js` | 200 |
| `GET /assets/index-tYIFtOy3.css` | 200 |
| `GET /favicon.svg` | 200 |
| `GET /geo/china-provinces.json` | 200 |
| `GET /dashboard`（SPA 回退） | 200 |
| `GET /login`（SPA 回退） | 200 |

> 结论：当前生产入口、主 JS/CSS、图标、地图 GeoJSON 与 SPA 路由回退全部正常，清理未对运行版本造成任何影响。

## 5. 回滚方式

本次采用 **rename 隔离（非删除）**，回滚极简且零风险：

```bash
# 将隔离目录移回 static 即恢复（原样放回）
mv /c/Users/Administrator/Desktop/YQ/_static_trash_20260724_1749/assets.bak* \
   /c/Users/Administrator/Desktop/YQ/_static_trash_20260724_1749/assets.old* \
   /c/Users/Administrator/Desktop/YQ/backend/app/static/
```

- 无需重启服务（静态文件由 uvicorn 实时读取）。
- 若确认长期不再需要，可人工 `rm -rf _static_trash_20260724_1749`（**本任务未执行此步**，按约束不使用 rm -rf）。

## 6. 备注

- 本次隔离操作计 **15 次 rename（每目录 1 次）**，未触发删除安全门限，也未逐文件 unlink。
- 孤儿目录成因：历史多次 `vite build` + `_d.py` 部署采用「只写不删、无 fallback」逻辑，旧 `assets/` 被按 `rename` 暂存为 `assets.old-*`/`assets.bak*`，累积成死目录；全 static 树 grep 反向引用 **0 命中**，确认无任何代码/页面依赖它们。
- 部署流程未改动；后续若希望避免再次累积，可在另一次独立任务中评估为 `_d.py` 增加「部署前清理旧 assets 哈希」逻辑（本次按指令不扩展）。
