# Phase Security-2-F：权限覆盖率报告

- 生成时间：2026-07-31
- 目标：写操作 API 保护率、前端管理入口控制率、角色测试覆盖率 → **100%**

> 本报告由只读审计脚本自动生成，全过程未修改任何代码、数据库记录或权限数据。
> 数据来源：生产库 `opinion_db@127.0.0.1:5432`（仅 SELECT）+ FastAPI 路由内省 + 前端源码静态扫描。

---

## 一、后端 API 覆盖率

| 指标 | 数值 |
|---|---|
| API 路由总数 | 103 |
| 写操作路由（POST/PUT/PATCH/DELETE） | 49 |
| 其中：公开端点（`POST /api/login`，设计如此） | 1 |
| 其中：自身操作（`POST /api/logout`，无风险） | 1 |
| **应受权限保护的写路由** | **47** |
| **实际受保护（require_permission / require_admin）** | **46** |
| **写操作保护率** | **46/47 = 97.9%** |
| 未达标 | 1（`POST /api/propagation/rebuild/{event_id}`） |

| 读路由防护分布 | 数量 |
|---|---|
| `require_permission` | 24 |
| `require_admin` | 2 |
| 仅登录态 | 26 |
| 公开 | 2 |
| **读操作细粒度保护率** | **48.1%** |

---

## 二、前端覆盖率

| 指标 | 数值 |
|---|---|
| 需要权限门槛的路由 | 13 |
| 已设置 `meta.permission` | 13 |
| **路由门槛覆盖率** | **100%** |
| 需要门控的操作组 | 24 |
| 已门控（含容器级 / 路由级兜底） | 23 |
| **操作门控覆盖率** | **95.8%** |
| 未达标 | 1（`Propagation.vue` 构建传播链） |

---

## 三、权限码使用覆盖率

| 指标 | 数值 |
|---|---|
| 权限码总数 | 31 |
| 后端有路由引用 | 20 |
| **后端引用率** | **64.5%** |
| 已授予至少一个角色（不含超管短路） | 18 |
| **角色授权覆盖率** | **58.1%** |
| 孤儿权限（无角色持有） | 13 |
| 仅前端强制的读权限 | 5 |
| 真·死权限（前后端均未引用） | 6 |

---

## 四、角色测试覆盖率（现状）

| 角色 | 是否存在于生产库 | 现有 RBAC 测试是否覆盖 |
|---|---|---|
| `admin` | ✅ | ✅ |
| `analyst` | ✅ | ✅ |
| `viewer` | ✅ | ✅ |
| （动态新增角色） | — | ⚠️ 现有测试为硬编码角色，未从数据库动态读取 |

> 现有 `tests/test_rbac_hardening.py` 已覆盖 3 个角色共 71 个用例并全部通过（Phase RBAC-1 成果），但角色列表是**硬编码**的。任务书 Phase 10 要求改为**从数据库动态读取角色**，这样新增角色时测试自动扩展。该项属整改阶段任务，本阶段仅记录现状。

---

## 五、覆盖率汇总

| 维度 | 当前 | 目标 | 差距 |
|---|---|---|---|
| 写操作 API 保护率 | 97.9% | 100% | 1 个接口（SEC2-01） |
| 读操作细粒度保护率 | 48.1% | 视架构决策 | 26 个 GET 仅登录态（SEC2-02） |
| 前端路由门槛覆盖率 | 100% | 100% | ✅ 已达标 |
| 前端操作门控覆盖率 | 95.8% | 100% | 1 个按钮（SEC2-01） |
| 权限码后端引用率 | 64.5% | — | 5 个读权限仅前端强制 + 6 个死权限 |
| 权限角色授权覆盖率 | 58.1% | — | 13 个孤儿权限（SEC2-04） |
| 角色测试覆盖率 | 3/3 静态 | 全角色动态 | 需改为动态读取（Phase 10） |

---

## 六、阶段结论

1. 写操作 API 保护率 **97.9%**，距 100% 仅差传播链重建 1 个接口。
2. 前端路由门槛已 **100%** 覆盖；操作门控 **95.8%**，同样只差传播链 1 个按钮。
3. 修复 SEC2-01 后，写操作与前端两项覆盖率均可达 **100%**。
4. 本阶段**未做任何修改**。
