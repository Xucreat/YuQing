# 系统耦合度审计报告（只读）

> 审计时间：2026-07-30 ｜ 范围：`backend/app/`（120 模块，约 18.7k LOC）+ `frontend/`（39 组件）
> 方法：AST 静态导入图分析（模块级扇入 Ca / 扇出 Ce / 不稳定度 I=Ce/(Ca+Ce)、强连通分量循环检测、包级矩阵、分层违例、LOC 扫描）。**全程只读，未修改任何代码/配置/数据库。**

---

## 一、结论速览（✅ / ⛔）

- ✅ **导入依赖图是 DAG，0 个循环依赖** —— 模块级静态解耦良好，无 import 环（运行期循环导入风险极低）。
- ✅ **稳定性枢纽合理**：`core.config`(Ca27)、`db.session`(Ca22)、`models.user`(Ca21)、`db.base`(Ca19)、`models.opinion`(Ca18)、`collectors.base`(Ca18) 均为「高扇入 + 低扇出」的基础设施/基座，符合稳定抽象原则。
- ✅ **耦合分散度健康**：Top 12 模块才占 ≥50% 的总扇入，无单一上帝模块独占依赖；均值不稳定度 I=0.46（介于稳定与不稳定之间，正常）。
- ⛔ **存在 1 个 796 行「上帝模块」`collectors/service.py`**：直接串联 `RiskEngine`、`keyword_service`、`opinion_region_service`、`opinion_admission_service`、`ai.fallback` —— **采集管线是全局耦合中枢**。
- ⛔ **`core/scheduler.py` 在基础设施层硬编码串联** 采集→聚合→预警 三阶段（直接 import `CollectorService` / `auto_aggregate_after_collect` / `AlertService`）。
- ⛔ **7 个模块 >600 行**，变更风险集中（详见第六节）。
- ⚠️ **轻量反向依赖**：`models/opinion.py` 从 `services.risk_engine` 导入状态常量 `STATE_OCCURRED`（模型→服务，建议迁入常量/模型层）。
- ✅ **前端耦合度低**：集中式 `api/` 层（6 个领域文件 + 统一 request 封装），仅 **3/39** 组件含硬编码请求，4 个 pinia store，与后端契约解耦良好。

---

## 二、量化指标

| 指标 | 值 | 判读 |
|---|---|---|
| 应用模块数 | 120 | — |
| 应用代码 LOC | ~18,666 | — |
| 模块内导入边(有向) | 380 | — |
| 循环依赖组 | **0** | ✅ 无环 |
| 最大扇入 Ca | 27 (`core.config`) | 合理（基设） |
| 均值扇入 | 3.17 | 低，分布均匀 |
| 均值不稳定度 I | 0.46 | 正常 |
| 占 ≥50% 扇入所需 Top-N | 12 | ✅ 分散，非单点 |
| >600 行模块数 | 7 | ⛔ 偏高 |

---

## 三、稳定性枢纽（高扇入，健康）

| 模块 | Ca | Ce | I | 角色 |
|---|---|---|---|---|
| core.config | 27 | 0 | 0.00 | 配置基座（全系统依赖） |
| db.session | 22 | 1 | 0.04 | 数据库会话 |
| models.user | 21 | 1 | 0.05 | 用户 ORM |
| db.base | 19 | 0 | 0.00 | 声明式 Base |
| models.opinion | 18 | 2 | 0.10 | 舆情主模型 |
| collectors.base | 18 | 0 | 0.00 | 采集器抽象基类 |
| core.dependencies | 17 | 3 | 0.15 | 依赖注入(FastAPI Depends) |
| collectors.common | 13 | 0 | 0.00 | 采集公共工具 |

> 这些高扇入模块扇出都极低（I≈0），说明**稳定抽象落在正确的地方**——改它们代价高但本身很少依赖别人，符合「依赖指向稳定层」的良好结构。

---

## 四、包级依赖矩阵（跨包有向边，排除自环）

```
src\dst       api  collectors  core  db  main  models  schemas  services
api            0      3         32   17   0     46      15       26
collectors     0      0         14    0   0      4       0        5
core           0      1          0    3   0      3       0        2
db             0      0          1    0   0      0       0        0
main           0      1          2    1   0      0       0        0
models         0      0          0   19   0      0       0        1
services       0      0          9    1   0     46       5        0
```
**解读：**
- `services → models = 46` 是最重的一条跨包边 —— 领域服务与 ORM 强绑定（**数据耦合高**：改一个模型会波及大量服务）。
- `api → models(46) / api → core(32)` 正常（接口层依赖数据与基设）。
- `collectors → services = 5` 是**关键耦合点**：采集层反向依赖领域服务（见第五节）。

---

## 五、分层违例（真实问题，已核实）

| 层级越界 | 性质 | 严重度 | 说明 |
|---|---|---|---|
| `core.scheduler` → `services.event.aggregator` / `alert_service` / `collectors.service` | 基础设施层认知并硬编码领域服务 | 中 | 采集→聚合→预警三阶段在 scheduler 内串联；新增管线步骤需改基础设施 |
| `models.opinion` → `services.risk_engine` | 模型导入服务常量 `STATE_OCCURRED` | 低 | 仅一个状态常量，建议迁至模型/常量模块 |
| `collectors.service` → 5 个领域服务 | 采集层直接编排风险/关键词/地域/准入/AI 回退 | **高** | 见第六节，全局耦合中枢 |

> 其余 `core.dependencies/permissions → models.user/role`、`core → db.session` 属依赖注入与鉴权自然需求，非实质违例。

---

## 六、上帝模块（>600 LOC，变更风险集中）

| 模块 | LOC | 类 | 函数 | 扇出 Ce | 备注 |
|---|---|---|---|---|---|
| api.admin_data_sources | 879 | 0 | 25 | — | ⛔ 胖过程式路由：0 类、25 函数，应下沉到 service |
| collectors.service | 796 | 3 | 13 | 12 | ⛔ 采集管线中枢，串联 5 服务 |
| services.event.aggregator | 768 | 1 | 32 | 12 | 事件聚合核心（红线：禁改） |
| services.report_service | 752 | 0 | 24 | — | 报告生成 |
| services.dashboard_service | 729 | 0 | 14 | — | 驾驶舱指标 |
| services.event.narrative | 664 | 5 | 18 | 7 | 事件叙事 |
| services.event.migration | 647 | 0 | 12 | 9 | 事件迁移 |

> `__init__` 桶文件重导出也是耦合面：`models.__init__`(19)、`api.__init__`(17)、`collectors.__init__`(12)；实测 `models.__init__` 扇入为 0，说明业务代码走叶子模块导入（✅ 未通过桶文件耦合），但桶文件本身仍是单一重导出面。

---

## 七、前端耦合度

| 项 | 情况 | 判读 |
|---|---|---|
| 组件数 | 39（22 视图 + 15 组件） | — |
| API 层 | 集中 `src/api/`（admission/alert/event/opinion/report + index 封装） | ✅ 契约集中 |
| 硬编码请求组件 | 3 / 39 | ✅ 低（绝大多数走 api 层） |
| 状态管理 | 4 个 pinia store，引用分散 | ✅ 轻耦合 |
| 已知缺陷 | 无全局 401 拦截器 → token 失效静默白屏（DataManage.vue） | ⚠️ 韧性耦合问题 |

---

## 八、与当前四条主线的关系（设计视角，未改）

1. **地域关键词前置过滤**：天然接缝在 `CollectorService`——`keyword_service` 与 `opinion_region_service` 已被注入该模块。落地会**进一步加重 796 行上帝模块**，但可在 collector 层以分支方式实现（保持旧参数/返回向后兼容），**无需触碰 Event 聚合 / RiskEngine（红线）**。
2. **Grok / 微博辅助源**：经 `collectors.registry` 注册接入，与 `collectors.base` 低耦合（✅ 设计良好，默认 `enabled=false`、不进上报口径）。但**其输出流经同一 `CollectorService` 管线**（风险/关键词/地域），因此继承同一中心耦合——功能正确，但辅助源无法与主线关键词逻辑隔离。
3. **关键词架构重构**：`services → models(46)` 的高数据耦合意味着关键词模型/匹配逻辑改动会波及多服务；地域前置改造宜控制在 collector 层，避免上移到 services 全局。
4. **红线合理性**：`event.aggregator`(768 LOC / Ce12)、`risk_engine` 均为高内聚高内部耦合模块，任何改动影响面大——「禁改」的红线在耦合度层面成立。

---

## 九、建议（仅设计，未执行）

- 🔧 **抽离 CollectorService 管线策略**：将「风险评分 / 关键词匹配 / 地域过滤 / 准入」抽为 collector 层可插拔步骤（策略/管道接口），降低上帝模块、为地域前置提供干净分支点（不破红线）。
- 🔧 **`models/opinion` 的状态常量**迁入模型层或 `core/constants`，消除模型→服务反向依赖。
- 🔧 **`core/scheduler` 只触发 `CollectorService.run()`**，由服务内部串联采集→聚合→预警，避免基础设施认知领域服务。
- 🔧 **`api.admin_data_sources`(879 行) 下沉业务逻辑**到 service，路由回归薄层。
- 🔧 **前端补全局 401 拦截器**，消除静默白屏的韧性耦合。

> 以上均为设计建议；按本次「不修改任何」要求，**未对任何文件做改动**。
