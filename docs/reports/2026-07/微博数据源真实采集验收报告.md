# 微博数据源真实采集验收报告（Phase Weibo-1 真实采集阶段）

- 日期：2026-07-28
- 目标：对已完成集成的微博（八爪鱼 API）数据源做**一次性真实采集验收**
- 方法：`WeiboOctopusCollector` → `CollectorService` one-shot 手动驱动（注入式，绕过 `data_sources` 与 scheduler）
- 约束：不修改 scheduler / 不等待周期任务 / 不影响其他数据源 / 采集后 `data_sources.weibo_octopus.enabled` 保持 `false`

---

## 一、结论速览

| 验证项 | 结论 | 依据 |
|---|---|---|
| A. 八爪鱼 API 返回 | ✅ 接口可达、鉴权通过、响应解析正确 | 真实调用生产 API：token 获取成功，`GET /data/notexported` 返回 HTTP 200，但**任务当前数据量为 0**（`total=0`） |
| B. CollectorRun 审计 | ✅ 行为正确 | 生产库 CollectorRun id=7246：`status=success / fetched_raw=0 / created=0 / failed=0 / error_msg=None`（空上游时正确空跑，不报错、不造数） |
| C. Opinion 字段 | ✅ 字段映射正确（fixture） | 测试库经真实代码链路落库 11 条，`source=weibo`、`source_type=weibo_post`、external_id/author/engagement 结构与去重均正确 |
| D. 风险链路 | ✅ 链路无异常（fixture） | RiskEngine 评分 → 2 个 Event 关联 → 80 条 Alert 生成，全程无异常 |
| E. 数据质量 | ✅ 指标符合预期（fixture） | 空正文 0、重复 external_id 被去重识别、无 url 1 条、风险分布 high=2/low=9 |

**总体结论**：集成代码（鉴权、响应解析、入库、风险评分、事件聚合、预警生成）**全部验证通过**。
**唯一阻塞**：八爪鱼任务 `3d8b1968-a502-4df3-ad10-f3d00c32a6a5` 当前**无已采集数据**（`/data/notexported` 返回 `total=0`），因此生产库本次真实采集得到 0 条微博舆情。待该任务产出数据后，重跑 one-shot 即可落地真实微博数据（C/D/E 亦将在真实数据上复验）。

> 说明：A、B 为**生产环境真实调用**结果；C、D、E 因上游无数据，采用**与真实代码完全一致的链路**（真实 `WeiboOctopusCollector._map_row` + 真实 `CollectorService` + 真实 `RiskEngine` + 真实 `auto_aggregate_after_collect` + 真实 `AlertService.evaluate`）在**测试库**用八爪鱼字段形状的 fixture 验证，验证后已清理，不污染任何环境。

---

## 二、执行方式与约束遵守

- **驱动方式**：`backend/scripts/weibo_one_shot_verify.py` 将 `WeiboOctopusCollector` 直接注入 `CollectorService(collectors=[...])`，调用 `collect_and_analyze(db, trigger_type="manual")`，**不读取 `data_sources` 表、不触发 scheduler**。
- **生产库身份门禁**：写库前运行 `scripts/db_identity_check.py`，结果 `VERIFIED`（system_identifier 匹配、alembic head = p13_weibo_fields、opinions=541）。
- **约束符合性**：

| 约束 | 是否符合 | 说明 |
|---|---|---|
| 1. 不修改 scheduler | ✅ | 未改动 `scheduler.py` / `main.py`；仅独立脚本驱动 |
| 2. 不等待周期任务 | ✅ | 手动 one-shot，未依赖 cron/interval |
| 3. 不影响其他数据源 | ✅ | 生产库微博舆情 0 条（无污染）；下游 fixture 仅在测试库并清理 |
| 4. 采集后 `data_sources.weibo_octopus.enabled=false` | ✅ | 验证后仍为 `False`；one-shot 直接注入采集器，从未翻转该开关 |
| 5. 输出真实采集验收报告 | ✅ | 本报告 |

- **开关状态**：按用户要求 `WEIBO_ENABLED=true`（运行总开关已开，作为待命）；`data_sources.weibo_octopus.enabled=false`（数据源级仍关，scheduler 不会周期采集微博）。

---

## 三、A. 八爪鱼 API 返回验证（真实）

- 鉴权：`BAZHU_API_KEY` 未设置，改用 `BAZHU_USERNAME/BAZHU_PASSWORD` 经 `POST /token` 换取 `access_token`（长度 1408，前缀 `eyJ…`），**成功**。
- 拉取：`GET /data/notexported?taskId=3d8b1968-…&size=100` 返回 **HTTP 200**，原始响应：

```json
{"data":{"total":0,"current":0,"data":null},"requestId":"0HNNBOV5GDLFQ:0000003F"}
```

- 结论：接口可达、鉴权有效、响应结构被 `_fetch_not_exported` 正确解析（无报错）。**但任务当前无未导出数据（total=0）**，故本次无真实微博可入库。
- 网络：经环境代理 `HTTPS_PROXY=127.0.0.1:7897` 自动路由，与 Grok 采集器同范式，无需改代码。

---

## 四、B. CollectorRun 审计（真实，生产库）

| 字段 | 值 |
|---|---|
| id | 7246 |
| collector_name | 微博 |
| status | `success` |
| fetched_raw | 0 |
| created | 0 |
| failed | 0 |
| error_msg | `None` |
| start_time / end_time | 2026-07-28 11:26:03 → 11:26:03 |

- 空上游时采集器正确空跑：不抛异常、不写假数据、审计行 `status=success` 且 `error_msg` 为空，符合 Phase Weibo-1 设计（失败才记 `failed`，空数据不算失败）。

---

## 五、C. Opinion 字段校验（fixture，真实代码链路，测试库）

一次性驱动经真实 `_map_row` + `CollectorService` 落库 12（原始）→ 11（去重 1 条重复 `mb1`）条微博舆情，全部 `analysis_status=completed`：

| 校验点 | 结果 |
|---|---|
| 总数 | 11 |
| `source` 全部 = `weibo` | ✅ 11/11 |
| `source_type` 全部 = `weibo_post` | ✅ 11/11 |
| `external_id` 存在 | ✅ 10/11（1 条 fixture 故意无 external_id，验证回退分支） |
| `author` 存在 | ✅ 11/11 |
| `engagement` 存在 | 4/11（仅含互动数字的行有；无互动行映射为 `None`） |
| `engagement` 结构正确（dict 含 likes/comments/reposts 且为 int） | ✅ 全部通过 |
| 去重生效（重复 `mb1` 仅入库一次） | ✅ `dedup_ok=true` |

字段映射样本（节选）：

| id | title | author | external_id | engagement | risk_score | risk_level | sentiment |
|---|---|---|---|---|---|---|---|
| 4025 | 廊坊某小区物业纠纷获调解 | 廊坊网友A | mb1 | {likes:120,reposts:5,comments:30} | 20 | low | positive |
| 4026 | 固安道路施工进展 | 固安发布 | mb2 | {likes:88,reposts:3,comments:12} | 20 | low | positive |
| 4027 | 三河一工厂事故通报 | 应急头条 | mb3 | {likes:12000,reposts:200,comments:450} | 85 | **high** | negative |
| 4028 | 香河民生服务升级 | 香河融媒 | — | None | 20 | low | positive |

> 注意 id=4027 的 `likes:"1.2万"` 被正确容错为 `12000`，且风险词「事故/爆炸」触发 `risk_score=85 / high`，证明互动数容错与 RiskEngine 评分均对微博数据生效。

---

## 六、D. 风险链路抽查（fixture，真实代码链路）

- `RiskEngine`：每条微博经 `refine()` 评分并写入 `risk_score/severity_score/event_state/risk_category` 等（见上表 high/low 分布），未改动评分规则。
- `Event` 聚合：手动一次性调用 `auto_aggregate_after_collect(SessionLocal)`（与 scheduler 同函数、非阻塞 advisory lock），本次新建 **2 个 Event**，`linked=2`；11 条微博舆情关联至 **2 个事件**（id=1695、1696）。
- `Alert` 评估：手动一次性调用 `AlertService.evaluate(db)` + `sync_alert_events(db)`（与 scheduler `_run_alert_eval_job` 一致），生成 **80 条** 预警记录指向微博舆情（rule `r2` 等命中），`status=pending`，风险等级由 `risk_score` 派生。
- **结论**：`Opinion → RiskEngine → Event → Alert` 全链路对微博数据**无异常**，且复用既有统一体系（未新建微博专用链路）。

---

## 七、E. 数据质量统计 + 与已有数据源比较（fixture）

本次微博（11 条 fixture）质量指标：

| 指标 | 值 | 说明 |
|---|---|---|
| 空 content 比例 | 0% | 采集器已丢弃无正文行 |
| 重复 external_id | 1（被去重） | 重复 `mb1` 已正确识别并跳过 |
| 无 url 比例 | 1/11 ≈ 9% | 设计内回退（title+publish_time 去重） |
| 风险等级分布 | high=2 / medium=0 / low=9 | 高风险来自「事故/腐败」等真实危害词 |

**与已有数据源比较**：生产库现有 16 个数据源、共 541 条舆情，风险分布示例：

| 数据源 | 总数 | high | medium | low |
|---|---|---|---|---|
| 新华网 | 72 | 9 | 3 | 60 |
| 人民网 | 106 | 5 | 6 | 95 |
| 中国新闻网 | 90 | 3 | 1 | 86 |
| 百度新闻 | 135 | 0 | 0 | 135 |
| 廊坊市政府网 | 31 | 0 | 1 | 30 |
| …（其余 11 个） | … | … | … | … |

微博接入后将成为第 17 个数据源。其风险分布形态取决于真实抓取内容；fixture 显示微博在高低风险两端均有分布，与现有新闻/政府源低风险为主的结构互补，符合舆情监测预期。

---

## 八、阻塞说明与复跑步骤

**阻塞根因**：八爪鱼任务 `3d8b1968-a502-4df3-ad10-f3d00c32a6a5` 的未导出数据缓冲为 0（`total=0`）。常见原因：
1. 该八爪鱼采集任务尚未实际运行 / 未抓到微博数据；
2. 任务虽建但搜索词/登录态失效导致无产出；
3. 任务 ID 指向的不是已产出数据的任务。

**复跑（真实数据落地）步骤**：
1. 在八爪鱼平台确认该任务已运行并产出微博数据（或换用有数据的 `BAZHU_TASK_ID`）。
2. 保持 `data_sources.weibo_octopus.enabled=false`、`WEIBO_ENABLED=true`。
3. 执行（生产库）：
   ```bash
   cd backend
   DATABASE_URL="postgresql+psycopg://opinion_user:opinion_pass@127.0.0.1:5432/opinion_db" \
   DB_IDENTITY_CHECK=off \
   .venv/Scripts/python.exe scripts/weibo_one_shot_verify.py
   ```
4. 脚本产出 `weibo_acceptance_result.json`，含真实 A–E 全部指标；届时 C/D/E 将在真实微博数据上复验。

---

## 九、涉及文件

- 新增（验证脚本，非业务代码）：`backend/scripts/weibo_one_shot_verify.py`、`backend/scripts/weibo_diag.py`、`backend/scripts/weibo_fixture_chain_verify.py`
- 新增（结果产物）：`backend/weibo_acceptance_result.json`、`backend/weibo_fixture_result.json`
- 既有（Phase Weibo-1 已实现，本次仅驱动验证，未改动）：`backend/app/collectors/weibo_octopus_collector.py`、`backend/app/collectors/service.py`、`backend/app/models/opinion.py`、`backend/alembic/versions/p13_weibo_fields.py`、`backend/app/core/config.py`、`backend/app/schemas/opinion.py`

> 验证脚本与结果产物均为验收辅助文件；如需保持仓库干净，可删除 `scripts/weibo_diag.py` 与 `weibo_*_result.json`（保留 `weibo_one_shot_verify.py` 供复跑）。
