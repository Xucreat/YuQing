# 舆情监测系统 v1.0.0 验收报告

> 冻结提交：`4cbf2fa578f42ef1e3eb44c3e4843a1a0653d712`
> 结论：**有条件通过（CONDITIONAL GO）** —— 满足代码冻结与归档条件；P0 安全项须在归档分发前处置。

## 1. 验收项核对

| # | 验收项 | 结果 | 依据 |
|---|---|---|---|
| 1 | 代码冻结基线可追溯 | ✅ | 提交 `4cbf2fa...` 已确定；将打 `v1.0.0-final` 标签并推送 |
| 2 | 数据库静态一致 | ✅ | running=0、迁移追平、无孤儿外键（数据库冻结报告） |
| 3 | 迁移追平 | ✅ | alembic head = `opinion_url_text_v1`，82 迁移，单 head 无 pending |
| 4 | 凭据不入库 | ✅ | `.env` gitignore；`.env.example` 入库；`/tmp/` 已忽略 |
| 5 | 测试库隔离 | ✅ | conftest 强制 `opinion_test@:5433`，误连生产直接抛错 |
| 6 | collection 挂起已修复 | ✅ | 全量 collection 1521 用例正常（测试与流水线报告） |
| 7 | 构建/编译/健康 | ✅ | compileall 通过、npm build 通过、`/health` 200、`/docs` 200 |
| 8 | RBAC 收口 | ✅ | 矩阵完整、403 边界清晰、动态角色覆盖（RBAC收口交付报告） |
| 9 | 交付文档齐全 | ✅ | 10 份最终归档文档齐备（MANIFEST） |
| 10 | 归档包（bundle+dump） | ⏳ | 待生成（见 MANIFEST） |

## 2. 有条件项（P0，分发前必须完成）

- [ ] 删除工作区 `tmp/` 浏览器凭据缓存，并对相关账号轮换凭据。
- [ ] 清理/重命名生产库 `mrUser_*` 测试账号。

## 3. 风险与缓解

- **测试执行未在隔离库验证**：本环境未启动 `:5433` 测试库，仅完成 collection + 冒烟。缓解：发布前在 CI 隔离环境补全全量执行（遗留问题 P1-5）。
- **user_roles 空**：授权依赖代码/默认角色路径。缓解：后续版本落表绑定（遗留问题 P1-3）。
- **数据源全未启用**：当前无活跃采集。缓解：按启用流程审批后开启（遗留问题 P1-4）。

## 4. 验收结论

代码、数据库、安全、测试（collection 维度）、RBAC 与文档均达到冻结标准；授权动作（push / tag / bundle / dump）可执行。P0 安全项不影响代码基线完整性，但**须在 v1.0.0-final 归档包对外分发前处置完毕**。综合判定：**CONDITIONAL GO**。
