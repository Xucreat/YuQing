# 舆情监测系统 v1.0.0 数据库冻结报告

> 冻结提交：`4cbf2fa578f42ef1e3eb44c3e4843a1a0653d712`
> 生成方式：只读查询（未对生产库执行任何写入、DROP、迁移或恢复）

## 1. 目标

在打 v1.0.0-final 标签前，确认生产数据库处于可冻结的静态一致状态：无运行中的采集任务、迁移已追平、无孤儿外键、实例身份可识别。

## 2. 实例与环境

| 项 | 值 |
|---|---|
| 引擎 | PostgreSQL 16 |
| 主机:端口 | 127.0.0.1:5432 |
| 库名 | opinion_db |
| system_identifier | 7663057120701798896 |
| 业务表数 | 50 |
| opinions | 1963 |

## 3. 迁移状态（Alembic）

- 当前版本：`opinion_url_text_v1`
- 历史迁移数：82
- `heads` 与当前一致，单一 head，**无 pending 迁移**
- 结论：数据库 schema 已追平代码基线，冻结前无需执行任何迁移

## 4. 采集运行状态

| 状态 | 数量 |
|---|---:|
| success | 17327 |
| failed | 222 |
| warning | 19 |
| running | **0** |

`collector_runs.running = 0`：冻结时无正在进行的采集任务，满足“零活跃写操作”冻结前置条件。

## 5. 数据完整性 / 孤儿外键

| 检查 | 结果 |
|---|---:|
| role_permissions 孤儿（缺失 role 或 permission） | 0 |
| user_roles 孤儿（缺失 user 或 role） | 0 |
| permissions 自引用 module 孤儿 | 不适用（无 module_id 列） |

## 6. RBAC 数据快照

| 项 | 值 |
|---|---:|
| roles | 5 |
| permissions | 89 |
| role_permissions | 131 |
| user_roles | 0 |

> 注：`user_roles` 为空，说明生产账号的授权依赖代码/默认角色路径判定，详见《RBAC收口交付报告》与《遗留问题清单》。

## 7. 只读备份（建议，非本阶段执行）

- 完整备份：`pg_dump -Fc opinion_db > opinion_db_<ts>.dump`
- 全局对象：`pg_dumpall --globals-only --no-role-passwords > globals.sql`
- 本阶段仅做只读统计，未生成或替换既有备份文件；归档包中的 dump 见 MANIFEST。

## 8. 结论

数据库满足冻结条件：实例身份明确、迁移追平、无运行任务、无孤儿外键。**READY_TO_FREEZE（数据库维度）**。
