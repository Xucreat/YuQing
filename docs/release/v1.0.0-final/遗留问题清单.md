# 舆情监测系统 v1.0.0 遗留问题清单（Known Issues）

> 冻结提交：`4cbf2fa578f42ef1e3eb44c3e4843a1a0653d712`
> 分级：P0（发布前必须处理）/ P1（应尽快）/ P2（后续迭代）

## P0

1. **工作区浏览器凭据缓存（tmp/）**
   - `tmp/` 含 Chrome `Login Data`、`Cookies`、`Local State`、`trusted_vault.pb` 等个人凭据缓存。
   - 已追加 `/tmp/` 至 `.gitignore`（未入库），但物理文件仍在工作区。
   - 处置：归档分发前删除该目录，并对相关账号执行凭据轮换（改密/吊销会话）。

2. **生产库残留测试账号**
   - `users` 表中存在 `mrUser_8f57ad7a03`、`mrUser_37b5aec7b0` 两个测试账号。
   - 处置：冻结后从生产库清理或重命名，避免测试身份混入生产权限面。

## P1

3. **user_roles 为空，账号→角色绑定未落表**
   - `user_roles = 0`，生产账号授权依赖代码/默认角色判定路径，RBAC 矩阵与实际账号绑定不完全透明。
   - 处置：明确绑定方式并补充落表数据，消除隐式授权。

4. **data_sources 全部未启用（enabled=0）**
   - 冻结时点 57 个数据源全部 `enabled=False`，运行期无活跃采集任务。
   - 处置：确认是否为预期冻结态；若需恢复采集，按启用流程开启并经审批。

5. **完整测试执行未在隔离测试库验证**
   - 本环境 `opinion_test@:5433` 未启动，仅完成 collection 与冒烟/构建验证；`test_rbac_hardening.py` 动态用例在缺测试库时 skip。
   - 处置：在具备 `:5433` 的 CI/隔离环境补全全量用例执行与 RBAC 动态回归。

## P2

6. **外网链路端到端未充分证明**
   - 外网事件数量为 0，外网事件型告警的真实浏览器端到端路径无法在当前数据上证明。
   - 处置：构造稳定 RSS/外网 fixture，在隔离环境验证自动 RSS→风险→事件→告警链路。

7. **JWT 改密后未服务端吊销**
   - 当前改密后前端清 token 强制重登，但已签发 JWT 在过期前仍有效。
   - 处置：引入服务端令牌吊销/版本号机制。

8. **健康检查测试契约滞后**
   - `tests/test_health.py` 仍断言旧响应形状，未包含 `collector_discovery` 字段。
   - 处置：更新测试契约以断言 `status==ok` 并允许 discovery 元数据。

## 说明

- 以上问题均不影响代码冻结基线本身的完整性与可构建性。
- P0 项须在 v1.0.0-final 归档包分发前处理完毕。
