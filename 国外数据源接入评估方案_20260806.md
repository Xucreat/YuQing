# 国外数据源接入实现评估方案（NYT / CNN / Fox News / WSJ）

> 评估日期：2026-08-06
> 目标：在不修改既有已实现功能（中文源采集、廊坊/全国视图、现有风险模型、既有看板逻辑）的前提下，新增 4 个国外新闻数据源。
> 约束前提：国外源正文为英文，服务器位于中国大陆，存在 GFW 出口限制。

---

## 一、结论摘要（可行性判断）

| 维度 | 结论 |
|------|------|
| 能否复用现有采集框架 | ✅ 能。RSSCollector / ChinanewsCollector 已验证 RSS 通路，registry 表驱动装配支持"插一行即上线"，无需改 registry / CollectorService。 |
| 是否零代码改动即可上线 | ❌ **不能**。两处硬约束强制必须有最小新增：`region_id NOT NULL` 必须有归属 Region；准入服务对英文无地域命中条目会拒绝。 |
| 是否违背"不修改已实现功能" | ⚠️ 可用**纯新增（additive）**方式满足：只新增采集器类、新数据源行、一个"国际"哨兵 Region、两个新增判定分支、可选多语言列。既有中文源行为逐字节不变。 |
| 最大风险 | 🔴 **网络出口（GFW）**：nytimes.com / wsj.com 在境内通常不可达；CNN / Fox 可达性不稳定。这是上线成败的第一因素。 |
| 第二大风险 | 🟠 **风险/情感模型对外文失效**：RiskEngine 与 RuleFallbackProvider 100% 基于中文词表，英文稿一律 `risk_score=20 / neutral / other`，当前阶段无区分度（可接受为已知限制）。 |

**总体判定**：技术可行，推荐采用"纯新增 + 国际哨兵区域隔离"方案，分 4 个阶段灰度推进。

---

## 二、现有架构与本方案的关系（已确认可复用点）

| 现有组件 | 文件 | 对外国源的作用 | 是否需要改动 |
|---------|------|--------------|------------|
| `BaseCollector` | `collectors/base.py` | 标准化 `fetch()→list[dict]` 契约，直接复用 | 否 |
| `RSSCollector` | `collectors/rss_collector.py` | 通用 RSS 解析（惰性 `feedparser`），但 `source_name` 恒为 `"rss"`、feeds 来自 env，不足以区分 4 源 | 否（仅作参考，不复用其类） |
| `ChinanewsCollector` | `collectors/chinanews_collector.py` | **最佳范本**：专属 RSS 采集器，独立 `source_name` + 关键词过滤 | 否（仿照新建） |
| `GenericSiteCollector` | `collectors/generic_site.py` | 列表→详情 HTML 抓取，可用于无 RSS 站点 | 否（备用，不优先） |
| `registry.resolve_collectors` | `collectors/registry.py` | `import_class(class_path)` 动态装配 + 读 `data_sources` 表 | 否（插行即生效） |
| `CollectorService` | `collectors/service.py` | fetch→去重→建 Opinion→规则分析 闭环 | 仅新增 2 行（透传 language/country） |
| `DataSourceConfig` | `collectors/source_config.py` | 读取 `collection_mode` 等策略；`COLLECTION_MODES={"regional","national"}` | 仅新增枚举值 `"international"` |
| `OpinionRegionService.decide` | `services/opinion_region_service.py` | 决定 `region_id` | 仅新增 `international` 分支 |
| `OpinionAdmissionService.evaluate` | `services/opinion_admission_service.py` | 决定条目是否准入 | 仅新增 `international` 自动准入分支 |
| `Opinion` 模型 | `models/opinion.py` | `region_id NOT NULL`；无 language/country 列 | 新增可选列（迁移，历史行 NULL 不影响） |

---

## 三、四大国外源逐源接入可行性

| 数据源 | RSS 入口（示例，需实测） | GFW 可达性 | 接入难度 | 备注 |
|--------|------------------------|-----------|---------|------|
| **CNN** | `http://rss.cnn.com/rss/cnn_topstories.rss`、`cnn_world.rss`、`cnn_us.rss`、`cnn_business.rss` | 🟡 多数时段可达，偶发不稳定 | ★☆☆ 低 | 公开 RSS，无需鉴权，最优先验证 |
| **Fox News** | `https://moxie.foxnews.com/google-publisher/world.xml`、`/us.xml`、`/politics.xml` | 🟡 同上 | ★☆☆ 低 | 公开 RSS，无需鉴权 |
| **NYT（纽约时报）** | `https://rss.nytimes.com/services/xml/rss/nyt/World.xml`、`/HomePage.xml`、`/Business.xml`；另有官方 Developer API（需免费 key） | 🔴 境内通常**不可达** | ★★☆ 中 | RSS 域名也可能被阻断；建议走出口代理或 NYT API |
| **WSJ（华尔街日报）** | `https://feeds.a.dj.com/rss/RSSWorldNews.xml`、`RSSMarkets.xml`；官方为付费 API | 🔴 订阅墙 + 境内不可达 | ★★★ 高 | RSS 常要求鉴权/已废弃；最晚接入，建议走付费 API 或镜像 |

**建议接入顺序**：CNN / Fox（先验证通路与模型）→ NYT（配出口）→ WSJ（最后，需 API/代理）。

---

## 四、必须解决的 4 个结构性缺口

### 缺口 1：区域归属（最关键）
- `Opinion.region_id` 是 `NOT NULL` 外键，每条舆情必须有 Region。
- 现有 `OpinionRegionService.decide()` 仅识别中文地域别名（廊坊/各县），英文文本**零命中**。
- 若把国外源设 `collection_mode="national"`：会落入"全国"哨兵 Region（code `000000`，id=24），而看板/告警/地图**均无 region 过滤**（见既有记忆：National-Mode-4 下游污染问题未修）→ 国外稿污染"全国"视图。
- 若 scope 留空且不声明 national：走隐式 national → 准入直接 `rejected`（丢弃）。
- ✅ **结论**：必须新增一个独立的"**国际 / 海外**"哨兵 Region（新 code，如 `900000`），并通过新 `collection_mode="international"` 把国外稿导向它，**与"全国"哨兵物理隔离**，廊坊/全国视图零污染。

### 缺口 2：准入（Admission）
- `OpinionAdmissionService.evaluate()` 对非微博源：隐式 national 且无地域命中 → `rejected`；仅 `collection_mode=="national"` 才自动 `accepted`。
- ✅ **结论**：在准入服务新增 `collection_mode=="international"` 自动准入分支（与 national 平行，一行），保证国外稿不被误杀。

### 缺口 3：风险 / 情感对外文失效
- `RiskEngine`（`risk_engine.py`）与 `RuleFallbackProvider`（`services/ai/fallback.py`）完全依赖中文危害词 / 中文情感词表。英文稿：`severity=0 → final≈20`、`sentiment=neutral`、`risk_category=other`。
- ✅ **结论（本期可接受）**：国外稿统一 `risk_score=20 / neutral / other`，无风险区分度。若需有效风控，需二期引入英文 NLP（见阶段 3），**不阻塞一期接入**。

### 缺口 4：网络出口（GFW）
- nytimes.com / wsj.com 境内通常阻断；CNN/Fox 可达性波动。
- 现有 `http_get`/`make_session` 无代理支持。
- ✅ **结论**：必须提供出口代理（HTTP/HTTPS_PROXY 环境变量或独立境外采集节点）。建议在采集层做 env 驱动的代理注入（新增，不改现有请求逻辑语义）。

---

## 五、推荐实施方案（纯新增，分阶段灰度）

### 阶段 0：网络出口（前置，阻塞项）
- 确定出口方案：① 服务器配置 `HTTPS_PROXY` 走境外代理；或 ② 在境外节点部署轻量采集 agent，回传标准化 JSON。
- 验证：从目标服务器 `curl` 各 RSS 地址，确认可达与延迟。
- 改动：仅运维/环境变量层面，无代码侵入（或新增 env 读取，可选）。

### 阶段 1：采集层（零侵入既有功能）
- **新增文件** `backend/app/collectors/foreign_rss.py`：
  - `class ForeignRSSCollector(BaseCollector)`：构造时从 `config_json` 读 `feeds`（list）、`source_name`、`language`、`country`（参考 `GenericSiteCollector` 的 `cfg.update(kwargs)` 吸收多余键，避免 registry 透传 `collection_mode` 等键导致 `TypeError`）。
  - 复用 `common.parse_rss` / `feedparser` 惰性解析；每条产出 `{title, content, source, url, publish_time, language, country}`。
  - 可选按 `topic_keywords`（英文主题词）前置过滤。
  - 仿照 `ChinanewsCollector` 写 4 个薄子类（`NYTCollector` / `CNNCollector` / `FoxNewsCollector` / `WSJCollector`），各设独立 `source_name` 与 `feeds`。
- **新增 `data_sources` 表行**（4 条）：`class_path=app.collectors.foreign_rss.XXXCollector`、`type=rss`、`enabled=False`（默认关，验证后开）、`config_json` 含 `feeds` + `collection_scope:"international"` + `language:"en"` + `country:"US"`。
- **registry 无需改动**：动态 `import_class` 自动装配。
- ✅ 此阶段对既有中文源、CollectorService、看板**零影响**。

### 阶段 2：区域隔离与准入（最小新增分支）
- **迁移**：seed 一个"国际/海外"哨兵 Region 行（code=`900000`，name=`国际`），并在 `Opinion` 表新增**可空**列 `language`（varchar）、`country`（varchar）——历史行 NULL，行为不变。
- **`source_config.py`**：`COLLECTION_MODES` 增加 `"international"`（仅扩展枚举，不影响 regional/national 既有路径）。
- **`opinion_region_service.py`**：`decide()` 新增分支——`collection_mode=="international"` → `resolve_international_region(db)` 返回国际哨兵 Region，`accepted_international_sentinel`。
- **`opinion_admission_service.py`**：`evaluate()` 新增 `collection_mode=="international"` → `accepted=True, relevance_score=100`（与 national 平行）。
- **`collector/service.py`**：`Opinion(...)` 构造新增 `language=item.get("language"), country=item.get("country")`（两行，可空；既有采集器不传→NULL）。
- ✅ 既有中文源：仍走 regional/national 旧分支，**逐字节不变**；廊坊/全国视图不被污染（国外稿落在独立"国际"Region）。

### 阶段 3（可选，二期）：多语言风险 / 情感
- 英文情感：在手动"触发 AI 分析"链路（DeepSeek）使用英文 prompt；或接入多语言情感模型。
- 英文风险词表：在 `keywords` 表新增 `type='severity'` 英文危害词，经 `get_severity_keywords` 注入 `RiskEngine`（现有注入机制已支持，无需改 RiskEngine 结构）。
- 本期为增强项，不阻塞阶段 1/2 上线。

### 阶段 4（可选，前端）：国际舆情视图
- 前端**新增**"国际舆情"页签，按 `region_id=国际哨兵` 或 `language='en'` 过滤。
- 为纯新增路由/组件，不改动既有廊坊看板。

---

## 六、"不修改已实现功能"符合性说明

| 既有功能 | 是否改动 | 说明 |
|---------|---------|------|
| 中文源采集（人民网/长城网/中国新闻网/微博等） | ❌ 不动 | 既有 `data_sources` 行、采集器类、registry 默认源全部不变 |
| 廊坊/全国视图、看板、地图、告警 | ❌ 不动 | 国外稿落在独立"国际"Region，不进入既有查询口径 |
| 现有风险模型（中文） | ❌ 不动 | 仅二期可选注入英文词，不修改公式 |
| `registry` / `CollectorService` 主流程 | ❌ 不动主逻辑 | 仅 `Opinion()` 构造透传 2 个可空字段 |
| 准入 / 区域决策 | ⚠️ 仅新增分支 | 既有 regional/national 分支行为不变，新增 international 分支 |

**关于"严格零代码改动"的说明**：由于 `region_id NOT NULL` 与准入对无地域命中英文稿的拒绝逻辑，任何国外源要落库都**必须**有区域归属与准入放行。这无法在不触碰代码的情况下完成。本方案以"纯新增、既有行为不变"满足约束的精神；若坚持一字不改，则唯一可行结果是"国外源抓取后被准入/区域判定丢弃，仍不落库"——等同未接入。请确认采用"纯新增"口径。

---

## 七、风险矩阵

| 风险 | 影响 | 概率 | 缓解 |
|------|------|------|------|
| GFW 阻断 NYT/WSJ | 高（两源不可用） | 高 | 出口代理 / 境外采集节点 / 官方 API（NYT 免费 key、WSJ 付费） |
| RSS 结构变动 / 限流 | 中（某源断流） | 中 | 监控 CollectorRun(status)；feeds 配置化便于切换镜像 |
| 英文风险分失真 | 中（无区分度） | 必现 | 一期标注 known limitation；二期英文 NLP |
| 误入"全国"视图污染 | 高（破坏既有视图） | 中（若误用 national 模式） | 强制用独立 international 哨兵，禁止 national 模式接国外源 |
| 代理泄露/合规 | 中 | 低 | 代理仅限采集出口，不回源境内；审计日志 |
| 英文进中文热词/告警 | 低 | 低 | 现有告警规则为中文词，英文不命中；international Region 与告警解耦 |

---

## 八、验证与灰度建议

1. **单源冒烟**：先开 `CNNCollector`（`enabled=True`），手动 `POST /api/collector/run` 触发，查 `opinion` 表 `region_id` 是否=国际哨兵、`language='en'`、`source='CNN'`。
2. **准入/区域断言**：确认 `CollectorRun` 非 `rejected`，无 `admission_filtered` 暴涨。
3. **视图隔离断言**：廊坊 dashboard 总数、全国视图条数在接入前后**不变**（国外稿不计入）。
4. **逐源放开**：CNN→Fox→NYT（配代理）→WSJ（配 API），每源观察 24h CollectorRun 稳定性。
5. **一键熔断**：任意源异常 → `data_sources.enabled=False` 即时下线，零代码回滚。

---

## 九、工作量估算（纯评估，未实施）

| 阶段 | 内容 | 估时 |
|------|------|------|
| 0 | 出口代理/可达性验证 | 0.5–1 d（依赖运维资源） |
| 1 | `foreign_rss.py` + 4 子类 + 4 数据源行 | 1–1.5 d |
| 2 | 迁移 + Region 种子 + 2 个分支 + service 2 行 | 0.5–1 d |
| 3 | 英文 NLP（可选） | 2–3 d |
| 4 | 前端国际视图（可选） | 1–2 d |
| **合计（一期 0–2）** | | **2–3.5 d** |

---

## 十、需要你确认的关键决策

1. **口径确认**：是否接受"纯新增分支 + 国际哨兵区域"作为"不修改已实现功能"的实现方式？（严格零代码改动无法落库）
2. **出口方案**：GFW 处理用代理 env 还是境外采集节点？NYT/WSJ 是否申请官方 API key？
3. **WSJ 优先级**：WSJ RSS 受限，是否一期就接，还是先接 CNN/Fox/NYT 三源？
4. **是否二期做英文风险/情感**：本期接受"风险分统一 20/neutral"的已知限制，还是一并规划英文 NLP？

> 确认后我可按阶段 0→2 直接落地实现（纯新增，现有中文源零影响）。
