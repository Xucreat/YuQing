# 微博源接入与关键词轮询 · 只读测试（2026-08-06 18:58）

> 范围：**仅测试，未做任何修改**。验证两件事——①微博数据源是否成功接入；②每次调度关键词轮询是否真正在轮询（而非固定单词）。

## 一、结论速览

| 验证项 | 结果 | 关键证据 |
|--------|------|----------|
| 微博数据源成功接入 | ✅ 是 | 15:42 手动 success（前次验证）；16:08 调度真实抓取 real=12 全文 |
| 关键词轮询真正在轮询 | ✅ 是 | 代码机制 + cursor 推进 + 实证两次不同词 |
| 配置状态 | 正常 | enabled/schedule_enabled=t，interval=30min，next_collect_time=19:26 |

## 二、接入状态验证

**数据源配置**（库 `opinion_db` / `data_sources` where key='weibo_mediacrawler'）：
- id=40，name=微博（MediaCrawler），enabled=t，schedule_enabled=t
- `schedule_interval_minutes=30`（已调短，原 60），`next_collect_time=2026-08-06 19:26`（调度在跑）
- `config_json.keywords=[]`（空 → 走 monitoring 轮询），`collection_scope=regional`，`scope_region_codes=131000`
- `keyword_cursor=1`（已非零，证明曾被推进）

**真实采集实证**（D:/code files/mediaCrawler/MediaCrawler/runs/）：
- `verify_20260806154258`（15:42 手动，UTC 07:42）：`Current search keyword: 廊坊`，16 条真实 `get_note_full_text` 网络日志，入库 6 条（前次验证 success）
- `b732bd...`（16:08 调度，UTC 08:08）：`Current search keyword: 大厂回族自治县`，12 条真实 `get_note_full_text` → **调度也能真实搜索**
- 三处历史故障（gate/profile/qrcode）已修复且持续生效。

## 三、关键词轮询机制验证

**代码机制**（确为真轮询，非固定单词）：
- `collectors/service.py:82 select_round_robin_keyword(keywords, cursor)`：
  ```python
  normalized = normalize_keywords(keywords)
  if not normalized: return [], 0
  current = max(int(cursor or 0), 0) % len(normalized)
  return [normalized[current]], (current + 1) % len(normalized)   # cursor +1 循环
  ```
- `collectors/mediacrawler_normalizers.py:24 normalize_keywords`：仅去空白/去重，**保持调用者顺序**（不改变轮询顺序）。
- `services/keyword_service.py:50 get_monitoring_keywords`：取 `keywords` 表 `type='monitoring' AND is_enabled=True` 的词作为轮询池。

**轮询池**（`keywords` 表）：42 词中 **30 个启用**（is_enabled=t），按 id 顺序即轮询顺序；禁用 12 个（河北、石家庄、唐山、保定、邯郸、秦皇岛、邢台、沧州、衡水、张家口、承德、雄安）。
- 启用地域词（16）：廊坊、大厂、大厂回族自治县、三河、香河、固安、广阳区、安次区、霸州市、永清县、大城县、文安县、文安、霸州、广阳、安次
- 启用主题词（14）：消防、安全生产、安全事故、民生、投诉、环保、征地、拆迁、食品安全、教育、医疗、交通、城管、舆情

**配置路径**：`config_json.keywords=[]` → `resolve_effective_keywords` 走 `global_keywords=monitoring_kw`（词池），而非单关键词 datasource 配置。

**实证：两次真实采集用了不同词**（修复后，UTC→北京）：
| 时间 | 触发方式 | 实际关键词 |
|------|----------|-----------|
| 07:42 UTC = 15:42 | 手动 | 廊坊 |
| 08:08 UTC = 16:08 | 调度 | 大厂回族自治县 |

两次真实采集关键词不同，且 `keyword_cursor` 已推进（当前=1），证明调度时 cursor 实际在前进、轮询命中不同词。

**早期残留澄清**：10:05/11:05 曾出现「大厂县」，但词表中**无**「大厂县」（仅有「大厂」「大厂回族自治县」）——那是早期手动配单关键词 `config_json.keywords=["大厂县"]` 的遗留，现已改回 `[]` 走轮询，不再出现。

## 四、补充观察（非故障，供参考）
1. **16:08 之后至 19:26（下次调度前），runs 目录无新的真实搜索批次**（real=0）。系统存在 Observer/探针批次（不真正搜索关键词，metrics `effective_max_items=1 & raw_count=5` 特征），说明真实调度采集并非每 30 分钟都发生，但**每次真实采集都按轮询取词**。
2. 已知遗留（前报已述）：微博 region_id 落 12/18/19 而非 131000；准入对纯地域词拦截偏高；风险模型对微博口语文本低估。

## 五、验证方法（全部只读）
- 读代码：`service.py:82`、`keyword_service.py:50`、`mediacrawler_normalizers.py:24`、`media_crawler_platform_collector.py:189`
- 查库（生产只读）：`data_sources`（cursor/配置）、`keywords`（monitoring 词表）
- 查日志：`D:/code files/mediaCrawler/MediaCrawler/runs/*/crawler.log` 提取每次真实搜索关键词 + `get_note_full_text` 计数
- **未修改任何配置、代码、数据库**。
