# 数据源连续空抓 — 修复实施报告（2026-08-06）

针对诊断报告中的 ① 对齐 collector_name、② 修文安 frameset/选择器、④ 查河北日报调度频率，
本轮回实施 ①+②（已写入生产库 `opinion_db`），④ 为核查结论（未改，待确认）。

> 生产库写入前已通过 `scripts/db_identity_check.py` → **VERIFIED**（exit 0，opinions=1184），
> 并对 `data_sources`(id21/32/35) 做了 pg_dump 备份。

---

## ① 对齐 id21 / id32 的 collector_name（已提交）

**根因**：`collector_runs.collector_name` 来自 `collector.source_name`，而 registry 装配时
`source_name` 取 `config_json.source_name`。此前 id21/id32 的 `config_json.source_name` 被写成
子栏目名（`廊坊市政府网-区县动态` / `霸州市政府网-霸州动态`），导致真实采集写在带后缀的名字下，
而前端按 `data_sources.name`（无后缀）关联 → 显示"未运行/空"。这与既有
`name 必须 = config_json.source_name` 缺陷同源。

**修复**：
| id | name | config_json.source_name 修改 | 历史归一化 |
|----|------|------------------------------|-----------|
| 21 | 廊坊市政府网 | `廊坊市政府网-区县动态` → `廊坊市政府网` | collector_runs 489→615，opinions 10→41 |
| 32 | 霸州市政府网 | `霸州市政府网-霸州动态` → `霸州市政府网` | collector_runs 344→616，opinions 3→16 |

归一化后孤儿名（`廊坊市政府网-区县动态` / `霸州市政府网-霸州动态`）计数归 0，
id21/id32 的采集历史与舆情按源筛选均连续可见。scheduler 每个 tick 重新 `resolve_collectors`，
**无需重启后端**即可生效。

---

## ② 修文安县政府网（id35）frameset / 选择器（已提交）

**根因**：根域 `wenan.gov.cn/` 是 frameset 壳页（`<frameset><frame src=/GOV1/Index.html>`），
旧 `list_urls` 已正确指向内容帧 `/GOV1/Index.html`；但 config 缺 `link_rule` 与 `content_selectors`，
导致列表页把**栏目链接**（`/GOV1/Category_XX/Index.aspx`）当文章抓取、详情正文提取不稳定，
间歇性 `fetched_raw=0`。

**修复**（实测验证）：
```json
{
  "source_name": "文安县政府网",
  "list_urls": [
    "https://www.wenan.gov.cn/GOV1/Category_109/Index.aspx",
    "https://www.wenan.gov.cn/GOV1/Index.html"
  ],
  "link_rule": { "href_contains": "/GOV1/Item/", "href_exclude": ["Index.aspx"], "min_title_len": 6, "max_links": 20 },
  "content_selectors": ["#content", "div.conTxt", "div.printArea"],
  "max_articles": 10,
  "keywords": "文安,廊坊,河北,县"
}
```
`GenericSiteCollector.test_fetch()` 实测：**ok=true，12 条文章链接，正文 2566 字**。
列表固定到「今日文安」栏目 + 首页，只抓 `/GOV1/Item/` 文章链接，正文容器明确 → 消除抓栏目页当详情的空抓。

---

## ④ 河北日报调度频率 — 核查结论（未改，待确认）

**调度频率正常，不是 bug。**
- 当前为 30 分钟节奏：08-06 当天均匀运行 26 次（间隔 ~31 分钟），与其余源一致。
- 诊断报告里"13 天仅 55 次"的假象：该源 `created_at=2026-07-21` 但 `updated_at=2026-08-05 10:27`——
  即 08-05 10:27 才被启用/修正配置，此前根本没有被调度。启用后频率即正常。

**真正的"连续空抓"根因 = 过滤策略，不是频率、也不是代码崩溃：**
- 河北日报是**省级党报**（`scope_region_codes=130000`），生产 `region_kw` 仅含**廊坊地市词**
  （廊坊/霸州/三河/香河/固安/文安…）。
- 用生产关键词复现 `HebeiDailyCollector.fetch()` → **返回 0 条**：省级稿件几乎不点名具体廊坊市县，
  被 `matches_region_topic` 全部过滤。
- 但库内 59 次运行里 **45 次其实 `fetched_raw>0`**（末次 08-06 06:06），仅 14 次为 0；
  全期 `created` 仅 08-05 的 1 条。说明这是"省级源被廊坊关键词过滤 → 低产"的**预期表现**，
  采集器本身可正常取数（直接用 `region_kw=["廊坊","河北","文安","县"]` 测可得 7 条）。

**若要让河北日报真正贡献舆情，需定 scope 策略（架构决策，已暂停未改）：**
- 方案 A：将该源视为省级/全国源（参照人民网/新华网，置 `scope_region_codes` 为空 → 走 national 模式，
  用扁平 `monitoring_kw` 过滤，绑定到全国哨兵 region 24）；
- 方案 B：给该源的地域词补充「河北 / 河北省」，使省级稿件通过地域过滤但仍绑河北 region。
> 两种都会改变"按区域"的入库归属与下游看板口径，需你确认采用哪种再实施。

---

## 待你确认的后续
- ④ 河北日报是否采用方案 A / B（或保持现状"仅抓廊坊提及的省级稿"）？
- ③（诊断报告里的）永清/固安"零新稿偏长"属人工核实项，非代码缺陷，未在此轮处理。
