# Phase 6 阶段六：凭据门禁报告

生成时间：2026-08-19 16:40

## 结论先行

代码/脚本/测试输出**未泄露凭据值**（仅键名+路径）。新增灰度脚本凭据门禁：未轮换凭据（`.env.bak` 仍存在）时真实灰度脚本拒绝运行，需显式 `--ack-credentials-rotated` 绕过。4 个安全扫描测试全绿。

## 一、人工轮换清单（仅键名，不显示值）

文件：`.env.bak_20260806_152704`（未跟踪）

| 键名 | 动作 |
|------|------|
| `SECRET_KEY` | 轮换 |
| `INIT_ADMIN_PASSWORD` | **立即轮换** |
| `BAZHU_PASSWORD` | 轮换 |

## 二、灰度脚本凭据门禁（新增）

`_phase2_gray_run.py` 现在：
1. 检测 `.env.bak_20260806_152704` 是否存在 → 存在则拒绝运行（提示先轮换凭据）；
2. 显式 `--ack-credentials-rotated` 才能绕过；
3. 仍要求 `YQ_ADMIN_PASSWORD` 环境变量（拒绝空/硬编码）。

## 三、泄露检查

| 项 | 结果 |
|----|------|
| 代码/脚本明文凭据 | ✅ 无 |
| 报告/测试输出泄露凭据值 | ✅ 无（全程 REDACTED） |
| `.ps1` / 配置文件 | ✅ 无明文 |
| 备份文件是否移动/删除 | ✅ 未动 |

## 四、测试

`tests/test_phase5_security_scan.py`（4 用例）：
1. 硬编码凭据扫描（scripts/*.py）
2. 灰度脚本环境变量化 + 不含旧密码
3. 裸 PASSWORD 赋值扫描
4. **灰度脚本凭据门禁**（.env.bak 检测 + --ack-credentials-rotated + 环境变量）

## 五、结论

凭据未轮换，真实灰度被门禁拦截（fail-closed）。在用户人工轮换并显式确认前，不执行真实灰度。
