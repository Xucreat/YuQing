# 历史舆情数据清理报告

**执行时间**：2026-07-24 23:28（确认后执行）
**执行阶段**：阶段二（业务数据清理）
**数据库身份门禁**：`db_identity_check.py` 退出码 **0 / VERIFIED**（system_identifier=7663057120701798896，alembic head=p12_rbac_roleperms，opinions≥100）—— 确认为生产库 `opinion_db @ 127.0.0.1:5432`，未操作任何未知库。

## 一、清理前数量快照（已存档 `清理前数量快照.json`）

| 表 | 清理前 | 清理后 | 是否清理 |
|---|---:|---:|---|
| propagation_nodes | 662 | **0** | ✅ 清空 |
| alert_records | 80 | **0** | ✅ 清空 |
| event_opinions | 441 | **0** | ✅ 清空 |
| events | 212 | **0** | ✅ 清空 |
| opinions | 1088 | **0** | ✅ 清空 |
| collector_runs | 4507 | **0** | ✅ 清空（作为廊坊新周期起点） |

## 二、清理顺序与依据（外键校验）

经 `information_schema` 确认外键关系：
- `alert_records` → `events` / `opinions`（及保留的 `alert_rules`/`users`）
- `event_opinions` → `events` / `opinions`
- `propagation_nodes` → 自引用 `parent_id` + `opinions` / `events`
- `opinions` → 保留的 `regions`
- `events`、`collector_runs` 无指向目标表的依赖

据此按 **子表→主表** 顺序执行 DELETE（单事务逐表提交）：
`propagation_nodes → alert_records → event_opinions → events → opinions → collector_runs`

自引用表 `propagation_nodes` 以单条 `DELETE` 整表清空，PostgreSQL 在语句结束时统一校验约束，无残留引用，执行成功（影响 662 行）。

## 三、保留表复核（禁止清理项，数量无变化 ✅）

| 表 | 清理前 | 清理后 |
|---|---:|---:|
| users | 3 | 3 |
| roles | 3 | 3 |
| permissions | 26 | 26 |
| role_permissions | 19 | 19 |
| user_roles | 0 | 0 |
| alert_rules | 2 | 2 |
| data_sources | 30 | 30 |
| keywords | 46 | 46 |
| regions | 17 | 17 |
| user_operation_logs（审计） | 24 | 24 |

> 注：`login_logs` 表在本库不存在（非业务表，未受影响）。

## 四、结论

- 全部目标业务表已清空且验证为 0；
- 系统配置、RBAC、采集器配置、关键词、区域字典、审计日志均完整保留；
- 无级联误删，无孤儿记录，外键约束全程未触发冲突。

历史河北全量舆情业务数据清理完成，可作为廊坊市全域采集的新周期起点。
