# Phase 4B 历史 incoming 治理计划

生成时间：2026-08-19 15:20
原则：**第一阶段只审计不处理；处理仅在文件↔CollectorRun↔manifest↔ack_pending 关系明确时才调用幂等逻辑，且每次保留审计记录、支持回滚。**

---

## 一、当前状态（已审计，未处理）

- 287 个 incoming 全部为历史失败/超时/早期测试产物，**无活跃任务残留**。
- 不删除、不移动、不覆盖、不自动恢复任何文件。

## 二、分类处理方案

### 类 1：成功任务 ack 残留 —— 无（已归零）
无需处理。

### 类 2：失败任务数据（256 个）—— 待用户决策，默认不自动处理

| manifest | run | 状态 | 建议 |
|----------|-----|------|------|
| d5caf173（128） | #21248 timeout | baidu 42 全成功、其余完整 | 数据完整但 run=failed；**不建议 ack**（ack 会伪造成功）。可保留，或经用户确认后按「历史数据补录」单独评估 |
| 0b41b983（112） | #21118 zombie | baidu 26/42、其余完整 | 同上，且 16 baidu 缺数据 |
| 0b637f17（8） | #21045 timeout | baidu 全失败 | 同上 |
| 6a6c7f2e（8） | #21126 zombie | baidu 全失败 | 同上 |

**推荐动作**：保留不动。这些文件的 manifest 已在 rejected/archive，不会影响后续新采集（新任务用新 manifest_id，不会误消费）。

### 类 3：孤立文件（31 个）—— 建议人工确认后归档标记
- `manifest-3154428464654`（20 baidu）、`landing-8platform`（11）均为 08/17 早期测试残留，无 CollectorRun、无 manifest。
- **推荐动作**：不删除。可在用户确认后移动到 `quarantine/`（新增隔离目录）并生成移动清单，保留 SHA256 备查。

### 类 4：manifest 缺失 —— 已并入类 3
31 个孤立文件即 manifest 缺失，处理同类 3。

### 类 5：weibo 文件（1 个）—— 禁止自动处理
`landing-8platform` 下的 weibo 文件属禁用链路产物，**保留待人工**，不做任何自动动作。

## 三、若需处理，遵循的幂等/回滚原则

1. 任何 ack 动作仅对「status=success 且 ack_status=success 的 CollectorRun」调用 `ack_pending_export`，绝不强行 ack failed run。
2. 任何归档/移动动作先写审计 JSON（记录源路径、目标路径、SHA256、时间、理由），再执行，支持按记录回滚。
3. 每批处理 ≤ 10 个文件，逐批验证。
4. 不调用批量删除，不覆盖同名文件。

## 四、待用户确认项

1. 类 2 的 256 个失败任务产物：**保留（推荐）** 还是需补录入库？
2. 类 3 的 31 个孤立文件：是否允许移动到 `quarantine/` 并生成清单？
3. 类 5 的 1 个 weibo 文件：保留待人工，是否已确认？

## 五、本阶段结论

Phase 4B **审计阶段已完成**：287 文件完整清单（SHA256 全量）+ 分组 + 对账 + 建议均已产出。**未执行任何删除/移动/ack/恢复动作**，等待用户对上述三项确认后再进入处理阶段。
