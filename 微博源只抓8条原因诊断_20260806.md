# 微博（MediaCrawler）源"只抓到 8 条"原因诊断 — 2026-08-06

## 结论速览

微博源**抓取本身是正常的**（每轮确实向 MediaCrawler 要到 20 条原始帖）。"只抓到 8 条"是
**准入过滤 + 历史重复 + 单关键词轮询覆盖低**三者叠加的结果，不是采集故障：

| 环节 | 数值（19 轮累计） | 占比 |
|---|---|---|
| 原始抓取 fetched_raw | 100 | 100% |
| 准入拒绝 admission_filtered | 48 | **48%** |
| 历史重复 duplicate | 44 | **44%** |
| 净新增 created | **8** | 8% |

库内 `source='weibo'` 舆情共 110 条：其中 **102 条是旧 `微博` 源在 07-29 一次性入库的**，
新 `微博（MediaCrawler）` 源自 08-05 起仅新增 8 条（08-05:6 + 08-06:2），与 `created=8` 完全吻合。

---

## 一、抓取没坏：每轮确实拿到 20 条

- 运行是**真实执行**（非 fixture 回放）：`crawler.log` 含真实
  `WeiboCrawler.get_note_full_text` 网络调用，并产出 `search_contents_2026-08-06.jsonl`。
- 样例批次 `7e5bb42e…`（08-06 11:05）：`raw_count=26 → output_count=20`，fetch 健康。
- 注意 `max_items=20` 是**单个关键词**的上限，不是"每轮总产出"。
- 个别轮次只回 10 条（如 `f05aaf…`），是 MediaCrawler 对该关键词实际返回偏少，非 bug。

---

## 二、主因 A：微博准入门槛过严（吃掉 ~48%）

`app/services/opinion_admission_service.py` 对 `weibo_post` 单独加严（注释：
"prevent keyword recall pollution"）：

- 评分起点 10；地域词 +25、具体地标 +15、公共事务 +15、诉求 +25、风险事件 +30。
- **关键规则**（line 212-218）：
  ```python
  has_public_signal = bool(place_hits or public_hits or demand_hits or risk_hits)
  if region_hits and not has_public_signal:
      score = min(score, 35)          # 纯地域提及封顶 35
  accepted = final_score >= 40        # 35 < 40 → 拒绝
  ```
- 即：**一条微博必须同时命中"地域词"和"公共事务/诉求/风险/具体地标信号"才准入**；
  仅提及"大厂县"的旅游/风景/生活帖（无公共信号）会被判 35 直接拒绝。
- 实测 "大厂县" 批次 20 条里 **12 条因"仅有地域词、无公共信号"被拒**（admission_filtered=12）。

---

## 三、主因 B：轮询每轮只搜 1 个关键词（覆盖极低）

- 调度走 `select_round_robin_keyword`（`service.py:319`），每次从监控词表（~50 词）取 **1 个**
  关键词去搜；若 `data_sources.config_json.keywords` 非空，则整组作为**一次搜索**（逗号拼成一句）。
- `max_items=20` 是"单关键词"上限 → 19 轮仅覆盖 19 个关键词；60 分钟/轮，轮完 50 词需 ~50 小时，
  且轮换让同一词反复产出已抓过的旧帖。
- 证据：11:05 批次 `crawler.json` 显示 `keywords=["大厂县"]`、`effective_keywords_source=datasource`
  —— 当时只配了**单个关键词**，于是每轮都搜"大厂县"同一批稳定结果，故 `duplicate` 稳定为 6~8。
  （当前库内 `config_json.keywords=[]`，已改回走 monitoring_kw 轮询；用户"换关键词"应在此区间发生。）

---

## 四、主因 C：无时间窗 + 相关性排序 → 高重复（吃掉 ~44%）

- MediaCrawler 微博搜索**按相关性排序、无时间窗**，每次返回同一批顶部帖；首次抓取后，
  后续轮次这些帖全部变为 `duplicate`（每轮稳定 6~8 条重复）。
- **更关键**：旧 `微博` 源（07-29）已**一次性入库 102 条**廊坊相关微博，新 MediaCrawler 源自 08-05
  起搜到的稳定帖绝大多数已存在于库中 → 大量 duplicate。这才是 44 条重复的主体。
- 证据：`opinions WHERE source='weibo'` 共 110 条，102 条 created_at=07-29（旧源），新源仅增 8 条。

---

## 五、为什么"换关键词轮询"反而没多抓

用户的预期："换了关键词 + 轮询"应放量。实际：

1. 轮询是 **1 关键词/轮**，不是"20 × 关键词数"；
2. 即便每轮 20 条，**准入过严先砍掉约一半**（纯地域帖）；
3. 剩下的有效帖，因**旧源已抢占语料 + 无时间窗**，大多已是 duplicate；
4. 所以每轮净新增常为 0，仅在新关键词首次覆盖到尚未入库的帖时才偶得 1~2 条。

---

## 六、改进建议（架构/策略决策，本次未改代码）

| 方向 | 做法 | 风险/权衡 |
|---|---|---|
| 放宽微博准入 | 纯地域帖若带互动量（engagement bonus）或把准入线降至 35 | 与"防关键词召回污染"设计初衷冲突，需权衡 |
| 加时间窗 | 给 MediaCrawler 微博搜加"最近 N 天"过滤 | 减少重复、抓新帖，改动在 adapter/runtime 层 |
| 提升覆盖 | 每轮搜多个关键词 / 缩短轮询间隔 | 增加 MediaCrawler 调用与风控压力 |
| 正确配关键词 | 把 `config_json.keywords` 设为要监控的**多词列表**（整组一次搜索串），而非单关键词 | 注意整组是一次搜索，不是逐个轮询 |

请确认是否要调整上述任一项（尤其 A 的准入阈值与 C 的时间窗），我再动手改。
