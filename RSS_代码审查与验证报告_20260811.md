# 通用国内 RSS 数据源支持 — 代码 Review / 验证 / 本地构建报告

日期：2026-08-11 ｜ 范围：仅本次 RSS 改动（未触碰其他未提交改动）

## 一、Review 发现的问题（按严重程度排序）

| 等级 | 位置 | 问题 | 影响 | 修复 |
|---|---|---|---|---|
| **P0 阻断** | `admin_data_sources.py` `_build_test()` | RSS 分支在 `build_cfg` 变量赋值**之前**就引用它，必抛 `UnboundLocalError`，被外层 try 包装成「采集器构建失败」 | **新建 RSS 数据源一律 422 失败**（保存前真实抓取校验不通过），且「测试连接」永远失败 → 核心功能形同虚设 | 把 `build_cfg = {k: v ... if k not in STRATEGY_KEYS}` 提到 `foreign`/`rss` 分支之前 |
| **P1 SSRF 绕过** | `common.py` `http_get` / `rss_collector.py` | 原 `session.get(url)` 用 requests 默认 `allow_redirects=True`，且 curl 兜底带 `--location`。只校验了**初始** URL，302 跳转到 `127.0.0.1` / `169.254.169.254`（云元数据）**不被重新校验** | 一个“合法”RSS 地址可经重定向打到内网/元数据服务，绕过 SSRF | 新增 `http_get_guarded()`：`allow_redirects=False` + **对每一跳重新** `is_safe_rss_url` 校验；`_curl_get` 增加 `follow_redirects` 参数（默认 True，RSS 走 False）；`RSSCollector` 改用 `http_get_guarded` |
| **P2 数据错乱** | `rss_collector.py` + `Sources.vue` | `RSSCollector` 未接收 `source_name`，所有 RSS 源的 `collector_runs.collector_name` 与 `opinions.source` 都被写成类默认 `"rss"` | 列表中所有 RSS 源显示「未运行」（与 `ds.name` 关联不上），且不同 RSS 源运行记录互相串台 | `RSSCollector.__init__` 接收 `source_name` 覆盖类默认；前端 `rssConfigFromFeeds()` 在**新建/编辑**时写入 `source_name = 显示名` |
| 低 | `admin_data_sources.py` 校验 | `source_name` 等附加键未列入白名单，但当前 `_validate_rss_config` 仅校验 `feeds`，其余键放行——与既有「通用型」行为一致，可接受 | 无 | 未改（保持宽松，避免误伤） |

**结论**：上述 3 个均为真实 bug，已在本次会话内修复并补回归测试。其中 P0 若不修，本次“通用 RSS”功能在 UI 上完全不可用。

## 二、中国新闻网 / 其他采集器是否有回归

- **registry 解析 `row.class_path` 直接使用数据库行里的值**，不依赖 `type→class_path` 映射；`_TYPE_CLASS_PATH["rss"]` 的改写只影响**新建**走默认映射的源。
- `DEFAULT_SOURCES.chinanews` 带**显式** `class_path = ...ChinanewsCollector`，且本次**未改 `ChinanewsCollector` 一行**。
- 冒烟测试确认：`_is_rss(RSS_CLASS_PATH) == True`，`_is_rss("...ChinanewsCollector") == False` → 中国新闻网不会进入 RSS 分支。
- 单元断言：`chinanews` 仍解析为 `ChinanewsCollector`（测试 `test_chinanews_still_uses_chinanews_collector` 通过）。
- **未触碰** AI / 告警 / 风险评分 / Event 聚合 / 其他采集器逻辑。

## 三、执行的测试、构建命令及结果

后端（`.venv` Python 3.13）：
- `pytest backend/tests/test_rss_collector.py -q` → **19 passed**
  - 覆盖：feeds 读取（dict/字符串）、字段转换、多源部分失败隔离、三级去重、非法/空 URL 拒绝、SSRF 构造期+抓取期双层拦截、**重定向 SSRF（新增）**、`RSS_URLS` 回退、registry 装配、**source_name（新增）**、`_build_test` P0 回归（新增）、API 校验。
- `py_compile` 三个后端文件 → OK。
- 应用导入冒烟（`import app.api.admin_data_sources` 等）→ OK。
- 完整后端套件（两次：含本改动 / 仅基线）对比 → **本次改动零回归**（见下）。

前端（`frontend/`）：
- `vue-tsc --noEmit` 类型检查 → **`Sources.vue` 无类型错误**；仅有 `foreign/ForeignOpinionDetailModal.vue`、`ForeignWorkspace.vue` 的**既有** TS 错误（非本次改动、未触碰，`vite build` 不跑类型检查故不影响构建）。
- `node --max-old-space-size=1400 node_modules/vite/bin/vite.js build` → **✓ built in 16.45s（退出码 0）**。
- `python backend/_d.py` → **写入 46 个文件**到 `backend/app/static`；`index.html` 已指向新 bundle 且含 `RSS 地址` 文案。

## 四、失败测试归因（本次引入 / 既有 / 环境）

完整套件基线对比（将我的 3 个后端文件 `git stash` 回退、移走新测试后跑基线，再恢复）：
- 含本改动失败集合 = 64 项；基线失败集合 = 65 项。**几乎完全重合**。
- 仅“含本改动”中多出的 2 项：`test_ai_analysis.py::test_analyze_api_success`、`test_dashboard.py::test_dashboard_trend_window`。
- 隔离复跑确认：
  - `test_analyze_api_success` **孤立运行仍失败**（AssertionError）→ **既有问题**，与 RSS 无关（不 import 任何 RSS 代码）。
  - `test_dashboard_trend_window` **孤立运行通过** → 全量套件下的**顺序/状态抖动**，非回归。
- 另有两次全量运行因并发抢占同一测试库导致部分 ERROR 浮动，属**环境/并发**因素。
- **结论：本次改动引入的失败 = 0；仓库原有失败 = 绝大多数为未提交的外网/RBAC/调度等改动所致；环境失败 = 测试库并发抖动。**

## 五、SSRF / 重定向 / 配置校验结论

- **协议**：`is_safe_rss_url` 仅允许 `http/https`。
- **地址**：拒绝 `localhost`/`127.0.0.1`/`0.0.0.0`/`::1` 及所有私网/链路本地/保留/组播；构造期做静态拦截，抓取期再做 DNS 解析后的内网校验。
- **重定向（重点）**：`http_get_guarded` 手动跟随重定向，**每一跳**都重新做 `is_safe_rss_url` 校验；DNS 解析异常或 guard 自身异常均**安全返回 None，不崩溃**，杜绝 302 跳内网绕过。curl 兜底同步关闭 `--location`。
- **配置校验一致性**：`_validate_rss_config` 同时被 `_validate_create`（创建）、`_build_test`（测试连接）、PATCH 分支（编辑保存）调用，三处口径一致；缺 `feeds` 走 `RSS_URLS` 兼容，有 `feeds` 则逐个校验。

## 六、是否生成前端构建产物

**是**。`frontend/dist` 已生成，并已通过 `backend/_d.py` 部署到 `backend/app/static`（46 个文件，含全新 `index.html` 与 RSS 相关 bundle）。

## 七、是否启动了本地服务

**否。** 依据你的边界要求（不默认重启/不覆盖运行中服务/不动生产库），本次**未重启 uvicorn、未启动新验证服务**。理由：
- RSSCollector 实例化、读取 `config_json.feeds`、返回统一舆情字段、中国新闻网仍用原采集器，已由单元/导入测试充分覆盖；
- 你明确要求“不要对生产数据库创建测试数据”，而启动服务并真实创建 RSS 源会写入生产库，故停止于测试验证。

## 八、是否部署/重启

- **未做生产部署、未重启共享/生产服务、未删除任何旧构建产物**（static 中旧 bundle 仅被新文件覆盖，未 `rm`）。
- 若需要我**重启 uvicorn 使新前端立即生效**（或启动一个**非 8000 端口**的验证实例），请确认目标环境与命令，我再执行。

## 九、必须修复的问题

- 上述 P0/P1/P2 **均已修复并补回归测试**，已重新跑通（19/19）。无遗留必须修复项。
- 唯一“已知遗留”是仓库中既有的 `test_collector.py` 等失败与其他未提交改动相关，与本次 RSS 工作无关，按你要求未予处理。
