# 廊坊采集范围切换报告

**执行时间**：2026-07-24 23:28–23:35（用户确认后执行）
**执行阶段**：阶段二（业务数据清理，见《历史舆情数据清理报告》）+ 阶段三（采集范围切换）
**数据库身份门禁**：`db_identity_check.py` 退出码 **0 / VERIFIED**（opinion_db @ 127.0.0.1:5432，head=p12_rbac_roleperms）—— 全程仅操作真实生产库。

---

## 一、阶段二 · 历史业务数据清理（已完成）

- 清空：`propagation_nodes`(662) / `alert_records`(80) / `event_opinions`(441) / `events`(212) / `opinions`(1088) / `collector_runs`(4507)，按子表→主表顺序 DELETE，**全部归零并验证**。
- 保留（数量无变化）：users(3)/roles(3)/permissions(26)/role_permissions(19)/alert_rules(2)/data_sources(30)/keywords(46)/regions(17)/审计(24)。
- 详见 `历史舆情数据清理报告.md`。

## 二、阶段三 · 采集范围切换

### 2.1 区域范围
目标：河北省(130000) → **廊坊市全域**，含：
`131000 廊坊市` / `131022 固安县` / `131024 香河县` / `131028 大厂回族自治县` / `131082 三河市`。

### 2.2 关键词调整（DB：keywords 表）
- **禁用（12 个监测地域词）**：河北、石家庄、唐山、保定、邯郸、秦皇岛、邢台、沧州、衡水、张家口、承德、雄安。
- **保持启用**：廊坊、大厂、大厂回族自治县。
- **新增启用（3 个）**：三河、香河、固安。
- 结果：监测关键词启用数 17 → **20**（含主题词）。

### 2.3 数据源调整（DB：data_sources 表）
- **启用 11**：全国源 百度新闻/新华/人民网/中新网（scope=NULL）+ 廊坊全域 7 源
  （大厂县政府网站 131028、廊坊新闻网 131000、香河新闻 131024、廊坊市政府网 131000、三河市政府网 131082、香河县政府网 131024、固安县政府网 131022）。
- **停用 19**：河北省级 4（河北新闻网/河北日报/长城网/河北省人民政府，scope=130000）+ 其他地市 15（石家庄/唐山/邯郸/保定/张家口/衡水/雄安/承德/秦皇岛/邢台/沧州 及其政府网）。
- 决策说明：廊坊下辖 gov 源（三河/香河/固安/廊坊市政府网）原处停用，因属廊坊全域且用户新增了对应县关键词，一并启用以达成"廊坊全域"目标（可逆，如需收窄可再行停用）。

### 2.4 代码调整
- `backend/app/collectors/service.py:188`：全国源无 scope 时的区域回退 **130000(河北省) → 131000(廊坊市)**，注释同步更新（仅此一处，未改表结构/RBAC/认证/事件聚合/风险模型）。已 `py_compile` 校验通过。

### 2.5 配置调整（防止未来初始化恢复河北范围）
- `YQ/.env` `COLLECTOR_KEYWORDS`：`河北,大厂,…,廊坊,大厂回族` → `廊坊,大厂,大厂回族自治县,三河,香河,固安,…`。
- `backend/collectors/...` → 实为 `backend/app/core/config.py:66` `collector_keywords` 默认值：同步改为廊坊全域关键词；旁注更新。
- `backend/scripts/init_db.py` `MONITORING_KEYWORD_SEED`：地域锚点由"全省监测"改为"廊坊市全域监测"（移除河北/各地市，加入三河/香河/固安）。
- 均 `py_compile` 校验通过。

### 2.6 服务重启
- `taskkill /F /PID 72396` 强杀旧 uvicorn（端口 8000 已释放）；Bash 后台以新代码重启（新 PID 17052，端口监听正常，`/api/*` 返回 401 表明应用已加载且需鉴权）。

## 三、阶段三D · 小规模采集验证（真实单源）

直接复用 `CollectorService._process_collector` 对**百度新闻（全国源）**单源跑真实采集链路（不经 API、不需 admin 密码、不触发全部 11 源）：

| 校验项 | 结果 |
|---|---|
| 生效监测关键词 | 廊坊/大厂/大厂回族自治县/三河/香河/固安 + 主题词；**不含「河北」** ✅ |
| 全国源区域绑定 | 回退解析为 **廊坊市(code=131000, level=city, region_id=12)** ✅ |
| 真实采集 | fetched_raw=15 / created=15 / analyzed=15 / failed=0 ✅（外网可达） |
| 新数据区域分布 | **15 条全部 廊坊市(131000)**；河北其它地市 = **0** ✅ |

## 四、最终状态

| 表 | 行数 | 说明 |
|---|---:|---|
| opinions | 15 | 新周期首批，全部 廊坊市 |
| collector_runs | 1 | 新周期起点（本次验证触发） |
| propagation_nodes / alert_records / event_opinions / events | 0 | 已清空，待新数据累积 |
| data_sources | 11 启用 / 19 停用 | 廊坊全域+全国源 |

## 五、结论

河北省全量 → 廊坊市全域的切换已完成：历史业务数据清空、采集范围（关键词/数据源/代码回退码/初始化种子）全部收敛到廊坊，服务已用新配置重启，小规模真实采集验证确认新数据区域字段为**廊坊市(131000)**且不再采集河北其它城市。系统可作为廊坊市全域监测的新周期稳定运行。

> 备注：本次验证仅跑单源，未触发全量 11 源与后续事件自动聚合；后续定时(每 30 分钟)或手动全量采集将自然累积 events/alerts 等衍生数据。
