# 实施计划：预警风险分显示 / 批量研判预览 / 国内展示口径

> 用户确认直接采用，实施顺序严格为：**③ → ①A → ② → ①B**
> 约束：③ 纯前端、不动后端 display_risk；①A 长期展示兜底；② 保持"AI 只生成候选、不直接触发告警"边界；①B 一次性历史数据修复。
> 每个 Phase **单独验收、单独回滚**；下游 Phase 不依赖上游未验收的改动。

---

## 关键前置发现（修正此前诊断）

- **国内 AI 人工复核页面真实存在**：`frontend/src/views/Opinions.vue` 第 225–306 行内嵌 `<section v-else-if="scope==='domestic'" class="review-view">`，含逐条 `display_source`（`use_ai_display` / `keep_rule`）与"采用 AI 展示"下拉。此前"国内无复核页"的结论**有误**。
- **后端已具备"展示口径"数据通道**：`Opinion.current_risk_source`（默认 `rule`）+ `current_risk_score` + `current_risk_updated_at`；`_current_risk_score_expression()`（`domestic_ai_analysis.py:36-40`）在 `current_risk_updated_at IS NOT NULL` 时取 `current_risk_score` 否则取 `risk_score`；`decide_review` 在 `use_ai_display` 时把 `current_risk_source` 置 `ai` 并写入 AI 分，提示语明确"普通列表、驾驶舱及关联展示将读取 AI 风险"。
- **Q3 真因**：主列表 `Opinions.vue:196/199` 硬渲染 `row.risk_score`，未读取后端已维护好的 `current_risk_score`，故"采用 AI 展示"后列表仍显示规则分。
- 因此 Phase ③ 不再是"新增全局开关"，而是**前端把主列表风险列改为渲染 `current_risk_score`**——更贴合 Q3、且仍纯前端、不碰后端。

---

## Phase ③ 国内展示口径前端落地（纯前端）

**目标**：国内舆情主列表（"风险评分""级别"两列）按后端已维护的 `current_risk_score`/`current_risk_source` 展示；`use_ai_display` 后在列表即时显 AI 分。不动任何后端接口。

**改动文件**
1. `frontend/src/types/index.ts`：`Opinion` 接口补充 `current_risk_source?: 'rule' | 'ai'`、`current_risk_score?: number`（类型补全，不触运行时）。
2. `frontend/src/views/Opinions.vue`：
   - 新增本地函数 `displayRiskScore(row)`：`row.current_risk_source === 'ai' ? (row.current_risk_score ?? row.ai_risk_score ?? row.risk_score) : row.risk_score`。
   - 主列表"级别"列 `levelPill/levelText(row.risk_score)` → `displayRiskScore(row)`。
   - 主列表"风险评分"列 `riskColor(row.risk_score)/{{ row.risk_score }}` → `displayRiskScore(row)`；`current_risk_source==='ai'` 时附 `AI` 小标。
   - 追加 `.risk-src-tag` 样式（外网同款琥珀色标识）。

**验收标准**
- 取一条国内舆情，在"AI 人工复核"中对该条执行"采用 AI 展示"（`use_ai_display`）。
- 回到国内舆情主列表：该条"风险评分""级别"显示 AI 分（与复核页"AI 风险"列一致），且带 `AI` 标识。
- 未做复核 / "保留规则"的条目仍显示规则分，行为不变。
- 外网 `ForeignOpinionListView` 行为不受影响。

**回滚**：`git checkout frontend/src/types/index.ts frontend/src/views/Opinions.vue`（纯前端，无数据变更）。

**风险/备注**：极低。仅读取已返回字段；若后续还需要"列表级手动全局切换"（外网同款），可作为 Phase ③ 的增强项追加，不影响本阶段验收。

---

## Phase ①A 预警记录展示兜底（长期）

**目标**：`backend/app/api/alerts.py` 的 `_alert_record_payload` 中，当 `formal_risk_score` 为 `None` 时，用关联舆情当前 `risk_score`（`linked_opinion_current_risk`）兜底，杜绝列表出现 `- 分` 空白。**长期生效、不写库**。

**改动文件**：`backend/app/api/alerts.py`（`_alert_record_payload`）。

**验收标准**
- 历史缺 `rule_risk_snapshot.risk_score` 且缺 `ai_risk_snapshot.risk_score` 的 `AlertRecord`，在预警记录页显示兜底分（关联 `Opinion.risk_score`），不再 `- 分`。
- 新告警（快照含 `risk_score`）显示逻辑不变。
- 不修改任何持久化字段。

**回滚**：`git checkout backend/app/api/alerts.py`。

**风险**：低。`formal_risk_score` 仅在为 `None` 时兜底；不影响已正常数据。

---

## Phase ② 批量研判预览真实化（保边界）

**目标**：让"可能影响的预警"预览反映真实候选数量，同时**严格保持"AI 只生成候选、不直接触发告警"**边界（不改动 `ForeignAlertService.evaluate`）。

**改动文件**
1. `backend/app/api/domestic_ai_analysis.py:255`：`possible_alert_count: 0` 硬编码 → 调用新函数 `_preview_domestic_alert_count(db, opinion_ids)`，基于**已存在**的 completed `DomesticAIResult` 跨启用 `ai_risk_score` 规则的真实计数。
2. `backend/app/api/foreign.py:1296-1315`：新增独立 `preview_candidates` 统计"规则风险命中 + AI 分命中"候选数（与 `evaluate(dry_run)` 并列，不改动 `evaluate` 内部逻辑）；保留 `try/except` 兜底，异常时回退原 `evaluate` 结果或 0，避免 500。

**验收标准**
- 国内：所选舆情存在已完成 `DomesticAIResult` 时，预览"可能影响的预警"显示非零；无则 0（合理）。
- 外网：预览显示候选数；批量运行仍只生成候选、不直建正式 `ForeignAlert`（边界不变）。
- 任一预览计算异常时接口不 500（有兜底）。

**回滚**：`git checkout backend/app/api/domestic_ai_analysis.py backend/app/api/foreign.py`。

**风险/备注**：中。预览是在分析**之前**统计，无法预测"本次新研判"分数，需前端注明"预览为预估，以运行结果为准"。

---

## Phase ①B 历史数据回填（一次性）

**目标**：修复历史遗留 `AlertRecord`（快照缺 `risk_score` 键）导致 `- 分`。一次性脚本重建快照，**幂等、可回滚**。

**改动文件**：新增 `backend/scripts/backfill_alert_snapshots.py`。
- 遍历 `rule_risk_snapshot`/`ai_risk_snapshot` 缺 `risk_score` 键的 `AlertRecord`，用关联 `Opinion.risk_score` 重建 `rule_risk_snapshot.risk_score`（沿用 `AlertService.evaluate` 既有写法）。
- 须过 `scripts/db_identity_check.py`（VERIFIED 才执行；测试库 `DB_IDENTITY_CHECK=off`）。
- 默认 `--dry-run` 打印待修复数量与样例；`--apply` 才写库。
- 写库前自动 `pg_dump` 备份到临时文件并打印路径。

**验收标准**
- `--dry-run`：报告待修复 `AlertRecord` 数量、按来源分布、样例 ID；不写库。
- `--apply` 后：对应 `AlertRecord` 在预警记录页不再 `- 分`；重跑 `--dry-run` 数量归零（幂等）。
- 生产库执行前已生成 `pg_dump` 备份。

**回滚**：执行前 `pg_dump` 备份文件恢复；或脚本附 `--reverse` 将快照 `risk_score` 还原为执行前快照（可选）。

**风险**：中（写生产库）。靠身份校验 + dry-run + 备份三重保险；择期在低峰执行。

---

## 全局回滚顺序与边界

- 各 Phase 独立 git 提交、独立分支/commit，任一 Phase 验收不通过即 `git checkout` 对应文件回滚，不影响其他 Phase。
- 数据类 Phase（①B）必须在 ①A 之后（①A 已兜底展示，①B 为彻底修复历史数据），且执行前强制备份。
- 顺序含义：先解决"用户看得到"的展示口径（③）→ 长期兜底（①A）→ 统计准确性（②）→ 最后才动历史生产数据（①B）。
