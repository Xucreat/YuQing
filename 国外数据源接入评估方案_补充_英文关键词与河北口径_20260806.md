# 国外数据源接入评估方案 · 补充篇
## 英文关键词体系 + 河北（Hebei）地域口径

> 本文是 `国外数据源接入评估方案_20260806.md` 的**补充**，针对用户新明确的三点预期：
> ① 英文关键词（关键词管理页新增英语版本，功能不变）
> ② 地域范围由「廊坊」扩大到「河北（Hebei）」
> ③ 其余链路（采集 → Opinion → 聚合 → 预警）与现有数据源完全一致
>
> **状态：只读审计，未修改任何代码 / 数据库 / 配置。**
> 审计基准：生产库 `127.0.0.1:5432/opinion_db`（只读 SELECT），代码 HEAD 工作区。

---

## 0. 首要结论：上一版评估需要修正（更乐观）

上一版方案判断「必须新建国际哨兵 Region + 扩展 `collection_mode='international'` + Opinion 加 language/country 列」。

**经本轮逐行代码验证，该判断过于保守。** 按用户新口径（地域=河北），存在一条**完全复用现有机制、无需新建区域、无需扩展 collection_mode、无需改 Opinion 表**的通路：

> **外网源配置为 `collection_mode="regional"` + `scope_region_codes=["130000"]`**

生产库已确认存在该区域行，无需任何数据准备：

| id | code | name | level | parent_code |
|---|---|---|---|---|
| 2 | 130000 | 河北省 | province | (空) |
| 12 | 131000 | 廊坊市 | city | 130000 |
| 24 | 000000 | 全国 | province | (哨兵) |

**核心机制**（`opinion_region_service.py:180-195`）：`regional` 模式下即使正文**零地域命中**，也会走 `_default_scope_region(["130000"])` → 返回 Region id=2（河北省）→ `accepted_scope_default` 准入。英文正文匹配不到中文别名**不构成阻塞**，反而正好落在河北省。

---

## 1. 全链路逐环节验证（基于实际代码，非推测）

以一条 NYT 英文稿为例，源配置 `regional` + `scope=["130000"]`：

| # | 环节 | 代码位置 | 英文稿实际行为 | 判定 |
|---|---|---|---|---|
| 1 | 关键词注入 | `collectors/service.py:372-374` | 全局注入中文 `region_kw`(29)/`topic_kw`(14) 给**所有**采集器 | ⚠️ 新采集器需自行忽略，改用英文词 |
| 2 | 采集器抓取 | `service.py:516-518` | `collector.fetch(keywords, region_kw, topic_kw)` | ✅ 新类可重写，纯新增 |
| 3 | 「大厂」语义过滤 | `service.py:573` | 英文正文不含「大厂」→ 恒 True | ✅ 直通 |
| 4 | **区域决策** | `opinion_region_service.py:180` | hits=[] → `_default_scope_region(["130000"])` → **region_id=2 河北省** | ✅ **零改动通过** |
| 5 | **准入服务** | `opinion_admission_service.py:140-155` | 非 weibo + 非 national → `default_allow_non_weibo` | ✅ **零改动放行** |
| 6 | Opinion 落库 | `service.py:598+` | 去重 → 建记录，`region_id=2` | ✅ 直通 |
| 7 | 风险评分 | `risk_engine.py` | 中文词典零命中 → `risk=20 / neutral / other` | ⚠️ 能跑通，无区分度 |
| 8 | 事件聚合 | `auto_aggregate_after_collect` | 可执行，英文聚类质量未验证 | ⚠️ 待实测 |
| 9 | 预警匹配 | `alert_service.py:35,49` | 规则关键词为中文（事故/火灾/…）→ 英文稿基本不命中 | ⚠️ 预警近乎静默 |

**结论**：**第 1–6 环（采集→入库）可零改动打通**；第 7–9 环（风险/聚合/预警）**不会报错、不会阻塞**，但英文语义能力缺失，属**功能降级**而非故障。

> ⛔ 上一版方案中的「国际哨兵 Region」「`collection_mode='international'`」「Opinion 加列」在河北口径下**均不需要**，建议从实施清单中删除。

---

## 2. 英文关键词管理页：隔离方案

### 2.1 现状（生产库实测）

| type | category | 总数 | 启用 |
|---|---|---|---|
| monitoring | 地域 | 29 | 17 |
| monitoring | 主题 | 14 | 14 |
| sensitive | 社会稳定 / 安全事故 / … | 39 | 36 |

### 2.2 ⛔ 必须避开的陷阱：不能只靠 `category` 区分

若英文词沿用 `type='monitoring'` 仅换 `category`（如 `region_en`），会**立即污染三处既有功能**——因为扁平列表查询**不带 category 过滤**（`keyword_service.py:63-67`）：

| 受污染位置 | 代码 | 后果 |
|---|---|---|
| 所有中文采集器 | `service.py:371` → `keywords=monitoring_kw` | 中文源被塞入英文词 |
| **预警匹配** | `alert_service.py:49` | 预警规则关键词池被污染 |
| **看板热词** | `dashboard_service.py:596` | 热词榜混入英文 |

⛔ 该路线直接违反「不修改已实现功能」，**否决**。

### 2.3 ✅ 推荐方案：新增独立 `type='monitoring_en'`

**隔离性证明**：现有全部消费方都**显式**过滤 `Keyword.type == "monitoring"`（`keyword_service.py:65/97/132/198`），因此 `monitoring_en` 的词对既有链路**完全不可见**，行为零变化。

- 表结构**无需迁移**：`type` 是 `String(16)`，唯一约束为 `(word, type)` → 英文词与中文词天然共存。
- `category` 仍可复用 `地域` / `主题`（在 `monitoring_en` 命名空间内），语义与中文页一致。
- 关键词 CRUD API 已支持 `?category=` 与 `type` 过滤，**列表/新增/编辑/启停/删除逻辑无需改动** → 满足「功能不变」。

**唯一需要触碰的既有代码行**：

```
backend/app/api/keywords.py:34
ALLOWED_TYPES = {"monitoring", "sensitive"}          # 现状
ALLOWED_TYPES = {"monitoring", "sensitive", "monitoring_en"}   # 集合扩容
```

> 这是**集合扩容**，不改变任何既有取值的语义与分支，既有请求行为逐字节不变。若你要求**绝对零文件修改**，替代方案见 §2.5。

### 2.4 前端「英语版关键词页」

- 复用 `frontend/src/views/Keywords.vue`（已具备 `filters.type` / `filters.category` / 完整 CRUD）。
- 新增路由指向同组件，预置 `type='monitoring_en'`（route meta 或 query 注入），页面标题与类型下拉换英文集合。
- 权限沿用关键词页现有权限项，**不新增权限**。
- 工作量：**纯新增路由 + 组件轻量参数化**，不影响中文页。

### 2.5 替代方案（绝对零文件修改，但无 UI）

把英文关键词写入外网源的 `config_json.keywords`（`source_config.py` 已支持，且校验要求为**非空字符串的 JSON 数组**）：

```json
{ "keywords": ["Hebei", "Langfang", "Shijiazhuang", "Xiongan"] }
```

- ✅ 零代码改动；⛔ 但不满足「关键词管理页英语版本」的诉求，只能在数据源配置里维护。
- 建议：**一期先用此法验证通路，二期再上英文关键词页**。

### 2.6 消费侧接线（纯新增）

在 `keyword_service.py` **新增**一个函数（不改动任何既有函数）：

```
get_monitoring_en_keywords_grouped(db) -> {"地域": [...], "主题": [...]}
```

仅由新的外网采集器调用；既有 `get_monitoring_keywords*` 全部保持原样。

---

## 3. 河北（Hebei）地域口径：两层语义必须分开

这是最容易混淆的一点，务必区分：

| 层 | 问题 | 由谁负责 | 结论 |
|---|---|---|---|
| **归属层** | 这条稿子记到哪个 region_id？ | `opinion_region_service` | ✅ `scope=["130000"]` → 恒定河北省(id=2)，**零改动** |
| **相关性层** | 要不要抓这条稿子？ | **采集器内的英文关键词过滤** | ⚠️ 唯一控制点，必须在采集器实现 |

**关键警示**：`regional` 模式下，正文**不提河北也会被归到河北省**（scope 兜底）。因此「是否与河北相关」**只能靠采集器的英文关键词过滤把关**。若英文词表过宽（如只配 `China`），会把大量无关国际新闻挂到河北省名下。

> 建议英文地域词表：`Hebei`, `Langfang`, `Shijiazhuang`, `Baoding`, `Tangshan`, `Xiongan`, `Beijing-Tianjin-Hebei`, `Jing-Jin-Ji` 等。

### ⛔ 严禁的改法

**不要**把 `130000` 或 `Hebei` 加进 `LANGFANG_REGION_ALIASES`（`opinion_region_service.py:21-33`）。原因有二：

1. 该别名表被**所有中文源共用**，新增「河北」会让既有中文稿凭空产生地域命中 → 改变既有归属行为。
2. `decide()` 第 136 行 `county_codes = [code for code in hit_codes if code != "131000"]` 会把 `130000` **误当作区县代码**处理 → 单命中时错误归到省级、多命中时错误回落廊坊市，属逻辑破坏。

---

## 4. 下游影响与「污染」边界（诚实披露）

英文稿落 `region_id=2（河北省, level=province）`，与现有「全国哨兵」同为 province 级，因此**沿用同一套已知的下游行为**：

| 下游 | 影响 | 严重度 |
|---|---|---|
| 廊坊市/县 TOP 榜 (`_detail_regions`) | 排除 province 级 → **不受影响** | ✅ 干净 |
| 看板总量/趋势/来源/热词 | 全局聚合，无 region 过滤 → 英文稿**计入总数** | ⚠️ 中 |
| 中国地图 `_rollup_provinces` | 河北省桶数字增大（视觉上河北变热） | ⚠️ 中 |
| 预警 `alert_service` | 无 region 过滤，但规则关键词为中文 → 英文稿基本不命中 | ✅ 低 |
| 事件聚合 | 英文稿参与聚类，质量未验证 | ⚠️ 待实测 |

**缓解建议（可选，二期）**：给外网源统一打 `source` 前缀（如 `nyt`/`cnn`），看板若需剔除可按 source 过滤——但这属于修改看板，**一期不做**。

---

## 补充二：地域口径细化到「河北省下设的市县」（2026-08-06 二次补充）

> 针对用户进一步澄清：「地域范围扩大到河北（Hebei），包括河北省下设的省市县（即当前关键词管理中已启用的地域关键词）」。本节做事实核查 + 细化机制设计。

### B.1 事实核查：当前已启用的「地域」关键词只有廊坊

只读查询 `keywords WHERE category='地域' AND is_enabled=true`，返回 **17 条，全部属于廊坊辖区**：

| code（对应 Region） | 启用地域词 |
|---|---|
| 131000 廊坊市 | 廊坊 |
| 131002 安次区 | 安次、安次区 |
| 131003 广阳区 | 广阳、广阳区 |
| 131022 固安县 | 固安 |
| 131023 永清县 | 永清县 |
| 131024 香河县 | 香河 |
| 131025 大城县 | 大城县 |
| 131026 文安县 | 文安、文安县 |
| 131028 大厂回族自治县 | 大厂、大厂县、大厂回族自治县 |
| 131081 霸州市 | 霸州、霸州市 |
| 131082 三河市 | 三河 |

**重要提示**：这 17 个词**全部落在廊坊（131000 及其下级）**，库里**没有任何其他河北城市（石家庄/保定/唐山/…）的地域关键词**。因此「当前已启用的地域关键词 = 河北省下设的省市县」这一前提与库现实**不一致**——它实际只覆盖廊坊一市。请在确认项 B 里明确意图。

### B.2 区域决策如何把英文稿分到「具体市县」

`decide()` 的地域命中只比对 `LANGFANG_REGION_ALIASES`（中文，廊坊专用，`opinion_region_service.py:21-33,197-217`）。英文稿正文不含中文 → `hits=[]` → 一律走 `_default_scope_region(["130000"])` → **落在河北省(id=2)，无法细化到市县**。

要细化到市县，必须让 `decide()` 能识别英文地名。两条路径：

| 路径 | 做法 | 是否推荐 |
|---|---|---|
| ⛔ 路径 A：污染现有别名表 | 往 `LANGFANG_REGION_ALIASES` 加英文（如 `"131000": (..., "Langfang")`） | **否决**：中文源共用该表；且 `decide()` 第 136 行把非 131000 的命中当"区县"、多命中回落 **廊坊市(131000)**（第 148/157 行），会让外文"石家庄+保定"误归廊坊 |
| ✅ 路径 B：新增独立 `FOREIGN_REGION_ALIASES` | 新建英文别名表 + `decide()` 增加默认 `False` 的 `is_foreign` 形参；外文源多市命中时回落 **河北省(130000)** 而非廊坊 | **推荐**，向后兼容 |

**路径 B 的改动点（均为纯新增 / 默认分支，既有中文源零影响）**：
1. `opinion_region_service.py` 新增模块级常量 `FOREIGN_REGION_ALIASES`（Hebei 省市县 code → 英文别名）。
2. `_region_hits(text, is_local_source, is_foreign=False)`：当 `is_foreign=True` 额外遍历 `FOREIGN_REGION_ALIASES`。
3. `decide(..., is_foreign=False)`：传入 `is_foreign`；多命中/城市级回落改用 `fallback_code = "130000" if is_foreign else LANGFANG_CITY_CODE`（一行条件）。
4. 调用点 `collectors/service.py:577`：`decide(..., is_foreign=getattr(collector, "is_foreign", False))` —— 默认 False，中文源行为逐字节不变（已确认 `decide()` 现行调用只传 `scope_region_codes` 与 `collection_mode`）。

### B.3 推荐的英文别名表（覆盖「河北省 + 已启用地域词对应的廊坊市县 + 全部河北城市」）

```python
FOREIGN_REGION_ALIASES: dict[str, tuple[str, ...]] = {
    "130000": ("Hebei", "Hebei Province"),
    # —— 廊坊市县（= 当前已启用 17 个地域词对应的 code）——
    "131000": ("Langfang", "Langfang City"),
    "131002": ("Anci", "Anci District"),
    "131003": ("Guangyang", "Guangyang District"),
    "131022": ("Guan", "Guan County", "Gu'an"),
    "131023": ("Yongqing", "Yongqing County"),
    "131024": ("Xianghe", "Xianghe County"),
    "131025": ("Dacheng", "Dacheng County"),
    "131026": ("Wen'an", "Wen'an County", "Wenan"),
    "131028": ("Dachang", "Dachang Hui Autonomous County"),
    "131081": ("Bazhou", "Bazhou City"),
    "131082": ("Sanhe", "Sanhe City"),
    # —— 其余河北城市（按"河北省下设的省市县"口径补全覆盖）——
    "130100": ("Shijiazhuang",),
    "130200": ("Tangshan",),
    "130300": ("Qinhuangdao",),
    "130400": ("Handan",),
    "130500": ("Xingtai",),
    "130600": ("Baoding",),
    "130700": ("Zhangjiakou",),
    "130800": ("Chengde",),
    "130900": ("Cangzhou",),
    "131100": ("Hengshui",),
    "133100": ("Xiong'an", "Xiong'an New Area", "Xiongan"),
}
```

> 库已确认存在 `130000/130100/.../131100/133100` 全部 Region 行（§0 与本次查询），**无需任何数据准备**。

### B.4 两层独立：英文「地域关键词」≠ 英文别名表

务必分清两类英文词，职责不同、互不替代：

| 类别 | 存哪 | 谁消费 | 作用 |
|---|---|---|---|
| 英文地域**关键词**（`monitoring_en`, `category='地域'`） | `keywords` 表 | 外网采集器（采集过滤） | 决定**抓不抓**这条稿 |
| 英文地域**别名**（`FOREIGN_REGION_ALIASES`） | 代码常量 | `decide()`（区域决策） | 决定抓来的稿**归到哪个市县** |

两者**命名须一致**（关键词用 `Langfang`，别名也含 `Langfang`），否则"抓到了却归不到廊坊"。建议在英文关键词页初始化时，与 B.3 别名表保持 1:1 同步。

- 英文地域关键词初值 = B.3 表中每个 code 取首个英文名（Hebei / Langfang / Anci / Guangyang / Gu'an / Yongqing / Xianghe / Dacheng / Wen'an / Dachang / Bazhou / Sanhe / Shijiazhuang / Tangshan / … / Xiong'an）。
- 注意：这些**不需要**反向去启用中文的「石家庄/保定」等地域词（那是国内源的事，与本次"外网源"无关）；外网源只需英文别名即可细化归属。

### B.5 细化后的下游影响

英文稿现在会落到**具体市县**（如 131000 廊坊、130600 保定），而不只是省级 130000：
- ⚠️ 廊坊市/县 TOP 榜：英文稿若在正文命中 `Langfang` 等，会进入对应市/县桶——与中文稿**并列同视图**，可能稀释廊坊市县榜（一期若不接受可暂只用省级 scope，见确认项 B1）。
- ⚠️ 中国地图 `_rollup_provinces`：河北桶数字继续增大（现在还能细分到市）。
- ✅ 全国哨兵/廊坊视图**仍不受污染**（外网源走 regional + FOREIGN 别名，不碰 national 逻辑）。

### B.6 对改动清单（§5）的补充

在「纯新增」表追加：
| 新常量+形参 | `opinion_region_service.py`：新增 `FOREIGN_REGION_ALIASES`；`decide()`/`_region_hits()` 增加默认 `False` 的 `is_foreign`；多命中回落改 `"130000"`。调用点 `service.py:577` 传 `is_foreign=getattr(collector,"is_foreign",False)` |

在「既有文件最小触碰」表追加：
| `backend/app/collectors/foreign_rss.py` 各子类 | 设 `is_foreign=True` 与 `scope_region_codes=["130000"]` | 纯新增文件，不影响其他采集器 |

工作量影响：B 路径属"纯新增 + 默认分支"，约 +0.5 天（含别名表核对与灰度）。一期合计约 **4 天**。

---

## 补充三：UI 页签化 + 地域收敛到「河北省一级」（2026-08-06 三次补充）

> 针对用户最新澄清：① 地域**只定河北省（Hebei）省一级**，本期不做市县细化；② 关键词管理页、数据源管理页**各新开一个「外网」切换页签**；③ 外网源采集**先跑通**，英文关键词由用户后续在页签内自行添加。本节为方案补充，未改动任何代码/库。

### C.1 地域口径收敛：只到河北省（130000），市县细化降为二期

与 §B「细化到市县」不同，本期**只到省级**。因此：

- §B 路径 B（`FOREIGN_REGION_ALIASES` + `decide()` 的 `is_foreign` 形参）**降级为二期可选**，一期不实现、不改 `opinion_region_service.py`。
- 一期仍走 §0/§3 的**零改动通路**：`collection_mode="regional"` + `scope_region_codes=["130000"]`。所有外网稿恒落 河北省(id=2)，区域服务**零代码改动**。
- 后续若要细化到市县，再启用 §B 路径 B（纯新增、默认 `False`、向后兼容，不影响本期）。

### C.2 关键词管理页：新增「外网数据源」切换页签

- 现状 `frontend/src/views/Keywords.vue`：已有 `filters.type` / `filters.category` 与完整 CRUD（§2.4），当前无页签。
- 做法：在 `Keywords.vue` 顶部加 `el-tabs`，两个 pane：
  - **中文关键词**（默认）：`type` 过滤为 `monitoring` / `sensitive`（现有行为不变）。
  - **外网数据源关键词**：`type='monitoring_en'`（§2.3）。列表 / 新增 / 编辑 / 启停 / 删除逻辑**完全复用**，仅预置 `type`。
- 用户通过此页签**自行添加**英文关键词（地域词如 `Hebei`/`Langfang`、主题词如 `policy`/`economy`），系统不预置固定词表。
- 后端配合（仅一处集合扩容）：`api/keywords.py:34` 的 `ALLOWED_TYPES` 加 `"monitoring_en"`（§2.3，一行，既有取值语义不变）。
- 采集器消费：新增 `keyword_service.get_monitoring_en_keywords_grouped()`（§2.6），外网采集器读取英文词做相关性过滤。

### C.3 数据源管理页：新增「外网数据源」切换页签

- 现状 `frontend/src/views/Sources.vue`（node 虚拟化文件，本次已用 node 读明文确认）：工具栏含 **区域 / 启用状态 / 搜索** 筛选 +「新建采集源」按钮；表格为自定义 `<table class="tbl">`；已含 `collection_mode` 概念；**无 `el-tabs`**。
- 做法：在 `Sources.vue` 顶部加 `el-tabs`，两个 pane：
  - **国内数据源**（默认）：现有全部源。
  - **外网数据源**：仅显示外网源。
- **外网源的区分标记**（二选一，均不破坏国内源）：
  - 推荐（零迁移）：数据源 `config_json` 内加 `is_foreign: true`；页签按 `config_json.is_foreign` 过滤。
  - 或（更干净但需迁移）：`data_sources` 表新增可空 `is_foreign` 布尔列，便于后端/看板后续按列过滤。
- 「新建采集源」在外网页签下预置：`collection_mode=regional`、`scope_region_codes=["130000"]`、`config_json.is_foreign=true`、采集类型=RSS；其余表单字段（名称、频率、启用、质量展示、手动采集）与国内源一致。
- 外网源同样支持启用/停用、采集频率、手动采集、质量展示（复用现有逻辑）。

### C.4 关键词「先跑通、后添加」的衔接

外网 RSS 采集器抓取的是**订阅源最新条目**，英文关键词仅作**相关性过滤**（可选）。因此一期「跑通」阶段：

- 方案一：先用 `config_json.keywords`（§2.5）放 1–2 个种子词（如 `["Hebei"]`）验证通路；
- 方案二：采集器在**无英文词时不过滤、全量入库** feed 条目（需采集器支持"无关键词则跳过过滤"）；
- 跑通后：用户在「外网数据源关键词」页签自行增删英文词，采集器通过 `get_monitoring_en_keywords_grouped()` 实时读取生效。

> ⚠️ 无论哪种，外网稿恒归 河北省(130000)；"是否与河北相关"仍**只由英文关键词把关**（§3 警示一期依然成立，词表过宽会把无关国际新闻挂到河北名下）。

### C.5 修订后的工作量（一期）

| 阶段 | 内容 | 工作量 |
|---|---|---|
| 0 | 出口连通性（CNN/Fox/NYT/WSJ RSS 实测） | 0.5 天 |
| 1 | `foreign_rss.py`（1 基类 + 4 子类）+ `data_sources` 插 4 行（`regional` / `130000` / `is_foreign`） | 1.5 天 |
| 2 | 英文关键词体系：`monitoring_en` + `ALLOWED_TYPES` 一行 + `get_monitoring_en_keywords_grouped()` | 0.5 天 |
| 3 | 前端两个页签：关键词管理「外网」tab + 数据源管理「外网」tab | 1 天 |
| 4 | 灰度验证（单源单跑 → 查 `region_id=2` / 去重 / 聚合 / 预警静默） | 0.5 天 |
| **合计** | **一期（省级口径，不含市县细化、不含英文 NLP）** | **约 4 天** |

> 比 §B 的 4 天更轻：§B 的 +0.5 天「市县细化」已移除，等量工作量转为前端页签化（C.3）。

### C.6 对 §5 改动清单的更新

- **纯新增**：`foreign_rss.py`、前端「外网」tab（在 `Keywords.vue` / `Sources.vue` 内新增 `el-tabs` 节点，均为纯新增）、`get_monitoring_en_keywords_grouped()`、`data_sources` 插 4 行。
- **最小触碰**：`api/keywords.py:34` `ALLOWED_TYPES` +1 值；可选 `data_sources` 加 `is_foreign` 列（或仅用 `config_json`）。
- **移出一期（降为二期可选）**：§B 的 `FOREIGN_REGION_ALIASES` / `decide()` 的 `is_foreign` 形参 / 市县别名表。

---

## 5. 修订后的改动清单

### 纯新增（不触碰既有逻辑）
| 类型 | 内容 |
|---|---|
| 新文件 | `backend/app/collectors/foreign_rss.py`（1 基类 + NYT/CNN/Fox/WSJ 4 薄子类，接受并**忽略** `region_kw/topic_kw`，改用英文词） |
| 新函数 | `keyword_service.get_monitoring_en_keywords_grouped()` |
| 新路由/页面 | 前端两个「外网」tab：关键词管理页 `Keywords.vue` + 数据源管理页 `Sources.vue` 各新增 `el-tabs` 切换（纯新增节点） |
| 纯数据 | `data_sources` 插 4 行（`collection_mode=regional`, `scope_region_codes=["130000"]`, `config_json.is_foreign=true`）；英文关键词由用户在「外网数据源关键词」页签自行添加（`type='monitoring_en'`） |

### 既有文件的最小触碰
| 文件 | 改动 | 行为影响 |
|---|---|---|
| `backend/app/api/keywords.py:34` | `ALLOWED_TYPES` 集合加一个值 | 零（既有取值语义不变） |

### ⛔ 已从上一版方案中删除（河北口径下不需要）
- 国际哨兵 Region
- `collection_mode='international'` 扩展
- `Opinion` 表新增 `language` / `country` 列
- `OpinionRegionService.decide()` / `OpinionAdmissionService.evaluate()` 的 international 分支

---

## 6. 阻塞项与工作量（修订）

**阶段 0（阻塞，不变）**：网络出口。nytimes.com / wsj.com 境内通常被阻断，CNN/Fox 波动。必须先解决代理或境外采集节点，否则后续全部无法验证。

| 阶段 | 内容 | 工作量 |
|---|---|---|
| 0 | 出口连通性验证（4 站 RSS 实测） | 0.5 天 |
| 1 | `foreign_rss.py` + 数据源建行（英文词先走 `config_json` 验证通路） | 1.5 天 |
| 2 | 英文关键词体系（`monitoring_en` + 前端页 + 新 service 函数） | 1 天 |
| 3 | 灰度验证（单源单跑 → 查 region_id=2 / 去重 / 聚合 / 预警静默确认） | 0.5 天 |
| **合计** | **一期（不含英文 NLP）** | **约 3.5 天** |
| 4（可选） | 英文风险/情感模型，解决 §1 第 7–9 环降级 | 另评估 |

**建议接入顺序**：CNN / Fox（通路验证）→ NYT（需代理）→ WSJ（RSS 受限，最后）。

---

## 7. 待你确认（最终版，对应三次补充）

> 一期口径已收敛为：**省级河北（130000）+ 英文关键词 + 关键词管理/数据源管理各一个「外网」页签 + 关键词跑通后用户自行添加**。市县细化、英文 NLP 均为二期可选。

1. **地域归属**：外网稿统一落「河北省(id=2)」，且**正文不提河北也会归入**（相关性由英文关键词把关）——是否接受？（§3）
2. **看板口径**：英文稿计入看板总量/趋势/河北地图桶——是否接受？（如需隔离需改看板，超出「不修改既有功能」边界）（§4）
3. **网络出口**：代理环境变量 还是 境外采集节点？WSJ 一期是否接？（§6）
4. **【C1 外网源标记】**：外网源区分用 `config_json.is_foreign`（零迁移）还是 `data_sources` 加 `is_foreign` 列（干净但需迁移）？
5. **【C2 页签实现】**：关键词管理页 / 数据源管理页用 `el-tabs` 新增「外网」切换页签是否认可？（纯前端新增节点，不动既有 tab 外逻辑）
6. **【C3 跑通期关键词策略】**：先用 `config_json.keywords` 放种子词（如 `["Hebei"]`），还是采集器无英文词时全量入库 feed 条目？跑通后均在页签自行增删。
7. **【C4 市县细化（二期）】**：本期只到省级；若后续要细化到市/县，走 §B 路径 B（新增 `FOREIGN_REGION_ALIASES` + `is_foreign` 形参，向后兼容）。是否确认为二期？

---

*本补充文档基于只读代码审计与只读数据库查询生成，未产生任何写操作。*
