# 权限系统最小改动修复清单（待确认后实施）

> 生成时间：2026-08-03
> 范围：仅修复"角色权限子页面"4 个可见性/一致性问题。
> 原则：最小改动——只改前端路由/菜单门控 + 1 处后端接口鉴权宽松；不新增权限码、不改数据库、不动业务权限模型。
> 状态：本文为确认清单，**尚未实施任何改动**。

---

## 一、影响文件清单

| 文件 | 改动性质 | 涉及行 |
|---|---|---|
| `frontend/src/views/Opinions.vue` | 删除按钮门控 | 291 |
| `frontend/src/router/index.ts` | 路由 meta 门控 / `/system` 重定向 | 84, 97-104, 110, 116 |
| `frontend/src/components/AppLayout.vue` | 菜单可见性计算 | 219-222, 227 |
| `backend/app/api/users.py` | `GET /permissions` 鉴权放宽 | 495 |

注：`DataManage.vue` 当前磁盘损坏（Read 报 binary、Grep 无匹配），问题 4 的完整 per-tab 内容门控需先修复该文件，见第四节"重要遗留"。

---

## 二、问题 1：舆情-管理舆情 是否包含删除

- **根因**：后端 `opinions.py:465/506` 的删除接口用 `require_permission("opinions:write")`（即"管理舆情"）鉴权；权限种子备注已写"删除/编辑舆情"(`rbac10001.py:180`)。但前端 `Opinions.vue:291` 写的是 `const canDelete = computed(() => isSuperuser.value)`——删除按钮**仅超管可见**，且无独立 `opinions:delete` 权限码。结果：非超管的"管理舆情"角色在界面上看不到删除入口，观感上"不包含删除"，但后端其实允许其删除（前后端不一致）。

### 业务意图 A（推荐，与备注一致：管理舆情即含删除）
- 改动：`Opinions.vue:291` 改为
  ```ts
  const canDelete = computed(() => hasPermission('opinions:write'))
  ```
  仅 1 行，使 UI 与后端、与备注保持一致。
- 测试：用"管理舆情"非超管角色登录 → 舆情列表出现删除按钮且可删；超管行为不变。
- 数据影响：无。风险：低（仅是让 UI 入口与既有后端权限对齐，未授予新权限）。

### 业务意图 B（若真实意图是"仅超管可删"）
- 则前端现状正确，应修后端 + 备注：
  - 改动：`opinions.py:465`、`506` 的 `require_permission("opinions:write")` 改为超管校验（复用 `is_superuser_user` 或新增 `opinions:delete` 且默认只赋超管）；同步将 `rbac10001.py:180` 备注改为"编辑舆情（删除仅超管）"。
  - 测试/数据影响：需重启后端；备注为种子数据，改后需重新播种或 `UPDATE permissions`；属数据变更，需单独确认。

---

## 三、问题 2：已拥有"查看权限目录"刷新后仍不显示相关页面

- **根因**：`permissions:read`（查看权限目录）只门控后端 `GET /permissions`(`users.py:495`)，**没有对应的前端页面**；权限树 UI 嵌在"角色权限"页（路由 `/system/roles`，门控 `roles:read`），且系统管理菜单 `hasSystemPerm`(`AppLayout.vue:219`) 不含 `permissions:read`。刷新机制本身正常（`main.ts:27-39` 启动即 `fetchMe()` 覆盖本地缓存），问题是"权限名"与"页面门控"错位。

### 最小改动（让"查看权限目录"可进入角色权限页查看权限树）
1. `router/index.ts` `/system/roles` 的 `meta`：
   ```ts
   // 原：permission: 'roles:read'
   permissions: ['roles:read', 'roles:write', 'roles:delete', 'permissions:read'],
   permissionAny: true,
   ```
2. `router/index.ts` `/system` 的 `redirect`：在 `roles:read` 判断后追加
   ```ts
   if (hasPermission('permissions:read')) return '/system/roles'
   ```
3. `AppLayout.vue` `hasSystemPerm`(219)：追加 `|| hasPermission('permissions:read')`。
4. （健壮性）`users.py:495` `GET /permissions`：放宽至 `permissions:read` **或** `roles:read`，避免仅持 `roles:read` 的用户打开角色页时权限树拉取 403。

- 测试：仅授予 `permissions:read` 的角色登录 → 侧边栏出现"系统管理" → 进入后显示"角色权限"及权限树；仅持 `roles:read` 的用户权限树正常加载。
- 数据影响：无（仅接口鉴权放宽，不改表）。风险：低。

---

## 四、问题 3：用户/角色模块 只未勾"查看"整页就消失

- **根因**：路由 `/system/users` 门控 `users:read`、`/system/roles` 门控 `roles:read`(`router/index.ts:110,116`)，均为单一 `:read` 码，无"模块任一权限即可进"逻辑；`write/activate/delete` 只控页内按钮。

### 最小改动（让模块内任一权限都能打开该页）
1. `/system/users` 的 `meta`：
   ```ts
   // 原：permission: 'users:read'
   permissions: ['users:read', 'users:write', 'users:activate', 'users:delete'],
   permissionAny: true,
   ```
2. `/system/roles` 的 `meta`（与第三节第 1 步合并）：
   ```ts
   permissions: ['roles:read', 'roles:write', 'roles:delete', 'permissions:read'],
   permissionAny: true,
   ```
3. `AppLayout.vue` `hasSystemPerm`(219)：扩展为"模块内任一权限即显示"，建议改为：
   ```ts
   const hasSystemPerm = computed(() =>
     hasPermission('users:read') || hasPermission('users:write') ||
     hasPermission('roles:read') || hasPermission('roles:write') ||
     hasPermission('permissions:read') ||
     hasPermission('login_logs:read') || hasPermission('audit_logs:read'),
   )
   ```
   （或抽一个"模块可达"helper，按资源前缀 `users:`/`roles:` 判断，避免逐码罗列。）
4. `router/index.ts` `/system` 的 `redirect`：相应接受 `users:write/activate/delete` 与 `roles:write/delete`，否则会出现"菜单显示但点进去被踢回 /dashboard"。

- 测试：仅授予 `users:write`（不勾 `users:read`）的角色 → 系统管理可见、用户管理页可进、编辑按钮在、查看/删除按各自权限显隐。
- 数据影响：无。风险：低；注意 `/system` redirect 与 `hasSystemPerm` 必须同步改动。

---

## 五、问题 4：关键词模块 不勾"查看关键词" → 数据源整页消失

- **根因**：`/data` 路由门控 `keywords:read`(`router/index.ts:84`，注释"整页门槛取 keywords:read")；数据源/采集日志/AI线索审核均为 `/data` 内部 tab（非独立路由）；`hasDataPerm = keywords:read || isSuperuser`(`AppLayout.vue:227`)。无 `keywords:read` 连父页都进不去，`sources:read` 在门禁之外无效。

### 最小改动（使任一数据管理子模块读权限都能进入整页）
1. `router/index.ts` `/data` 的 `meta`：
   ```ts
   // 原：permission: 'keywords:read'
   permissions: ['keywords:read', 'sources:read', 'collectors:read'],
   permissionAny: true,
   ```
2. `AppLayout.vue` `hasDataPerm`(227)：
   ```ts
   const hasDataPerm = computed(() =>
     hasPermission('keywords:read') || hasPermission('sources:read') ||
     hasPermission('collectors:read') || isSuperuser.value,
   )
   ```

### 重要遗留（非最小改动，需排期）
- `DataManage.vue` 当前磁盘损坏（Read 报 binary、Grep 无匹配，疑似历史 node 写入损坏）。在上述路由/菜单改动后，"整页不消失"已解决，但要做到 **tab 内容级正确门控**（例如 `sources:read` 用户进入 `/data` 默认落在关键词 tab，因无 `keywords:read` 该 tab 数据会 403），必须：
  1. 先修复/找回 `DataManage.vue` 源文件；
  2. 在页内根据 `keywords:read`/`sources:read`/`collectors:read` 选择**默认 tab** 并**禁用/隐藏无权限 tab**。
- 测试：仅授予 `sources:read`（不勾 `keywords:read`）的角色 → 数据管理菜单可见、可进入、数据源 tab 可用；关键词 tab 若无权限应禁用/隐藏。
- 数据影响：无。风险：中；完整修复依赖 `DataManage.vue` 先修复。

---

## 六、验收标准（实施后应全部满足）
1. 问题 1：管理舆情角色删除按钮可见且能删（或按意图 B 为超管专属，且前后端一致）。
2. 问题 2：`permissions:read` 可进入角色权限页看到权限目录。
3. 问题 3：用户/角色模块勾选任一权限即可见对应页。
4. 问题 4：`sources:read` 可使数据管理页可见并可查看数据源。
5. 全链路：前端门控与后端 `require_permission` 语义一致，无"界面能进但接口 403"或反之。

---

## 七、待你确认（确认后我再实施）
1. 问题 1 选 **意图 A**（UI 跟随后端，1 行改动）还是 **意图 B**（后端收紧超管，需重启+种子数据变更）？
2. 是否同意"模块内任一权限即可进页"的放宽口径（问题 3）？
3. 问题 4 的 `DataManage.vue` 损坏文件，是否授权我先修复该文件、再补齐 tab 内容门控？

确认后我将按清单逐文件改动（每文件单独提交），并附前端构建 + 后端重启验证。
