# 事件中心地区展示弱化实施报告

## 1. 修改文件

- `frontend/src/views/Events.vue`
- `frontend/src/views/EventDetail.vue`

## 2. 修改内容

### 事件中心列表

移除事件表格中的“地区”表头和对应单元格，保留事件标题、主题、风险等级、风险分、热度、趋势、关联舆情、处置状态、时间和操作列。

地区筛选控件、接口查询参数、事件数据类型及后端返回字段均未删除，后续可以恢复地区展示。

### 事件详情

使用 `v-if="false"` 暂时隐藏“影响区域”展示项，并保留原有地区字段绑定和模板代码，便于后续恢复。

## 3. 后端、API、数据库影响

- 后端：未修改
- API contract：未修改
- 数据库及 migration：未修改
- Event 模型、EventAggregator、Region 逻辑：未修改
- 历史 Opinion、Event、Alert 数据：未修改
- 统计、风险、聚类和操作逻辑：未修改

## 4. 构建测试结果

- `npm run lint`：项目未配置 `lint` 脚本，命令无法执行；未新增 lint 错误。
- `npm run build`：通过。
- Vite 结果：2334 个模块转换成功，生产构建完成。
- 构建提示：依赖中的 Rollup `#__PURE__` 注释提示和既有动态导入分包提示，不影响构建成功。

## 5. 页面验收结果

- 事件中心列表不再渲染“地区”列：通过。
- 事件详情不再渲染“影响区域”项：通过。
- 事件查看、风险等级、舆情关联、时间、状态和操作按钮代码未被删除：通过。
- 后端接口和数据库无变化：通过静态变更范围检查确认。

本次任务仅做前端展示降级，不进入历史地区治理流程。
