# 外网舆情界面 · 待修改需求整理（仅整理，未改动）

> 整理时间：2026-08-11
> 范围说明：用户测试后提出的修改清单，**仅整理、暂不实施**。本文含现状核查、目标、涉及文件与待澄清点。

---

## 编号说明
用户原始编号从 3 直接跳到 5（缺第 4 项），本文保留原编号以免错位，未自行补号。

---

## ⚠️ 现状核查（实施前必读）

| 需求点 | 当前真实状态（代码核查结论） |
|---|---|
| 1. 预警中心整合外网 | **`Alerts.vue`（预警中心）早已内置国内/国外切换**：`预警规则` pane 与 `预警记录` pane 各有 `domestic/foreign` radio toggle（`Alerts.vue:4-43`）。外网告警、外网规则**已存在于预警中心**。重复的是 `ForeignWorkspace.vue`（外网舆情）的 `alerts`（外网告警）+ `alertRules`（告警规则）两个 tab。 |
| 2.1 编辑即改密 | `Users.vue:30` 编辑弹窗内已有「修改此用户密码」按钮 → 复用同一密码弹窗（旧/新/确认）。`openPasswordDialog` 区分 `self`/`admin` 模式（`Users.vue:93-96`）：本人改密需旧密码，管理员重置他人仅需新密码。 |
| 2.2 删除导航改密 | `AppLayout.vue:52/133/158` 左侧底部「修改密码」按钮 + 弹窗（`u-password`）。 |
| 3. 定时采集接口 | **已实现**：`GET /api/foreign/collection-schedule/status`（`foreign.py:1347`）、调度作业 `foreign_collector`（`scheduler.py`）、逐源 `schedule_enabled`/`schedule_interval_minutes`（`foreign.py:667-671`）。缺"全局状态面板"与"前端总开关"。 |
| 3.1.1 数据源表合并 | 外网数据源有**两套实现**：① `ForeignWorkspace.vue` 的 `sources` tab（内联表格，自调用 `/foreign/sources`）；② `数据管理→外网数据源`用的 `ForeignSourcesView.vue`。二者重复。 |
| 5.1 角色权限 | 外网权限命名偏细：`foreign:keywords:read/write`、`foreign:sources:read/write`、`foreign:alerts:*`、`foreign:events:*` 等（`DataManage.vue:114-117` 可见）。 |
| 5.2 用户管理 | `Users.vue:17` 操作列当前仅「编辑」「删除」，无「启用/停用」。 |

---

## 1. 预警中心整合外网告警 / 告警规则

**目标**：删除冗余入口，外网告警与规则统一收归预警中心，用国内/国外切换。

- **1.1** 删除"预警中心横向导航栏"中的「外网预警」「外网告警规则」入口。
  - 核查说明：左导航（`AppLayout.vue`）并无这两个字面菜单项；真正的重复在 `ForeignWorkspace.vue` 的 tab 栏（`alerts`=外网告警、`alertRules`=告警规则）。**建议**：将 1.1 理解为"移除 `ForeignWorkspace.vue` 中这两个外网 tab"，避免歧义。
- **1.2** 将外网舆情页的：
  - 「外网告警」子页 → 并入预警中心 `预警记录`（foreign 视图，已存在）；
  - 「告警规则」子页 → 并入预警中心 `预警规则`（foreign 视图，已存在）。
  - 切换方式：复用预警中心已有的 国内/国外 radio（参照数据管理页的切换样式）。
- **涉及文件**：`frontend/src/views/ForeignWorkspace.vue`（删 `alerts`/`alertRules` 两个 tab 及其模板/`loadAlerts`/`loadAlertRules` 调用）；`Alerts.vue` 基本无需改（已支持）。
- **注意**：`ForeignWorkspace.vue` 内 `foreignAlerts`/`alertRules` 相关逻辑若被其他 tab 复用需排查，避免误删。

## 2. 管理员修改密码流程收敛

- **2.1** 点击「系统管理 → 用户管理」表格操作列的**编辑**按钮后，支持修改该用户密码，复用现有（旧密码 / 新密码 / 确认新密码）弹窗。
  - 现状：`Users.vue` 编辑弹窗已含「修改此用户密码」按钮触发同一弹窗，但入口较隐蔽（藏在编辑弹窗内）。用户希望**编辑按钮即可直达改密**。
  - **待澄清**：重置"他人"密码时是否仍需「旧密码」？当前逻辑：本人(self)需旧密码，他人(admin)仅需新密码。若要求统一显示旧/新/确认三框，需明确他人改密是否校验旧密码（后端 `/users/{id}/reset-password` 当前只收 `new_password`）。
- **2.2** 删除左侧导航栏底部的「修改密码」入口（`AppLayout.vue:52/133/158` 的 `u-password` 按钮 + `el-dialog`）。
- **涉及文件**：`AppLayout.vue`、`Users.vue`。
- **注意**：删除底部"修改密码"后，本人自助改密入口是否保留需确认（可保留在用户管理内，或用户中心）。

## 3. 外网定时采集

- **3.1 接口是否实现 / 如何启用（已答复）**
  - 已实现：状态接口 `GET /api/foreign/collection-schedule/status`、调度作业 `foreign_collector`、逐源 `schedule_enabled`/`schedule_interval_minutes` 均已落地。
  - **前端启用路径**：
    1. 数据管理 → 外网数据源：逐个外网源开启"定时采集"并设间隔（`ForeignSourcesView`，对应 `schedule_enabled`/`schedule_interval_minutes`）；
    2. 全局总开关 `foreign_collection_schedule_enabled`（后端 setting / 环境变量）需为 true —— **前端目前无此总开关 UI**，建议补一个"调度链路与状态"面板（展示 last_run/running/enabled + 全局启停）。
  - **待澄清**：是否要在前端新增全局状态面板与总开关？还是仅用逐源开关即可？
- **3.1.1 合并外网数据源表格**
  - 目标：只保留「数据管理 → 外网数据源管理」（`ForeignSourcesView.vue`，参照国内 `SourcesView`），删除「外网舆情页 → 外网数据源」子页（其内联表格与前者重复）。
  - 涉及文件：`ForeignWorkspace.vue`（删 `sources` tab 及模板/`loadSourcesView`/`loadScheduleStatus` 调用）；`数据管理`侧无需新增（已复用 `ForeignSourcesView`）。
  - 注意：`loadScheduleStatus()` 若仅服务于该 tab，可一并移除或迁移至数据管理页。

## 5. 系统管理

- **5.1 角色权限（`Roles.vue`）**
  - **5.1.1** 外网权限划分过细，需合并/简化（例如将 `foreign:keywords:*`、`foreign:sources:*`、`foreign:alerts:*`、`foreign:events:*` 收敛为更粗的粒度）。
    - 注意：权限 key 变更会牵连 `DataManage.vue`、`Alerts.vue`、`ForeignWorkspace.vue` 等多处门控，需同步评估。
  - **5.1.2** 操作列增添「启用 / 停用」按钮。
    - 待核查：角色模型是否有 `is_enabled`/`enabled` 字段及对应 PATCH 接口；若无需后端补。
- **5.2 用户管理（`Users.vue`）**
  - **5.2.1** 操作列增添「启用 / 停用」按钮。
    - 待核查：用户模型是否已有 `is_active`/`enabled` 及对应接口（`Users.vue` 当前删除有 `user.username==='admin'` 保护，启用停用需类似保护）；后端若无 toggle 接口需补充。

---

## 待澄清汇总（实施前需确认）

1. **1.1 删除入口的精确位置**：是 `ForeignWorkspace.vue` 的 `alerts`/`alertRules` 两个 tab，还是另有子导航？（建议按前者理解）
2. **2.1 他人改密是否校验旧密码**：统一三框 vs 仅新密码。
3. **2.2 删除后本人自助改密入口**：保留在哪里。
4. **3.1 前端是否新增全局状态面板 + 总开关**，还是仅逐源开关。
5. **5.1.1 外网权限合并后的目标粒度**（具体合并方案）。
6. **5.1.2 / 5.2.1** 启用/停用对应的后端字段与接口是否存在，是否需要后端配合。

---

## 涉及文件清单（供后续实施参考）

| 文件 | 关联需求 |
|---|---|
| `frontend/src/views/ForeignWorkspace.vue` | 1.1 / 1.2 / 3.1.1（删 alerts、alertRules、sources 三个 tab） |
| `frontend/src/views/Alerts.vue` | 1.2（已支持，基本不改） |
| `frontend/src/components/AppLayout.vue` | 2.2（删底部修改密码） |
| `frontend/src/views/Users.vue` | 2.1 / 5.2.1（编辑改密、启用停用） |
| `frontend/src/views/Roles.vue` | 5.1.1 / 5.1.2（权限收敛、启用停用） |
| `frontend/src/views/DataManage.vue` + `foreign/ForeignSourcesView.vue` | 3.1.1（保留的唯一外网数据源管理） |
| `backend/app/api/foreign.py` / `core/scheduler.py` | 3.1（状态接口已存在，可能补总开关） |
| 后端用户/角色相关接口 | 5.1.2 / 5.2.1（启用停用接口待核查） |
