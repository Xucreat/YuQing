# 微博（MediaCrawler）源三项改进实施方案

> 目标：减少重复、抓到新帖、正确覆盖关键词。
> 依据：已逐文件核实后端采集链路（`backend/app/collectors/*`、调度器 `core/scheduler.py`）与外部 `MediaCrawler` 仓库（`D:/code files/mediaCrawler/MediaCrawler`）。
> 范围：**仅给出实施方案，未改动任何代码/数据**。待你确认后执行。

---

## 0. 已核实的关键链路与约束（决定方案形态）

| 环节 | 文件:行 | 现状 |
|---|---|---|
| 关键词轮询（每轮 1 个） | `service.py:82-91` `select_round_robin_keyword` → `service.py:297-322` `_mediacrawler_keyword_turn` | 每个调度周期只从关键词列表取 **1 个** 词，`keyword_cursor` 持久化到 `data_sources.keyword_cursor`，逐轮推进 |
| 关键词来源解析 | `mediacrawler_normalizers.py:42-62` `resolve_effective_keywords` | 若 `config_json.keywords` 非空 → 整组作为 `datasource` 源（逗号 join 成**一次搜索串**）；否则走全局 `monitoring_kw`（~50 词）轮询 |
| 命令构建（逗号 join） | `mediacrawler_command_builder.py:83-104`（`--keywords` 行 93） | `--keywords "a,b,c"` → MediaCrawler 对每个词分页 |
| MediaCrawler 分页 | `media_platform/weibo/core.py:137-191` `search()` | 对每个关键词翻页直到 `page*10 >= CRAWLER_MAX_NOTES_COUNT`；**每关键词至少翻 1 页（10 条）**，无时间窗参数 |
| max_items 上限 | `mediacrawler_command_builder.py:12` `MAX_ITEMS=20` + `mediacrawler_runner.py:363-369` 校验 `<=20` | 单轮产出被硬卡在 ≤20 条 |
| 调度间隔 | `data_sources.schedule_interval_minutes`（weibo=60）；调度器每次 tick 读库 | 缩短间隔 = 改该列，无需改代码 |
| ⚠️ 时间窗原生支持 | `config/weibo_config.py:24` `WEIBO_SEARCH_TYPE="default"`（**写死，不读 env**） | **MediaCrawler 微博无 start/end time 参数**；时间窗只能**在适配器内 post-fetch 过滤** |

**结论先行：**
- 方向三（正确配关键词）+ 方向二(2A 缩短间隔) 是**零代码风险、立即可做**的，且方向三本身就实现"整组一次搜索"覆盖。
- 方向一（时间窗）必须**在适配器内做 post-fetch 过滤**，或（可选）改外部仓把 `WEIBO_SEARCH_TYPE` 改成 `real_time`。
- 方向二"每轮多关键词"在 20 条上限 + 每词至少 10 条 的约束下，单轮无法干净均分，推荐用"方向三整组 + 缩短间隔"替代。

---

## 方向三（先做、最稳）：把 `config_json.keywords` 配成多词列表

**原理**：`resolve_effective_keywords` 发现 `config_json.keywords` 非空就整组返回，`command_builder` 用逗号 join → MediaCrawler 一次搜索覆盖全部词。这正是"整组是一次搜索串"的官方支持路径，无需改代码。

**操作（二选一）**：

1. 直接改库（写库前先跑 `scripts/db_identity_check.py` 确认 VERIFIED）：
```sql
-- 例：把要监控的词配成 JSON 数组（顺序即搜索顺序）
UPDATE data_sources
SET config_json = jsonb_set(
        config_json::jsonb,
        '{keywords}',
        '["廊坊","霸州","大厂县","三河","固安","永清"]'::jsonb
     )
WHERE key = 'weibo_mediacrawler';
```
2. 或走管理 API：`POST /api/admin/data-sources/{id}`，`config_json.keywords` 传数组（`admin_data_sources.py:_split_source_keywords` 已支持数组）。

**坑**：`keywords` 必须是 **JSON 数组且元素非空字符串**，字符串/逗号串会被 `source_config.py:432-437` 拒。配完下一轮起 `effective_keywords_source=datasource`，不再走 50 词轮询。

---

## 方向二：提升覆盖（缩短间隔 + 多关键词）

### 2A 缩短轮询间隔（零代码，推荐先做）
- 直接改 `data_sources.schedule_interval_minutes`：60 → 30（或 20）。
- 调度器每次 tick 读库推进 `next_collect_time`，**改完立即生效，无需重启**。
- 配合方向三（整组一次搜索）后，缩短间隔 = 同样关键词组被更频繁刷新 → 更快命中新帖。

### 2B 每轮多关键词（需改代码，受 20 上限约束）
> 仅当方向三+2A 仍不够时再做。

- 改 `service.py:297-322` `_mediacrawler_keyword_turn`：从"取 1 个"改为"取 K 个窗口"（`K = config_json.keywords_per_run`，默认 1 保持兼容），返回 `normalized[current:current+K]`，cursor 推进 K。
- `command_builder` 已支持逗号 join 多词，无需改。
- **约束与对策**：MediaCrawler 每词至少翻 1 页（10 条），且输出总上限 20 条 → K 个词会按关键词顺序挤占前 20 条（后词被截断）。对策二选一：
  - (b1) 把 `MAX_ITEMS` 上限从 20 提到 `K*10`（改 `command_builder.py:12` + `runner.py:367` 校验），并评估登录时长/风控；
  - (b2) 保持 round-robin 但用 2A 缩短间隔让 cursor 更快轮完整组（**推荐**，最简单）。

---

## 方向一：加时间窗（减少重复 / 抓新帖）

### 1A 适配器内 post-fetch 过滤（推荐，零外改）
- 在 `WeiboNormalizer.normalize`（`mediacrawler_normalizers.py:178-215`）增加时间窗：
  - 新增读取 `config_json.time_window_days`（默认 0 = 不过滤）。
  - 若 `publish_time` 为 `None` 或 `publish_time < now - N 天` → 返回 `None`（该行被 `_read_jsonl` 计为 content empty 丢弃）。
- 调用处：`media_crawler_platform_collector.py:330-331` `_normalize_row` 已委托 normalizer；只需在 normalizer 里拿到 `time_window_days`。传递方式：在 `fetch()` 的 `crawler_config` 或构造 normalizer 时注入（normalizer 当前无配置上下文，需给 `WeiboNormalizer` 加一个 `time_window_days` 字段，由 collector 在 `build()`/构造时赋值）。
- **效果**：stale 旧帖不占 20 条预算、减少"地域命中但旧帖被 admission 拒"的噪声；预算留给较新帖。
- **注意**：时间窗**不减少 cross-run 重复**（重复是按 `external_id` 在入库时判重，见 `service.py` 入库逻辑）。要降重复仍需方向二（高频）+ 方向三。

### 1B（可选）real_time 排序 —— 改外部仓
- `D:/code files/mediaCrawler/MediaCrawler/config/weibo_config.py:24` 改 `WEIBO_SEARCH_TYPE = "real_time"` → 搜索按最新排序，更易命中新帖。
- **风险**：改的是外部 checkout（非本项目），影响所有走该 root 的 MediaCrawler 调用；该常量不读 env，无法按源隔离。
- **建议**：用 git 分支或注释保留回滚；或改为读 env（`os.getenv("WEIBO_SEARCH_TYPE","default")`）再按源注入（需同步改 `mediacrawler_runner.py:460-473` 的 environment 字典加 `WEIBO_SEARCH_TYPE`）。

---

## 建议实施顺序与验证

1. **方向三**：配 `config_json.keywords` 数组 → 立即获得整组覆盖。
2. **方向二 2A**：间隔 60→30 → 刷新更快。
3. **方向一 1A**：加 `time_window_days` 过滤 → 降 stale 噪声。
4. （可选）1B real_time / 2B 多词提上限。

**每步验证**（写库前跑身份门禁）：
```sql
SELECT start_time, status, fetched_raw, created, duplicate, admission_filtered
FROM collector_runs
WHERE collector_name='微博（MediaCrawler）'
ORDER BY start_time DESC LIMIT 12;
```
- 方向三生效后：`effective_keywords_source=datasource`、单轮 `fetched_raw` 覆盖整组。
- 1A 生效后：旧帖占比下降、`admission_filtered` 比例回落。
- 手动触发：`POST /api/collector/run`（weibo 源）走同闭环核对。

**DB 安全**：所有写库操作前先 `python backend/scripts/db_identity_check.py`（须 VERIFIED 退出码 0）；测试库设 `DB_IDENTITY_CHECK=off`。

---

## 风险与未决项
- **real_run_gate**：MediaCrawler 调度真实抓取受 `MEDIA_CRAWLER_REAL_RUN_GATE` 进程级开关控制（见 `mediacrawler_runtime.py:294`）；配置变更后若调度仍 0 产出，先确认该 gate 已开 + 进程已 reload。
- **登录风控**：缩短间隔/提高 max_items 会增加微博登录态调用频率，需观察是否触发验证码/封禁。
- **准入阈值**：本报告三项不改 `opinion_admission_service.py` 的微博加严逻辑；若仍觉放行过少，那是独立议题（前序诊断已单列）。
