# 事件中心幂等收口 · Phase 7.5-A 收口验收报告

> 日期：2026-07-24 12:14–12:20
> 性质：**只读验收 + 仅重启 :8000**（未改业务代码、未执行任何 DDL/DML、未删数据）
> 目标：确认 advisory lock 新代码已在运行环境生效，为 Phase 7.5-B 历史重复事件清理提供安全前置。

---

## A. 后端运行状态（核对结果）

| 实例 | 监听 PID | 启动时间 | 加载代码 | 健康 |
|---|---|---|---|---|
| **:8000** | 55732（监听）+ 12640（worker） | **2026-07-24 12:16:45** | **新代码（含聚合 advisory lock）** | `/openapi.json` → HTTP 200 |
| :8011 | 48672（监听）+ 59076（worker） | 2026-07-24 11:16:40 | 旧代码（无聚合锁） | 存活但未重载 |

**代码版本判定依据**：两实例均启动于 11:16:40，**早于**锁代码落地时间（aggregator.py `11:45:32`、advisory_lock.py `11:45:45`）→ 重启前均加载旧代码。重启后 :8000 新进程启动于 12:16:45（晚于文件修改），且启动日志 `Application startup complete` 无 import/traceback 错误，证明含 `advisory_lock` 的新 `aggregator.py` 导入链路正常。

**启动命令**（与既有实例一致，供复现）：
```
C:\Users\Administrator\Desktop\YQ\backend\.venv\Scripts\python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --log-level info
```

---

## B. 生产代码生效验证

1. **重启动作**：`taskkill /F /PID 38092 /PID 35500` 强杀旧 :8000 两进程 → 端口释放确认 → `Bash run_in_background` 按上述命令以 cwd=backend 重启。
2. **端口与进程**：`:8000` 重新 LISTENING（PID 55732，启动 12:16:45）。
3. **健康**：`GET /openapi.json` → **HTTP 200**；日志见 `Application startup complete` 及正常业务流量（`GET /api/alerts/unread 200`）。
4. **调用路径**：新 `aggregator.py` 含 `EVENT_AGGREGATE_LOCK_KEY`（L64）、`try_acquire_advisory_lock`（L287）、`skipped` 返回（L295）、`release_advisory_lock`（L303）；启动无报错即确认 `EventAggregator.aggregate()` 调用路径有效。**未触发实际聚合**（避免 DML 写入），锁逻辑正确性由 Phase 7.5-A 单测 4/4 通过保证。
5. **:8011 处理**：按要求**仅重启 :8000**，:8011 未触碰，仍运行旧代码（见下方风险）。

---

## C. 重复事件安全复核（只读重跑）

| 指标 | 基线（Phase 7 审计） | 当前 | 变化 |
|---|---|---|---|
| events 总数 | 237 | **241** | +4（不同 title 的合法新事件） |
| 重复 title 组 | 43 | **43** | **0（无新重复组）** |
| 冗余事件 | 43 | **43** | **0（无新冗余）** |
| opinions 总数 | 966 | 988 | +22（正常新采集） |
| alert_records（关联事件） | — | 78（76 关联） | 正常 |

**结论**：
- **未产生新的重复事件**——重复组数与冗余数纹丝不动（43 / 43）；新增 4 个事件 title 各异、+22 舆情为正常采集，与 Phase 7 scheduler 锁已阻断"每周期双调度"一致。
- **advisory lock 生效判定**：代码已确认在 :8000 新进程加载并单测通过；数据侧"零新增重复"与锁预期吻合。但静止态无持有行（调用期短锁属正常），且 **:8011 仍运行旧代码** → 跨实例完整保护尚未达成（详见风险与条件）。

**advisory lock 持有情况**：pg_locks 中 advisory lock 恰 1 个持有者（pid 43404，objid 2817738734，granted=t，backend_start 12:16:47）→ 即 Phase 7 scheduler 单例锁，完好无损。

---

## D. Phase 7.5-B 开始前检查报告

### 1. 当前运行实例
- **:8000** — 新代码（聚合锁生效），PID 55732，健康。
- **:8011** — 旧代码（无聚合锁），PID 48672，存活。

### 2. 锁保护状态
| 锁 | 状态 | 范围 |
|---|---|---|
| Phase 7 scheduler 单例锁 | 生效（1 持有者） | 全实例（跨进程） |
| Phase 7.5-A 聚合 advisory 锁 | **:8000 生效** | 仅 :8000 新代码进程 |

### 3. 数据状态
- 重复事件 43 组 / 43 冗余，自基线零新增；opinions 988、alerts 76 关联事件，均正常。
- 存量重复事件**仍未清理**（Phase 7.5-B 范围，本次不执行）。

### 4. 是否满足进入历史合并条件
**条件基本满足，但存在一处前置缺口，需先处理 :8011 方为完全满足。**

- ✅ :8000 已加载聚合锁，可阻止由 :8000 发起的并发双聚合。
- ⚠️ **:8011 仍为旧代码**：若「手动聚合落在 :8000（新，取锁）」与「:8011 定时 auto_aggregate（旧，不取锁）」并发，二者互不阻塞 → 仍可能产生新的重复事件，刚清理完的存量会被再次污染。
- 由于 Phase 7 scheduler 锁已保证**每个调度周期仅一个实例触发 auto_aggregate**，纯定时聚合不会双触发；残留风险仅存在于「手动 + 自动跨实例并发」场景。

**建议（二选一，进入 Phase 7.5-B 前完成）**：
1. **重载 :8011 新代码**：按 :8000 同法 `taskkill` 后后台重启 `--port 8011`，使两实例均含聚合锁；或
2. **关停 :8011 冗余实例**：与 Phase 7 收口建议一致，单实例部署，彻底消除跨实例竞态。

完成上述任一项后，Phase 7.5-B（事务合并 43 组 A 类重复事件）即可安全启动。

---

## 操作留痕（本次）
- 强杀 :8000 旧进程：PID 38092（已退出）/ 35500（已终止）
- 后台重启 :8000：PID 55732（12:16:45）
- 未执行：任何 DDL/DML、数据删除、:8011 改动、Phase 7.5-B 清理
