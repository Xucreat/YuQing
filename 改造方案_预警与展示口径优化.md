# 改造方案：预警风险分显示 / 批量研判预览 / 国内展示口径

> 范围说明：本文档为改造方案，**未对源码做任何修改**。每一项均给出根因、改动点（文件+函数+代码片段）、风险与验证方式，供评审后实施。
> 三处根因已在前一轮只读诊断中确认，本文不重复论证，只列方案。

---

## 一、预警记录「正式记录风险分 = - 分」回填与展示兜底

### 1.1 改动点一览
- 展示期兜底（立即生效、不写库）：`backend/app/api/alerts.py` → `_alert_record_payload`
- 持久化回填（一次性脚本）：新增 `backend/scripts/backfill_alert_snapshots.py`

### 1.2 方案 A：展示期兜底（推荐先上，零风险）
在 `_alert_record_payload`（`alerts.py:62-67`）计算完 `formal_risk_score` 后，若仍为 `None` 且能取到关联舆情当前风险分，则用其兜底，避免空白：

```python
# alerts.py  _alert_record_payload  现有 62-67 行之后追加
linked = payload.get("linked_opinion_current_risk")
if payload["formal_risk_score"] is None and linked and linked.get("risk_score") is not None:
    payload["formal_risk_score"] = linked["risk_score"]
    payload["formal_risk_level"] = linked.get("risk_level") or row.risk_level
```
- 优点：不触动历史数据，列表/详情立即不再空白。
- 局限：快照列本身仍为旧值，仅影响展示；排序/筛选若依赖 `formal_risk_score` 仍按快照。

### 1.3 方案 B：一次性回填脚本（持久化修正）
针对**两个快照都缺 `risk_score` 键**的历史 `AlertRecord`（新链路 `AlertService.evaluate` 已写入，不会命中），用关联 `Opinion.risk_score` 重建 `rule_risk_snapshot`。

新增 `backend/scripts/backfill_alert_snapshots.py`（要点）：
```python
from app.db.session import SessionLocal
from app.models.alert import AlertRecord
from app.models.opinion import Opinion

def _level(score):
    return "high" if score >= 70 else "medium" if score >= 40 else "low"

def backfill(dry_run=True):
    db = SessionLocal(); fixed = 0
    rows = db.scalars(select(AlertRecord)).all()
    for r in rows:
        rule = r.rule_risk_snapshot or {}
        ai = r.ai_risk_snapshot or {}
        if "risk_score" in rule or "risk_score" in ai:
            continue  # 新数据，跳过
        if not r.opinion_id:
            continue
        op = db.get(Opinion, r.opinion_id)
        if not op or op.risk_score is None:
            continue
        rule["risk_score"] = op.risk_score
        rule["risk_level"] = _level(op.risk_score)
        rule["sentiment"] = op.sentiment
        rule.setdefault("source", "rule")
        r.rule_risk_snapshot = rule
        fixed += 1
        if not dry_run:
            db.flush()
    if not dry_run:
        db.commit()
    print(f"fixed={fixed} dry_run={dry_run}")
```
约束（遵循项目红线）：
- 写库前必须过 `scripts/db_identity_check.py`（生产库 opinions≥100 才算 VERIFIED；测试库置 `DB_IDENTITY_CHECK=off`）。
- 幂等：已含 `risk_score` 的快照跳过；建议先 `python backfill_alert_snapshots.py --dry-run` 核对行数再实跑。

### 1.4 验证
- A：刷新预警记录页，原 `- 分` 行显示关联舆情当前分。
- B：`dry_run` 输出 `fixed=N`；实跑后查询 `SELECT count(*) FROM alert_records WHERE rule_risk_snapshot->>'risk_score' IS NULL AND ai_risk_snapshot->>'risk_score' IS NULL;` 应为 0。

---

## 二、批量 AI 研判预览「可能影响的预警」真实化

### 2.1 改动点一览
- 国内：`backend/app/api/domestic_ai_analysis.py` → `_batch_preview`（`:255` 硬编码 `0`）
- 外网：`backend/app/api/foreign.py` → 批量预览块（`:1296-1315`）

### 2.2 固有局限（必须和产品对齐）
预览发生在**分析之前**，此时 `DomesticAIResult` / `ForeignAIResult` 可能尚不存在。因此预览只能基于**已存在**的 AI/规则风险结果统计，无法预测"本次新研判"将产生的分数。建议预览文案改为「基于已有分析结果的预估」，真实候选数以批量运行结果（`alert_preview`）为准。

### 2.3 国内：替换硬编码为真实统计
新增辅助函数，复用现有候选生成逻辑（`domestic_manual_review_service.generate_domestic_ai_alert_candidates` 的判定），但不落库：

```python
# domestic_ai_analysis.py
def _preview_domestic_alert_count(db, opinion_ids: list[int]) -> int:
    from app.models.domestic_ai_result import DomesticAIResult
    from app.models.alert import AlertRule
    rules = db.scalars(select(AlertRule).where(
        AlertRule.enabled.is_(True), AlertRule.rule_type == "ai_risk_score")).all()
    if not rules:
        return 0
    results = db.scalars(select(DomesticAIResult).where(
        DomesticAIResult.opinion_id.in_(opinion_ids),
        DomesticAIResult.status == "completed")).all()
    by_op = {r.opinion_id: r for r in results}
    cnt = 0
    for rid in opinion_ids:
        r = by_op.get(rid)
        if not r or r.risk_score is None:
            continue
        for rule in rules:
            if r.risk_score >= rule.risk_threshold and _source_kw_match(rule, rid, r):
                cnt += 1; break
    return cnt
```
`_batch_preview` 中把 `"possible_alert_count": 0` 改为 `possible_alert_count = _preview_domestic_alert_count(db, all_ids)`（注意 `_preview_domestic_alert_count` 需先取 `all_ids`；现有函数内 `all_ids` 已定义于 `:220`）。

### 2.4 外网：基于现有 ForeignRiskResult + ForeignAIResult 统计候选
不要在 `evaluate` 内放开 `ai_risk_score`（那会破坏"AI 只生成候选、人工确认才建正式预警"的边界）。改为新增独立的预览统计：

```python
# foreign.py 批量预览块
from app.services.foreign_alert_candidate_service import preview_candidates  # 新增
possible_alert_count = preview_candidates(
    db, opinion_ids=[row.id for row in rows])  # 统计已启用规则(规则风险) + 已启用 ai_risk_score 规则(基于已完成 ForeignAIResult) 的候选数
```
`preview_candidates` 思路：规则风险部分复用 `evaluate(dry_run=True).triggered_count`；AI 部分遍历已启用 `ai_risk_score` 规则 × 已完成 `ForeignAIResult`，满足阈值即计 1。与 `:1296-1315` 现有 `try/except` 结构保持一致（失败回退 0）。

### 2.5 验证
- 国内：选若干**已存在 completed DomesticAIResult 且跨过 ai_risk_score 阈值**的舆情 → 预览数 > 0；无启用 ai_risk_score 规则时为 0（符合预期）。
- 外网：同理，已分析舆情命中启用规则时预览数 > 0。
- 边界：未分析舆情在预览中计 0，但批量运行结果（`alert_preview.candidate_count`）仍反映真实候选 —— 需在前端注明「预览为预估，以运行结果为准」。

---

## 三、国内展示口径（AI / 规则）前端落地

### 3.1 现状（已确认）
- `OpinionOut`（`schemas/opinion.py:80`）**已返回** `ai_risk_score: int = 0`，即国内每条舆情本就带 AI 分。
- 展示口径开关 `foreign-risk-source`（localStorage）**仅外网**实现（`ForeignWorkspace.vue:467-474` / `ForeignOpinionListView.vue:169-176`）。
- 国内 `Opinions.vue` 风险列直接渲染 `row.risk_score`，无任何口径开关。

### 3.2 方案（前端为主，最小改动，推荐）
由于 `ai_risk_score` 已在 payload 中，**纯前端切换即可**，无需后端改接口。镜像外网实现：

`frontend/src/views/Opinions.vue` 增加：
```ts
// script
const riskSource = ref<'current' | 'rule' | 'ai'>(
  window.localStorage.getItem('domestic-risk-source') === 'ai' ? 'ai'
  : window.localStorage.getItem('domestic-risk-source') === 'rule' ? 'rule' : 'current')
function setRiskSource(value: 'current' | 'rule' | 'ai') {
  riskSource.value = value
  window.localStorage.setItem('domestic-risk-source', value)
  loadOpinions()   // 若后端走 risk_source 参数则传参；纯前端切换可不调
}
// 展示取值
function dispRiskScore(row: any) {
  if (riskSource.value === 'ai') {
    const s = row.ai_risk_score
    return (s && s > 0) ? s : row.risk_score   // AI 分为空时回退规则分
  }
  return riskSource.value === 'rule' ? row.risk_score : (row.current_risk_score || row.risk_score)
}
```
模板：在风险列处加一个 `<select v-model="riskSource" @change="setRiskSource(riskSource)">`（选项：持久化当前风险 / 系统规则 / AI 研判），并把风险列 `row.risk_score` 改为 `dispRiskScore(row)`，等级/颜色函数（`levelPill`/`riskColor` 等）直接传该值即可（阈值一致，配色不变）。

可选后端统一（若希望与外网完全一致、由后端返回 `display_risk`）：
- `list_opinions`（`opinions.py:56`）增加 `risk_source: str | None = None` 参数，返回时附带 `display_risk = {source, risk_score, risk_level, sentiment}`（ai 取 `ai_risk_score`，rule/current 取 `risk_score`/`current_risk_score`）。前端读取 `row.display_risk.risk_score`。此改动与前端切换二选一，不必同时做。

### 3.3 回退与一致性
- `ai_risk_score` 为 0/None（从未 AI 研判）→ 自动回退 `risk_score`，避免显示 0 分误导。
- 该开关**仅作用域国内**，不影响外网 `foreign-risk-source`。

### 3.4 验证
- 进入国内舆情列表 → 切换「AI 研判」→ 已研判舆情显示 `ai_risk_score`，未研判回退规则分；刷新后 localStorage 保持。
- 确认外网列表行为不受影响（独立 key `foreign-risk-source`）。
- `vite build` 后部署到 `backend/app/static`（遵循项目部署链：`cd frontend && vite build` → 拷贝 dist）。

---

## 风险与回滚
1. 一/三为展示层修改，不破坏数据；前端需重新构建部署（OOM 用 `node --max-old-space-size=1400`）。
2. 二为接口逻辑：国内替换硬编码为统计，外网新增预览函数并保留 `try/except` 兜底，失败仍回退 0，不引发 500。
3. 回填脚本（1.3）写库，必须过身份校验、先 dry-run；可整表回滚（快照重建前建议 `pg_dump` 告警表）。

## 建议实施顺序
**三（前端，立竿见影、低风险）→ 一A（展示兜底，立即消除空白）→ 二（预览真实化）→ 一B（回填持久化，择期）**。
如需我先实现其中任一项，告知具体编号即可。
