# 微博八爪鱼生产模式接入实施报告

## 1. 修改文件

- `backend/app/collectors/weibo_octopus_collector.py`
  - 默认确认接口改为官方 `POST /data/markexported`。
  - `BAZHU_FETCH_SIZE` 继续以 1000 为默认值。
  - `ack_pending_export()` 不再因 `total > returned` 拒绝确认；确认仍只在存在本批待确认 token 时发生。
- `backend/app/collectors/service.py`
  - 保持 `fetch -> Opinion 入库 -> 规则分析/RiskEngine -> markexported` 顺序。
  - 入库异常、分析/RiskEngine 异常或确认接口异常均不丢失失败状态。
  - 微博运行结果增加 `duplicate`，并将 ack 状态规范为 `success/failed/deferred`。
  - 增加微博消费完成日志：`task_id/upstream_total/upstream_returned/created/duplicate/failed/analyzed/ack_status/warning_reason/duration`。
- `backend/app/models/collector_run.py`
  - 增加必要的 `duplicate` 统计列。
- `backend/alembic/versions/p23_collector_run_duplicate.py`
  - 为现有 `collector_runs` 增加 `duplicate integer not null default 0`，不新增状态表、游标表或队列表。
- `backend/app/api/admin_data_sources.py`、`backend/app/api/sources.py`、`backend/app/api/collector.py`、`backend/app/schemas/collector.py`
  - 将 `duplicate`、upstream 与 ack 统计透传到已有运行日志/任务结果接口。
- `backend/app/core/scheduler.py`
  - 保持微博独立任务，并扩充定时微博日志字段。
- `backend/tests/test_weibo_octopus_collector.py`
  - 更新 partial queue 语义测试，增加重复计数和 `total > returned` 仍确认且记录 warning 的测试。

## 2. 消费流程

```text
八爪鱼 /data/notexported (size=1000)
        -> 字段映射与微博评论过滤
        -> external_id/url 幂等检查
        -> Opinion 入库并提交
        -> 规则分析 + RiskEngine 写回并提交
        -> 成功：POST /data/markexported
```

`collector_main` 仍通过 `exclude_data_source_keys={"weibo_octopus"}` 排除微博；`weibo_consumer` 仍通过 `include_data_source_keys={"weibo_octopus"}` 独立消费，新闻和政务采集不受微博异常影响。

## 3. ack 策略

- 获取 HTTP 失败：不调用 markexported，`ack_status=failed`。
- Opinion 入库提交失败：不调用 markexported，`ack_status=failed/deferred`（按失败发生阶段记录）。
- 规则分析或 RiskEngine 失败：本批不确认，`ack_status=deferred`。
- markexported 自身失败：`ack_status=failed`，保留上游数据等待重试。
- `total > returned` 且本批处理全部成功：允许确认，`ack_status=success`，运行记录/日志写入 `partial_queue_accepted` warning。该 warning 只表示本次批次已成功消费，不表示上游全部历史未导出数据已经同步完成。

## 4. 测试结果

已执行：

- Python 编译检查：通过。
- 微博接口、映射、去重、partial queue ack 测试：`13 passed`。
- 微博调度任务注册与数据源隔离测试：`1 passed`。

新增/保留的集成断言覆盖：正常确认、重复微博不重复创建 Opinion、入库异常不确认、RiskEngine 异常不确认、微博异常不阻断新闻采集。当前工作区的测试库是手工初始化且没有 Alembic 版本表，完整数据库集成用例需先在 CI/目标测试库执行 `p23` 迁移后再跑全量；未将该环境阻塞误报为通过。

## 5. 生产开启步骤

1. 备份并确认目标数据库身份。
2. 执行 `alembic upgrade head`，确认 `collector_runs.duplicate` 存在。
3. 配置生产环境：`WEIBO_ENABLED=true`、`BAZHU_API_KEY`（或用户名密码）、`BAZHU_TASK_ID`、`BAZHU_FETCH_SIZE=1000`。
4. 保持 `collector_main` 排除 `weibo_octopus`，确认 `weibo_consumer` 每小时独立运行。
5. 重启后端，先观察一小时日志和 `collection-logs`，再进入连续试运行。

## 6. 五小时验收方案

每小时记录一次 `upstream_total/upstream_returned/created/duplicate/failed/analyzed/ack_status`，并核对八爪鱼任务待导出数量。

1. 第 1 小时：确认 fetch size=1000、正常批次入库、`ack_status=success`。
2. 第 2 小时：注入/观察重复微博，确认 `duplicate` 增长且 Opinion 数不重复增长。
3. 第 3 小时：验证 `total > returned`，确认有 `partial_queue_accepted` warning 且仍 markexported。
4. 第 4 小时：观察一次入库或 RiskEngine 失败演练，确认不 markexported、`ack_status=deferred/failed`，下一轮可恢复。
5. 第 5 小时：核对新闻/政务任务持续成功、微博日志完整、Event/Alert 现有增量链路无异常；满足后再保持生产配置。
