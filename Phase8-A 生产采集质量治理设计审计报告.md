# Phase8-A 生产采集质量治理设计审计报告

审计性质：只读审计、分析、报告。  
审计时间：2026-07-29 15:49（Asia/Shanghai）。  
审计窗口：2026-07-22 14:36:00 至 2026-07-29 14:36:00（左闭右开）。  
数据库：`opinion_db`，PostgreSQL 16.6。  
数据库身份：`system_identifier=7663057120701798896`，与生产身份基线一致。

本阶段仅执行了数据库 `SELECT`、代码/配置读取和站点 HTTP GET 可达性核查。未修改代码、数据库数据、数据库结构、关键词、配置、采集策略、测试、RiskEngine、Alert 或 Event。

## 1. 审计结论摘要

1. 国家级源的 Option C 已真实生效，但当前窗口 347 条新增中有 343 条为“无地域命中、仅主题命中”，占 98.85%。这证明当前方案的主题兜底召回非常强，也证明它不能把“主题命中”直接解释为“廊坊本地舆情”。
2. 343 条仅主题命中中，教育 154 条、民生 90 条、交通 73 条、医疗 50 条为主要贡献者；四个宽泛词合计产生 367 次文档命中，因一篇文章可命中多个词，不能相加为文章数，但噪声集中位置清晰。
3. 对既有 20 条人工样本的判断为 A=3、B=13、C=4。A 表示可作为治理参考，不表示已确认是廊坊本地事件；B/C 合计 85%，说明当前主题兜底的主要风险是外地/全国内容，而不是抓取链路失效。
4. 霸州市政府网 223 次运行全部 `success` 且 `fetched_raw=0`。现场 GET 在证书校验和 `verify=False` 下均在 TLS 握手阶段 EOF；HTTP 只返回 301 到 HTTPS。因此当前证据支持 C（TLS/网络可达性）为主因，暂不支持 A/B。问题等级建议定为 P1（单源持续盲区），不是全局 P0。
5. 大厂县政府网站的 9 次连续失败全部由同一调用契约错误造成：`GovernmentCollector.fetch()` 不接受当时运行进程传入的 `region_kw`。当前工作树中的实现已经接受该参数，后续运行恢复为 `fetched_raw=20`、`status=success`。事件已恢复，但运行版本一致性仍需修复/验证，残余风险为 P2，历史事故按 P1 处理。
6. 不建议在本审计阶段直接切换到 C+ 或 C++。建议先保留当前 Option C 作为可比基线，完成逐源健康指标、标题/正文命中拆分和地域别名灰度评估，再决定策略。

## 2. 证据与统计口径

### 2.1 数据库与运行口径

- `collector_runs`：按 `start_time` 统计；`success` 只表示本次调用未抛出采集器级异常，不表示 `fetched_raw>0` 或 `created>0`。
- `opinions`：按 `created_at` 统计；国家级源对象为 `source in ('新华网','人民网','中国新闻网')`。
- 国家级命中文本：`title + content[:800]`，与当前 collector 实现一致。
- 地域/主题词：`keywords` 表中 `type='monitoring' AND is_enabled=true`，再按 `category='地域'`/`category='主题'` 分组。
- “仅主题命中”：至少命中一个主题词，且没有命中任何地域词。一个文档命中多个主题词时，各词分别计数，因此逐词合计可能大于文档总数。
- `合理占比`：人工抽样估计“对本地治理监测仍有直接候选价值”的比例，不等同于“已确认廊坊本地”。全量 343 条未做逐条人工标注；每词样本不足时只给风险等级和范围，不伪造精确比例。

### 2.2 固定窗口总览

|指标|结果|
|-|-|
|collector_runs|3,439|
|success|3,426|
|failed|13|
|warning/partial/running|0/0/0|
|success rate|99.62%|
|国家级源新增|347|
|其中地域命中|4|
|其中仅主题命中|343|
|仅主题命中占国家级新增|98.85%|

审计后继续运行的实时快照（2026-07-29 15:49 左右）为国家级源 350 条、仅主题命中 346 条；这只是新增批次导致的窗口漂移，不改变固定窗口结论。

## 3. 国家级源主题命中审计

### 3.1 分源结果

|源|窗口新增|地域命中|仅主题命中|仅主题占本源|
|-|-|-|-|-|
|新华网|89|1|88|98.88%|
|人民网|133|2|131|98.50%|
|中国新闻网|125|1|124|99.20%|
|合计|347|4|343|98.85%|

三个国家级源的 `region_id` 均绑定为廊坊市，但正文地域词命中只有 4 条。`region_id` 是采集源的区域绑定结果，不是文章正文已出现廊坊地域信号的证明。

### 3.2 仅主题命中逐词贡献

|关键词|命中数量|占343条|合理占比（抽样估计）|噪声风险|建议|
|-|-|-|-|-|-|
|教育|154|44.90%|低于20%|很高|作为观察/政策参考词单独留存，避免作为国家级源无条件放行依据|
|民生|90|26.24%|约20%-35%|高|需叠加地域、对象或具体事件条件；不宜单独兜底|
|交通|73|21.28%|约10%-25%|高|区分交通事故/管制/运输治理与一般行业新闻|
|医疗|50|14.58%|低于20%|高|需增加本地对象、地点或公共卫生事件条件；境外援助和全国公益内容噪声明显|
|消防|25|7.29%|约20%-40%|中高|保留价值高于宽泛词，但需识别火灾救援、演练、消防宣传和外地事件|
|环保|12|3.50%|约20%-40%|中高|保留观察，要求地点/项目/污染事件等附加信号|
|投诉|6|1.75%|低于20%|高|消费、海外政治和平台治理语境会误命中；不宜单独放行|
|食品安全|2|0.58%|低于20%|中高|主题本身有治理价值，但本窗口两条均为外地/境外政治语境，需地点或监管对象条件|
|城管|2|0.58%|低于20%|高|样本量小，存在“城市管理”泛化或非城管语境命中，先观察|
|安全事故|1|0.29%|低于20%|中高|本窗口为汛期避险指南，未证明廊坊关联；需与事故类型/地点条件结合|
|舆情|1|0.29%|低于20%|高|“负面舆情”等元话语易命中，不应单独作为内容相关性证明|
|安全生产|0|0.00%|无样本|暂无法评估|当前窗口无贡献，先保持，不据此删除|
|征地|0|0.00%|无样本|暂无法评估|当前窗口无贡献，先保持，不据此删除|
|拆迁|0|0.00%|无样本|暂无法评估|当前窗口无贡献，先保持，不据此删除|

说明：命中数是文档数，不是字符串出现次数。逐词命中存在重叠，例如同一篇文章同时命中 `民生、教育、医疗`。本窗口仅主题命中组合中，单独命中教育 120 条、交通 56 条、民生 55 条、医疗 25 条、消防 21 条，是主要噪声来源。

### 3.3 人工合理性判断

既有报告对最近 20 条仅主题样本的人工判断为 A=3、B=13、C=4：

- A：主题与地方治理关注面相关，可作政策/治理参考，但不代表廊坊本地事件。
- B：主题相关，但对象、地点或场景明显外地，需要二次人工判断。
- C：主题词仅在背景或偶然语境出现，和本地监测目标关系弱。

代表性噪声包括：贵州/云南/福建/浙江等地教育或民生事件、广东台风交通管制、阿尔及利亚医疗公益活动、股票行情中的行业词。代表性保留价值包括法治教育、公共治理、消防防灾等可供本地治理参考的政策或方法类内容。

因此，“合理占比”不能按主题词权重直接换算。宽泛词命中量越大，越应优先做上下文抽样；不能仅因为某词的治理语义重要，就把全国性文章当作本地事件召回。

## 4. 国家级源主题策略评估

### 4.1 三方案观测下界

|方案|规则|本窗口观测保留量|召回影响|噪声影响|实施复杂度|
|-|-|-|-|-|-|
|Option C 当前|地域 OR 全量主题|347/347；其中仅主题343条|观测候选召回最高；但“候选召回”不等于本地有效召回|最高，98.85%为无地域主题兜底|低，当前已上线|
|Option C+|地域 OR 核心主题|按题设核心词严格计算：4条地域 + 39条仅命中 `消防/环保/食品安全` = 约43/347；若把现有 `安全事故`也归入核心，则约44条|会保留地域命中和安全/防灾类候选；会丢弃约304条当前仅由宽泛主题保留的候选，真实本地召回损失无法由当前数据证明|明显下降，但不能假设被丢弃的教育/民生/医疗/交通全部是噪声|中低：需要核心词分层、逐源比较、灰度与回滚口径|
|Option C++|标题命中主题直接进入；正文命中主题需额外条件|347条中标题命中任一主题 88条；地域命中 4条；`地域 OR 标题主题`约91条；另有约256条为正文主题但标题未命中，须由额外条件决定|比 C 更关注标题主题主体，可能保留高信号事件标题；正文-only 本地事件可能被压缩|通常低于 C，但取决于“额外条件”；条件过严会漏掉标题概括、正文才出现地点/对象的事件|中高：需稳定分离标题/正文、定义额外条件、增加解释字段/测试和灰度验证|

上述 C+ 数值是当前词库和当前 343 条样本的观测下界，不是未来策略的真实召回率。题设中的 `应急`、`防灾减灾`目前不是启用主题词，因此无法从历史数据直接计算其贡献。

### 4.2 对三个方案的建议

- 对 Option C：建议暂时保持为基线，用于连续观察和对比；不要把当前高成功率解释为高质量，也不要在没有分源指标的情况下继续扩大主题词。
- 对 Option C+：建议作为优先灰度评估方向。它最容易形成可解释的核心主题层，适合先比较“国家级源有效密度”和人工噪声率；本审计不直接切换。
- 对 Option C++：建议作为第二阶段实验方向。它更符合“标题中的主题通常是文章主体”的直觉，但“正文命中需要额外条件”尚未定义，不能在规则未定义时直接上线。
- 不建议：直接删除教育、民生、医疗、交通；直接新增独立宽泛词 `安全`；只按命中数量决定词的去留；用 RiskEngine、Alert 或 Event 事后弥补采集准入层的噪声。

## 5. 地域词覆盖审计

### 5.1 当前词与目标地域覆盖

当前启用地域词 16 个：

`廊坊、大厂、大厂回族自治县、三河、香河、固安、广阳区、安次区、霸州市、永清县、大城县、文安县、文安、霸州、广阳、安次`

|目标地域|当前覆盖|证据/问题|
|-|-|-|
|廊坊市|是（语义覆盖）|当前词为 `廊坊`，可匹配“廊坊市”；没有精确词 `廊坊市`|
|大厂|是|存在 `大厂` 与 `大厂回族自治县`|
|三河|是|存在 `三河`|
|燕郊|否|当前没有该常用片区名；仅有 `三河`，对只写“燕郊”的文章不能保证命中|
|香河|是|存在 `香河`|
|固安|是|存在 `固安`|
|永清|是|存在 `永清县`，包含 `永清`|
|霸州|是|存在 `霸州` 与 `霸州市`|
|文安|是|存在 `文安` 与 `文安县`|
|大城|是|存在 `大城县`，包含 `大城`|

“覆盖”仅指字符串层面的常用行政区/片区信号，不代表乡镇、街道、园区和重点项目覆盖。当前国家级源对正文做简单包含匹配，也没有同义词、别名或行政层级推理。

### 5.2 燕郊与胜芳新增必要性

|词|新增必要性|理由|
|-|-|-|
|燕郊|高|三河市下辖高频常用名称，新闻和社交内容经常只写“燕郊”而不写“三河”；当前国家级源地域 OR 主题链路会因缺少该别名而漏掉仅含“燕郊”的本地候选。应先做只读命中回溯/样本核验，再进入关键词变更审批。|
|胜芳|中|霸州市重要镇域和常用地名，存在单写“胜芳”而不写“霸州”的内容；但相对燕郊，当前业务监测目标和窗口产出对其必要性证据较弱，建议先做来源、历史标题和重点事件回溯。|

总体判断：重点片区别名覆盖缺口为中高风险；燕郊优先级高于胜芳。审计阶段不新增关键词。

## 6. 霸州市政府网异常分析

### 6.1 运行证据

固定窗口内：

- `霸州市政府网` 223 次运行；223 次 `status=success`。
- 223 次 `fetched_raw=0`、`created=0`。
- `collector_runs.error_msg` 全部为空；仓库中未发现独立的持久化请求日志表/文件可用于还原每次 HTTP 细节。
- `data_sources` 配置：`list_urls=["https://www.bazhou.gov.cn"]`，显式关键词为 `霸州,廊坊,河北`，正文选择器为 `div.nr`，`max_articles=8`，类为 `GenericSiteCollector`。

### 6.2 现场只读 HTTP 核查

同一审计环境对配置 URL 发起 GET：

- `https://www.bazhou.gov.cn`：证书校验开启时 `SSLEOFError: UNEXPECTED_EOF_WHILE_READING`。
- `https://www.bazhou.gov.cn`：`verify=False` 仍为同一 TLS EOF，排除单纯证书校验失败。
- `http://www.bazhou.gov.cn`：返回 `301 Location: https://www.bazhou.gov.cn/`；跟随跳转后仍在 TLS 握手失败。
- `https://bazhou.gov.cn` 与 HTTP 变体结果相同方向。

因此当前顺序判断为：

1. C：TLS/网络/站点 HTTPS 服务异常，最符合现有证据。
2. D：配置的 HTTP/HTTPS 入口可能需要核验，但 HTTP 已明确重定向到 HTTPS，不能仅据此认定 URL 填错。
3. A：网站无内容，不符合“列表页都无法完成 TLS 握手”的证据。
4. B：解析器失效，目前尚未拿到 HTML，无法验证 `link_rule` 或 `div.nr`；不能排除未来 TLS 恢复后仍存在解析问题，但不是当前零抓取的首要原因。

### 6.3 问题等级与建议

问题等级：**P1（单源持续不可用造成区域盲区）**。不是全局采集 P0，但 223 次连续零抓取已超过正常短时波动范围。

是否需要修复：**需要**。建议顺序是域名解析/出口、TLS 证书与协议、反向代理和站点 HTTPS 服务核验；网络恢复后再验证列表链接数量、详情页正文和 `div.nr` 选择器。当前不应先修改解析器或关键词，也不应将 `success + fetched_raw=0` 视为健康。

## 7. 大厂县政府网站稳定性分析

### 7.1 最近失败类型

固定窗口内 `大厂县政府网站` 共 230 次：成功 221、失败 9。9 次失败的 `error_msg` 完全一致：

`TypeError: GovernmentCollector.fetch() got an unexpected keyword argument 'region_kw'`

失败集中在 2026-07-28 19:00 至 22:30 左右，随后运行恢复；截至 2026-07-29 15:30 的最近运行均为 `status=success`、`fetched_raw=20`。当前工作树代码中 `GovernmentCollector.fetch(self, keywords=None, region_kw=None, topic_kw=None)` 已接受统一参数；`CollectorService` 仍会以 `keywords=..., region_kw=..., topic_kw=...` 调用所有 collector。

### 7.2 判断

- 根因是采集服务与运行时加载的 collector 版本不一致/旧进程未重载，属于 fetch 调用签名契约问题。
- 不是 TLS、目标站点无内容或关键词命中导致的失败；错误发生在进入 fetch 业务逻辑之前。
- 当前站点抓取已恢复，最近连续成功运行每次抓到 20 条原始内容，短期稳定性已有证据。
- 仍存在部署漂移风险：若多实例、旧进程或发布后未重启，统一调用契约可能再次失配。

问题判断：历史事故 **P1**；当前残余风险 **P2**。  
是否需要修复：**需要做运行版本一致性修复/验证**，包括确认生产进程实际加载的模块版本、发布后重启/健康检查和统一 fetch 签名回归验证；不需要借此修改 match_mode、RiskEngine、Alert 或 Event。

## 8. 生产治理优先级

评分依据：收益看能否恢复有效覆盖或降低误报；风险看是否形成持续盲区/运行状态误判；改动范围按网络核验、运行发布、观测指标、策略灰度分别估计。

|问题|收益|风险|改动范围|优先级|
|-|-|-|-|-|
|霸州市政府网 TLS/HTTPS 持续不可达|恢复霸州来源覆盖|高，223次连续零抓取|中：网络/证书/站点协同，恢复后再做解析验收|P1，立即核查|
|大厂县政府网旧进程 fetch 签名失配|避免本地核心源再次中断|高，已出现9次连续失败|小到中：运行版本、发布重启、健康检查|P1，立即收口|
|国家级源主题兜底噪声|显著提高有效新增密度、降低人工筛查成本|高，343条中大部分无地域信号|中：词分层、分源灰度、回滚和人工抽样|P1，先评估后决策|
|`success` 掩盖 `fetched_raw=0/created=0`|尽早发现空抓取、重复抓取和解析退化|中高，当前健康率被高估|小：指标/告警/日志口径，不改数据库结构|P1，建议立即补监控口径|
|燕郊地域别名缺口|提高三河重点片区召回|中高，可能漏掉只写燕郊的内容|小：关键词变更与灰度验收|P2，先做回溯|
|胜芳地域别名缺口|提高霸州重点镇域召回|中，必要性待历史数据验证|小：关键词变更与灰度验收|P2，先做回溯|
|C++ 标题/正文分层|可能进一步降低正文偶然命中|中，条件过严会漏召回|中：规则定义、字段解释、测试、灰度|P2，先做离线对比|
|直接扩展 AI、ES/Redis 或修改 RiskEngine/Alert/Event|对本次采集准入问题收益不直接|高，扩大变更面并掩盖根因|大|不做|

## 9. 立即修、保持和不要做

### 立即修/立即核查

- 霸州市政府网：先修复或确认 TLS/HTTPS 可达性，再验证列表和正文解析。
- 大厂县政府网站：完成生产运行时与工作树版本一致性核查，防止 fetch 签名漂移复发。
- 采集质量指标：至少区分运行成功率、非零抓取率、非零新增率、连续失败次数和有效新增率；空抓取应进入可观测异常，而不是普通 success。

### 可以保持

- 国家级源的 Option C 代码实现作为当前对比基线保持。
- `region_or_topic` 的边界仍仅限新华网、人民网、中国新闻网；不扩散到政府源或 GenericSiteCollector。
- 当前 RiskEngine、Alert、Event 及数据库结构保持不变。
- `教育、民生、医疗、交通` 不因单一窗口的高命中量立即删除；先保留为对比数据，避免不可逆地损失潜在治理参考内容。

### 不要做

- 不要直接把 C 切成 C+ 或 C++，不做无灰度的全量策略切换。
- 不要把 `success_rate` 当成有效采集质量，也不要把 `created=0` 直接等同于解析失败。
- 不要在当前证据不足时先改霸州 `content_selectors`；当前首要失败发生在 TLS 之前。
- 不要新增独立宽泛词 `安全` 来解决“安全”覆盖；当前已有复合主题词和敏感词包含关系，扩词会放大噪声。
- 不要用 AI、RiskEngine、Alert 或 Event 的下游逻辑替代采集准入治理。
- 不要引入 ES、Redis、队列等基础设施解决当前三个具体问题。

## 10. 后续只读验证建议

1. 对 C+/C++ 做离线重放：使用同一固定窗口，输出按源、按词、按标题/正文的保留量、重复量、人工噪声率和风险事件保留量。
2. 对国家级源建立至少一个新的完整窗口样本，按词分层抽样，不再只抽最近 20 条；特别关注教育、民生、交通、医疗。
3. 对地域词做命中回溯：检索现有标题/正文中 `燕郊`、`胜芳` 的历史出现次数，分别统计来源、是否已有 `三河`/`霸州`伴随词，再提交关键词变更评审。
4. 霸州在传输恢复后做三段式验收：列表页 HTTP、候选链接数量、详情页正文长度/选择器命中；三段都通过后再判断是否存在解析器问题。
5. 大厂在发布/重启后连续观察至少 10 个调度周期，确认无签名错误且 `fetched_raw`、`created`、`status` 三者分布正常。

## 11. 代码与配置依据

- `backend/app/collectors/common.py`：`http_get` 对网络/HTTP 异常返回 `None`；`matches_region_topic` 对国家级源执行 `region_or_topic`。
- `backend/app/collectors/xinhua_collector.py`、`people_collector.py`、`chinanews_collector.py`：使用 `title + content[:800]`，并传入 `match_mode="region_or_topic"`。
- `backend/app/collectors/generic_site.py`：读取 `config_json` 的 `list_urls`、`link_rule`、`content_selectors` 和显式 `keywords`；列表抓取为空时最终返回空列表。
- `backend/app/collectors/base_http.py`：GenericSiteCollector 使用统一 HTTP GET、重试和正文降级解析。
- `backend/app/collectors/service.py`：统一向 collector 传入 `keywords`、`region_kw`、`topic_kw`；collector 级异常写入 `CollectorRun.status='failed'`，普通空列表仍可被记为 `success`。
- `backend/app/collectors/government_collector.py`：大厂政府源当前 `fetch` 签名已兼容统一参数，且维持政府源全量采集。
- 生产数据库：`keywords`、`data_sources`、`collector_runs`、`opinions`、`regions` 只读查询。

## 12. 最终结论

当前生产系统不存在已证实的全局采集链路中断，P0 暂无。但国家级源的 Option C 已暴露出明显的质量治理问题：它把主题命中转化为高比例全国/外地候选，尤其集中在教育、民生、交通、医疗四个宽泛词。

下一阶段应优先处理两个事实性故障：霸州 HTTPS/TLS 单源盲区、大厂运行时 fetch 契约漂移；同时建立国家级源 C+/C++ 的离线可比基线。策略是否切换应等待这三类证据完成后再确认。本报告不直接选择 Option C、C+ 或 C++，并在确认前保持只读状态。

