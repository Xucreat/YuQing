# 采集幂等性加固 · 生产实施报告

- **实施时间**：2026-07-24 10:29 ~ 10:45
- **目标库**：opinion_db @ 127.0.0.1:5432（生产，身份门禁 VERIFIED）
- **实施范围**：重复数据清理 + 唯一约束迁移（p6/p7）+ 传播树重建 + 调度器单实例验证
- **结论**：✅ **全部 7 步成功，零失败，零回滚**

---

## 步骤 0：执行前确认

| 检查项 | 结果 |
|---|---|
| 数据库身份门禁（db_identity_check.py） | **VERIFIED**（exit 0）：opinion_db，system_identifier=7663057120701798896，opinions=1000 |
| alembic current → head | `collrunbatch001` → `p7evtuniq01`（待升 p6+p7 两级，迁移链无意外中间版本） |
| 后端实例 | 2 个（:8000 PID 6016、:8011 PID 9868），均为 10:14 重启、已加载 advisory lock 新代码 |
| 采集窗口 | 10:30 批次已结束（22 源全 success）后才开始清理，无写入竞争 |

## 步骤 1：备份生产数据库 ✅

- 文件：`YQ/backup/opinion_db_pre_cleanup_20260724_103142.dump`
- 大小：1,035,368 字节（pg_dump custom 格式）
- 完整性验证：`pg_restore --list` 共 18 个 TABLE DATA 段，opinions / event_opinions / propagation_nodes / alert_records 均在

## 步骤 2~4：清理重复数据（单事务，验证后 COMMIT）✅

执行方式：受控事务脚本（等价于 `cleanup_duplicate_opinions.sql` 全部步骤），验证查询全 0 才 COMMIT，否则自动 ROLLBACK。

| 操作 | 行数 |
|---|---|
| 预览：待删冗余舆情 | 34（较演练期 33 多 1，为清理前新采集产生，符合预期） |
| 0.5 event_opinions 并回 keep | 0（keep 侧关联均已存在，**无事件关联丢失**） |
| 1 删除 alert_records | 5 |
| 0.6 propagation_nodes.parent_id 置 NULL（断自引用） | 6 |
| 删除 propagation_nodes | 61 |
| 删除 event_opinions（重复舆情侧） | 63 |
| 2 删除 event_opinions 字面重复 | 10 |
| 3 删除冗余舆情 | **34** |

**COMMIT 前验证（全部为 0 才提交）**：

| 验证项 | 结果 |
|---|---|
| exact_url_dups | 0 ✅ |
| event_opinion_dups | 0 ✅ |
| dangling_parent_fk（传播节点悬空父引用） | 0 ✅ |
| remaining_delete_set（删除集残留） | 0 ✅ |
| **事务结果** | **COMMIT** |

## 步骤 5：alembic upgrade head ✅

- 迁移前经数据库身份门禁再次校验（VERIFIED）
- current：`p7evtuniq01 (head)`
- 约束落地验证：
  - `ix_opinions_url_unique`（opinions.url 部分唯一索引，WHERE url<>''）✅ 存在
  - `uq_event_opinions_event_opinion`（event_opinions(event_id, opinion_id) 唯一约束）✅ 存在

自此，**同一 URL 舆情重复入库、同一事件重复关联同一舆情，在数据库层被物理禁止**。

## 步骤 6：重建受影响事件传播树 ✅

- 受影响事件：**64 个**（删除前从 event_opinions 捕获，含字面重复事件）
  - ID：146,193,208,210,220,223,238,256,260,262-267,269-274,277,279-286,288-297,299,300,313-329,331-333,343,348
- 执行：逐事件 `PropagationService.rebuild_for_event`，每事件独立提交
- 结果：**ok=64，fail=0**
- 重建后验证：悬空父引用=0，传播节点总数 660

## 步骤 7：scheduler 单实例验证 ✅

| 证据 | 结果 |
|---|---|
| pg_locks advisory lock 持有者 | **恰好 1 个**（会话 pid 69104，10:14:27 建立，即 :8000 实例） |
| 09:30 / 10:00 批次（旧状态） | 各 **2 批**（双实例双调度，间隔 <1ms） |
| **10:30 批次（锁生效后首个整点）** | **仅 1 批**（batch 9d975837…，22 源全 success） |

结论：两实例并存的情况下，advisory lock 已确保**只有一个调度器实际触发采集**，双触发问题在生产上消除。

## 遗留事项与建议

1. **:8011 实例（PID 9868/36348）建议择机关停**——调度已单例化无双触发风险，但双实例仍多耗资源且 API 写路径存在理论并发（现有 url 唯一索引已兜底）。
2. 备份文件保留至少 7 天：`backup/opinion_db_pre_cleanup_20260724_103142.dump`。
3. 后续采集若出现 URL 冲突报错日志，属唯一索引正常拦截行为（采集层 `common.py` 已有先查后插，冲突概率低）。
4. 回滚预案（如需）：`pg_restore` 该备份 + `alembic downgrade collrunbatch001`。

## 变更清单

- 数据：opinions -34、event_opinions -73、propagation_nodes -61(重建后 660)、alert_records -5
- 结构：+2 唯一约束（p6urluniq01、p7evtuniq01）
- 代码：无业务代码改动（scheduler advisory lock 为此前已部署代码，本次仅验证生效）
- 临时脚本：已全部删除
