# 通山县乡镇政府信息公开平台 — 数据源接入配置说明

> 生成日期：2026-08-07
> 配套配置文件：`通山县乡镇数据源接入配置.json`（12 个源，JSON 数组，沿用现有 `generic_site` / `news_site` schema）
> 县级主站：`http://www.tongshan.gov.cn/`（域名 gov.cn，网站标识码 4212240017）

---

## 一、总体架构（重要）

通山县采用「**县级门户统一承载 + 乡镇信息公开子平台**」模式：

- **县本级**有独立政府网站（`tongshan.gov.cn`）。
- **12 个乡镇没有各自独立的 .gov.cn 域名**，全部作为子栏目挂在县站下：`/zc/dfbmptlj/xzxxgkpt/<乡镇基码>/`。
- **187 个行政村没有官方政府网站**，无法从政府官网直接抓取村级信息。

因此，监控通山县各级政务信息 = 主站 1 个域名 + 12 个乡镇子平台。

## 二、12 个乡镇子平台入口（均已实测 HTTP 200）

| 乡镇 | 子平台基码 | fdzdgknr | 政府工作机构(jgjj) | 财政资金(czzj) | 规划信息(ghxx) | key |
|---|---|---|---|---|---|---|
| 通羊镇 | tyz_22381 | 22386 | 22387 | 22389 | ghxx(固定) | tongshan_tongyang |
| 南林桥镇 | nlqz_22391 | 22396 | 22397 | 22399 | ghxx(固定) | tongshan_nanlinqiao |
| 黄沙铺镇 | hspz_22401 | 22406 | 22407 | 22409 | ghxx(固定) | tongshan_huangshapu |
| 厦铺镇 | spz_22411 | 22416 | 22417 | 22419 | ghxx(固定) | tongshan_xiapu |
| 九宫山镇 | jgsz_22421 | 22426 | 22427 | 22429 | ghxx(固定) | tongshan_jiugongshan |
| 闯王镇 | cwz_22431 | 22436 | 22437 | 22439 | ghxx(固定) | tongshan_chuangwang |
| 洪港镇 | hgz_22441 | 22446 | 22447 | 22449 | ghxx(固定) | tongshan_honggang |
| 大畈镇 | dfz_22451 | 22456 | 22457 | 22459 | ghxx(固定) | tongshan_dafan |
| 大路乡 | dlx_22461 | 22466 | 22467 | 22469 | ghxx(固定) | tongshan_dalu |
| 燕厦乡 | ysx_22471 | 22476 | 22477 | 22479 | ghxx(固定) | tongshan_yanxia |
| 慈口乡 | ckx_22481 | 22487 | 22489 | 22494 | ghxx(固定) | tongshan_cikou |
| 杨芳林乡 | yflx_22498 | 22509 | 22511 | 22516 | ghxx(固定) | tongshan_yangfanglin |

> ⚠️ **基码不连续，不能机械 +5/+6 推算**：慈口乡（基码 22481→fdz 22487，偏移 +6）、杨芳林乡（基码 22498→fdz 22509，偏移 +11）均打破前 10 个乡镇的 `+5` 规律；慈口乡/杨芳林乡的 czzj 也不等于 jgjj+2。上表数值为**逐个抓首页跳转 + fdz 跳转页实测核实**，非推算。

## 三、采集技术要点（务必注意）

1. **WAF 防护**：站点前置防火墙（响应头 `Wzws-Ray`），会对无 UA / 异常 UA 的抓取拦截。
   - 采集器请求必须带浏览器 UA（如 `Mozilla/5.0 ... Chrome/120.0 Safari/537.36`），否则首页返回空或 403。
   - 实测：`curl` 带浏览器 UA 可稳定返回 200；本机 WebFetch 直连被拦（fetch failed）。

2. **JS 跳转，采集器不能依赖浏览器渲染**：
   - 子平台首页 `.../<基码>/` 仅 83 字节，内容为 `window.location="./fdzdgknr_NNNNN/"`；
   - `fdzdgknr_NNNNN/` 再跳 `./jgjj_NNNNN/`。
   - 因此配置里的 `list_urls` 已**直接填入解析后的终页**（jgjj / czzj / ghxx 落地页），无需采集器执行 JS。

3. **文章链接规律**：详情形如 `.../jgjj_22387/202603/t20260326_4357185.shtml`。
   - `link_rule.href_regex` = `t\d{8}_\d+\.shtml$` 可精准命中。

4. **正文容器**：实测为 `<font id="Zoom">...</font>`。
   - `content_selectors` 已设为 `["#Zoom","font#Zoom","div.TRS_Editor","div.article","div.content"]`，前两项为精准命中，后三项是通用兜底。

5. **栏目选择**：每个源默认监控 3 个高频政务公开栏目（已全量实测 200）：
   - `jgjj` 政府工作机构（含机构简介/职能）
   - `czzj` 财政资金（预决算等，舆情价值高）
   - `ghxx` 规划信息（固定 slug，12 个乡镇通用）

## 四、配置字段说明（与现有 schema 对齐）

| 字段 | 取值 | 说明 |
|---|---|---|
| `type` | `news_site` | 与现有政府站点配置一致 |
| `scope_region_codes` | `421224` | 通山县行政区划代码（非 131000 廊坊） |
| `schedule_interval_minutes` | `60` | 乡镇站点更新频率低于新闻频道，设为 60 分钟 |
| `keywords` | `[<乡镇名>, "通山"]` | 沿用现有「地域词前缀」约定，过滤非本乡镇/非通山内容 |
| `max_articles` / `timeout` / `request_interval` | `20` / `15` / `0.5` | 与现有配置一致 |

## 五、可扩展栏目（需逐乡核实后再加，暂未纳入）

每个子平台左侧导航还含更多栏目，部分基码不统一（如 `qtzdgknr` 非固定 slug、`ldxx`/`tjxx`/`ggzypz`/`gysyjs`/`zfcg`/`zkly`/`zc_22382` 等），**不同乡镇基码可能不同**，追加前请按「抓 jgjj 页 → 读左侧导航真实链接」的方式逐乡核实，不要套用偏移公式。

## 六、导入与验证建议

1. 先小范围灰度：将 12 个源 `enabled` 保持 `true` 但 `schedule_enabled` 可临时置 `false`，手动触发 1~2 个乡镇验证采集/解析正常后再全量开启。
2. 关注 WAF：若采集器 IP 被限流，需降低并发 / 加白名单 UA，或降低 `schedule_interval_minutes`。
3. 与现有廊坊源（scope 131000）并存时，靠 `scope_region_codes` 区分地域，互不干扰。
4. 若你们的导入脚本期望「单对象单文件」（同 `人民网廊坊数据源接入配置.json` 形式），可将本数组按 `key` 拆成 12 个独立 JSON；需要我拆分可直接说。
